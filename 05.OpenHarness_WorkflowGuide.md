# OpenHarness 全流程实战向导

**OpenHarness** 是目前最具代表性的开源 Agent Harness 框架（GitHub ⭐ 15.2k+）。
本向导基于 **官方仓库 `main` 分支**（[https://github.com/HKUDS/OpenHarness](https://github.com/HKUDS/OpenHarness)）
的实际源码整理，覆盖从安装配置到多智能体协作的完整实战路径（对应 **OpenHarness v0.1.x / 2026**）。

> **版本说明（重要）**：当前最新稳定版为 v0.1.9，`pip install openharness-ai` 安装即可使用。
> 框架内置命令行入口 `oh`（Windows PowerShell 下为 `openh`），以及个人 Agent 应用 `ohmo`。
> 所有 Provider 接入、Skill 加载、Hook 生命周期均已与官方文档对照验证。

> **核心理念**：OpenHarness 将 LLM 的"智能"与"执行能力"解耦，
> 通过 **Harness（线束）** 为模型提供 **工具（Tools）+ 知识（Skills）+ 记忆（Memory）+ 安全边界（Permissions）**，
> 使任意 LLM 从单纯"回答者"升级为能真正完成任务的 **功能 Agent**。
> 公式：`Harness = Tools + Knowledge + Observation + Action + Permissions`


| 章节     | 内容                                                       |
| ------ | -------------------------------------------------------- |
| 一      | 安装与环境配置                                                  |
| 二      | 架构概览与核心概念                                                |
| 三      | Provider 多模型接入                                           |
| 四      | Agent 循环引擎（Agent Loop）                                   |
| 五      | 工具系统（43+ Tools）                                          |
| 六      | 技能系统（Skills）                                             |
| 七      | 权限与安全治理（Permissions）                                     |
| 八      | 记忆与上下文管理（Memory & Context）                               |
| 九      | 插件系统（Plugin）                                             |
| 十      | MCP 集成（Model Context Protocol）                           |
| 十一     | 多智能体协作（Multi-Agent / Swarm）                              |
| 十二     | ohmo 个人智能体应用                                             |
| **十三** | **完整端到端实战（从安装到多 Agent 协作一键跑通）**                          |
| 十四     | 自定义扩展（Tool / Skill / Plugin 开发）                         |
| 十五     | 补充知识点（Dry-run / 调试 / 非交互模式 / Web 代理 / Ollama 本地模型 / 故障排查） |
| 十六     | 总结                                                       |


---

## 一、安装与环境配置

### 1.1 安装方式

#### 一键脚本安装（Linux / macOS / WSL，推荐）

```bash
# ============================================================
# 官方一键安装脚本（Linux / macOS / WSL）
# 会自动创建虚拟环境并将 oh / ohmo / openharness 链接到
# ~/.local/bin，无需手动激活 venv
# ============================================================
curl -fsSL https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.sh | bash
```

```powershell
# ============================================================
# Windows PowerShell 一键安装
# 注意：Windows 下命令行入口为 openh，而非 oh
# （因为 oh 是 PowerShell 内置 Out-Host 别名）
# ============================================================
iex (Invoke-WebRequest -Uri 'https://raw.githubusercontent.com/HKUDS/OpenHarness/main/scripts/install.ps1')
```

#### pip 安装（推荐用于已有 Python 虚拟环境）

```bash
# ============================================================
# pip 安装 OpenHarness（最小依赖）
# openharness-ai : 框架本体，包含 oh CLI、ohmo 个人 Agent、
#                  engine/tools/skills/permissions/mcp 等全部子系统
# uv             : 高性能 Python 包管理器（可选，替代 pip），
#                  官方推荐用于开发模式
# ============================================================
pip install openharness-ai          # 稳定发布版
# 或使用 uv（更快的依赖解析）
pip install uv                      # 安装 uv 包管理器
uv pip install openharness-ai       # 用 uv 安装 openharness-ai
```

#### 开发者模式安装（源码）

```bash
# ============================================================
# 克隆源码并以可编辑模式安装
# uv sync --extra dev : 安装全部开发依赖（pytest / ruff / mypy 等）
# ============================================================
git clone https://github.com/HKUDS/OpenHarness.git  # 克隆仓库
cd OpenHarness                                        # 进入项目目录
uv sync --extra dev                                   # 安装开发依赖
uv run pytest -q                                      # 验证安装：运行全量测试（114 条）
```

### 1.2 验证安装

```bash
# ============================================================
# 验证 CLI 是否可用
# Linux/macOS/WSL 下使用 oh，Windows PowerShell 下使用 openh
# ============================================================
oh --version    # 打印 OpenHarness 版本号，例如：openharness 0.1.9
oh --help       # 打印完整 CLI 帮助文档
```

```bash
# ============================================================
# 验证 Python 包是否成功安装
# ============================================================
python -c "import openharness; print(openharness.__version__)"
# 预期输出：0.1.9（或当前最新版本号）
```

### 1.3 交互式配置向导（oh setup）

```bash
# ============================================================
# oh setup — 交互式 Provider 配置向导
# 引导用户选择 AI 提供商（Anthropic / OpenAI / Copilot 等）
# 并完成鉴权配置，全程无需手动编辑配置文件
# ============================================================
oh setup
# Windows PowerShell:
openh setup

# 向导流程：
# 1. 选择 Provider 工作流（Anthropic / OpenAI-Compatible / Claude Subscription 等）
# 2. 输入 API Key（安全存储至配置文件，不会明文写入代码）
# 3. 配置 Base URL（使用默认官方地址或自定义中转站）
# 4. 验证连通性
```

> **提示**：`oh setup` 使用"工作流（Workflow）"概念管理 Provider，
> 每个工作流对应一个具名 Profile，同时维护多个 Profile 即可在不同模型间自由切换。
> 配置文件默认存储于 `~/.openharness/settings.json`。

---

## 二、架构概览与核心概念

OpenHarness 整体运行流（按实际执行路径）：

```
用户输入（CLI 提示 / React TUI / -p 参数 / ohmo 消息）
   ↓ ① RuntimeBundle 组装          读取 settings.json、加载 Provider Profile
   ↓ ② PromptAssembler 构建        注入 CLAUDE.md + Skills + 系统提示词
   ↓ ③ QueryEngine.query()         向 LLM API 发起流式请求
   ↓ ④ API Client 流式接收          Streaming Tool-Call Cycle
   ↓ ⑤ stop_reason == "tool_use"?
         是 → ToolRegistry 分发工具调用
              PermissionChecker 鉴权
              PreToolUse Hook 触发
              Tool.execute() 执行
              PostToolUse Hook 触发
              结果追加到 messages → 回到 ③
         否 → 输出最终回答，结束
```

### 2.1 核心子系统一览

| 子系统            | 目录              | 职责说明                                              |
| -------------- | --------------- | ------------------------------------------------- |
| **引擎**         | `engine/`       | Agent Loop：流式工具调用循环、API 重试、并行工具执行、Token 计费        |
| **工具**         | `tools/`        | 43+ 工具：文件 I/O、Shell、搜索、Web、MCP 调用                 |
| **技能**         | `skills/`       | 按需加载的知识 `.md` 文件，模型需要时才注入上下文                      |
| **插件**         | `plugins/`      | 命令（Commands）、Hook、子 Agent 插件扩展                    |
| **权限**         | `permissions/`  | 多级权限模式、路径规则、命令黑名单                                 |
| **Hook**       | `hooks/`        | PreToolUse / PostToolUse 生命周期事件                   |
| **命令**         | `commands/`     | 54 条斜杠命令：`/help` `/commit` `/plan` `/resume` 等    |
| **MCP**        | `mcp/`          | Model Context Protocol 客户端，支持 HTTP 传输 + 自动重连       |
| **记忆**         | `memory/`       | 跨会话持久记忆（MEMORY.md）                                |
| **任务**         | `tasks/`        | 后台任务生命周期管理                                        |
| **多 Agent 协调** | `coordinator/`  | 子 Agent 生成、Team 注册、任务委派                           |
| **提示词组装**      | `prompts/`      | 系统提示词构建、CLAUDE.md 发现与注入                           |
| **配置**         | `config/`       | 多层配置合并、Profile 管理、版本迁移                            |
| **UI**         | `ui/`           | React/Ink TUI 后端协议 + 前端终端界面                       |

### 2.2 什么是 Agent Harness？

```
模型（Intelligence）
       ↓
  Harness（线束）
  ┌─────────────────────────────────────────────┐
  │  Tools       ← 执行能力（43 种工具）           │
  │  Skills      ← 领域知识（按需 .md 注入）        │
  │  Memory      ← 持久记忆（MEMORY.md）           │
  │  Permissions ← 安全边界（路径/命令规则）        │
  │  Hooks       ← 生命周期（Pre/PostToolUse）     │
  └─────────────────────────────────────────────┘
       ↓
  功能 Agent（真正完成任务的助手）
```

> **类比**：LLM 是"大脑"，Harness 是"身体 + 手脚 + 安全帽"。
> 没有 Harness 的模型只能"想"，有了 Harness 的模型才能真正"做"。

---

## 三、Provider 多模型接入

### 3.1 内置工作流（Workflow）

| 工作流名称                       | 协议格式              | 典型后端                                       |
| --------------------------- | ----------------- | ------------------------------------------ |
| **Anthropic-Compatible API** | Anthropic 请求格式    | Claude 官方 / Kimi / GLM / MiniMax           |
| **Claude Subscription**     | Claude CLI 订阅桥接   | 本地 `~/.claude/.credentials.json`           |
| **OpenAI-Compatible API**   | OpenAI 请求格式       | OpenAI / OpenRouter / DashScope / DeepSeek |
| **Codex Subscription**      | Codex CLI 订阅桥接    | 本地 `~/.codex/auth.json`                   |
| **GitHub Copilot**          | Copilot OAuth 流程 | GitHub Copilot 设备流授权                       |

### 3.2 Anthropic 兼容系 Provider

| 后端                  | Base URL                          | 示例模型                               |
| ------------------- | --------------------------------- | ---------------------------------- |
| **Claude 官方**       | `https://api.anthropic.com`       | `claude-sonnet-4-6`、`claude-opus-4-6` |
| **Moonshot / Kimi** | `https://api.moonshot.cn/anthropic` | `kimi-k2.5`                        |
| **智谱 / GLM**        | 自定义 Anthropic 兼容端点               | `glm-4.5`                          |
| **MiniMax**         | 自定义 Anthropic 兼容端点               | `minimax-m1`                       |

### 3.3 OpenAI 兼容系 Provider

| 后端                   | Base URL                                                  | 示例模型                            |
| -------------------- | --------------------------------------------------------- | ------------------------------- |
| **OpenAI**           | `https://api.openai.com/v1`                               | `gpt-5.4`、`gpt-4.1`            |
| **OpenRouter**       | `https://openrouter.ai/api/v1`                            | provider-specific               |
| **阿里 DashScope**     | `https://dashscope.aliyuncs.com/compatible-mode/v1`       | `qwen3.5-flash`、`deepseek-r1`   |
| **DeepSeek**         | `https://api.deepseek.com`                                | `deepseek-chat`                 |
| **SiliconFlow**      | `https://api.siliconflow.cn/v1`                           | `deepseek-ai/DeepSeek-V3`       |
| **Google Gemini**    | `https://generativelanguage.googleapis.com/v1beta/openai` | `gemini-2.5-flash`              |
| **Groq**             | `https://api.groq.com/openai/v1`                          | `llama-3.3-70b-versatile`       |
| **Ollama（本地）**       | `http://localhost:11434/v1`                               | 任意本地模型                          |
| **NVIDIA NIM**       | `https://integrate.api.nvidia.com/v1`                     | `openai/gpt-oss-120b`           |
| **GitHub Models**    | `https://models.inference.ai.azure.com`                   | `gpt-4o`                        |

### 3.4 Provider Profile 管理命令

```bash
# ============================================================
# Provider Profile 管理 — 列出 / 切换 / 添加 Provider
# ============================================================

# 列出所有已保存的 Provider Profile
# 输出示例：
#   claude-api: Anthropic-Compatible API [ready]
#   * ollama: Ollama [ready]  （* 表示当前激活的 Profile）
#     moonshot: Moonshot (Kimi) [missing auth]
oh provider list

# 切换当前激活的 Provider Profile
# <profile_name> : str — oh provider list 输出的 Profile 名称
oh provider use claude-api

# 添加自定义 Provider 端点
# --label       : str — 显示名称（用于 UI 展示）
# --provider    : str — 底层 Provider 类型（openai / anthropic / Ollama 等）
# --api-format  : str — 请求格式（openai / anthropic / copilot）
# --auth-source : str — 鉴权来源（openai_api_key / anthropic_api_key 等）
# --model       : str — 默认使用的模型名称
# --base-url    : str — API 根地址
oh provider add my-endpoint \
  --label "My Custom Endpoint" \
  --provider openai \
  --api-format openai \
  --auth-source openai_api_key \
  --model my-model-name \
  --base-url https://example.com/v1
```

### 3.5 添加 Ollama 本地模型

```bash
# ============================================================
# 配置 Ollama 作为本地模型 Provider
# 前提：Ollama 已安装并在后台运行（ollama serve）
#       目标模型已拉取（ollama pull glm4:latest）
# ============================================================

# 步骤 1：添加 Ollama Profile
oh provider add ollama \
  --label "Ollama 本地" \
  --provider Ollama \
  --api-format openai \
  --auth-source openai_api_key \
  --model glm-4.7-flash:q8_0 \
  --base-url http://localhost:11434/v1
# 输出：Saved provider profile: ollama

# 步骤 2：激活 Ollama Profile
oh provider use ollama
# 输出：Activated provider profile: ollama

# 步骤 3：验证 Profile 状态
oh provider list
# 预期输出中 ollama 显示 [ready]

# 步骤 4：启动 Agent（使用本地模型）
oh
```

### 3.6 GitHub Copilot 鉴权

```bash
# ============================================================
# 使用 GitHub Copilot 订阅作为 LLM 后端
# 无需额外 API Key，使用 GitHub OAuth 设备流授权
# ============================================================

# 步骤 1：一次性登录（会打开浏览器完成 GitHub 授权）
oh auth copilot-login

# 步骤 2：查看鉴权状态
oh auth status

# 步骤 3：使用 Copilot 启动
uv run oh --api-format copilot
# 或设置环境变量后直接启动
export OPENHARNESS_API_FORMAT=copilot
oh

# 注销 Copilot 凭证
oh auth copilot-logout
```

> **注意**：`oh auth` 子命令管理所有 Provider 的鉴权状态。
> 官方文档已记录的子命令为：
> `oh auth status`（查看全部 Profile 鉴权情况）、
> `oh auth copilot-login`（GitHub Copilot 登录）、
> `oh auth copilot-logout`（注销 Copilot 凭证）。
> 其他 Provider 的登录流程通过 `oh setup` 统一完成。

---

## 四、Agent 循环引擎（Agent Loop）

### 4.1 引擎架构

```
while True:
    # ① 向 LLM 发起流式请求，传入当前 messages 列表和工具定义
    response = await api.stream(messages, tools)

    # ② 判断停止原因
    if response.stop_reason != "tool_use":
        break  # 模型不再调用工具 → 输出最终回答

    # ③ 逐个处理工具调用请求
    for tool_call in response.tool_uses:
        # 权限检查（PermissionChecker）
        # → PreToolUse Hook 触发
        # → Tool.execute() 实际执行
        # → PostToolUse Hook 触发
        result = await harness.execute_tool(tool_call)

    # ④ 将工具执行结果追加到 messages，让模型看到结果
    messages.append(tool_results)
    # 循环继续 — 模型根据结果决定下一步动作
```

> **设计哲学**：模型决定"做什么"，Harness 负责"怎么做"——安全、高效、全程可观测。

### 4.2 引擎核心特性

| 特性                           | 说明                                        |
| ---------------------------- | ----------------------------------------- |
| **流式工具调用循环**                 | Streaming 接收 LLM 输出，实时检测 tool_use 信号       |
| **指数退避重试**                   | API 限流或瞬断时自动重试，间隔随次数指数增长                   |
| **并行工具执行**                   | 模型同时请求多个工具时，并发执行以减少等待时间                     |
| **Token 计数与费用追踪**            | 每轮记录 prompt_tokens / completion_tokens，可视化成本 |
| **最大轮次限制（`--max-turns`）**   | 防止 Agent 无限循环，默认值可在 CLI 或配置文件中设置           |

### 4.3 启动参数详解

```bash
# ============================================================
# oh CLI 完整参数说明
# Linux/macOS/WSL: oh [OPTIONS]
# Windows PowerShell: openh [OPTIONS]
# ============================================================

oh [OPTIONS] COMMAND [ARGS]

# ---- 会话控制 ----
# -c / --continue       : 继续上一次会话（恢复历史 messages）
# -r / --resume <id>    : 指定会话 ID 继续，<id> 可从 /resume 命令获取
# -n / --name <name>    : 为当前会话命名，便于后续 resume

# ---- 模型参数 ----
# -m / --model <name>   : 临时指定模型名（覆盖当前 Profile 配置）
# --effort <level>      : 推理努力程度（具体可用值由 Provider 决定，适用于支持的模型）
# --max-turns <n>       : Agent Loop 最大轮次，超出后自动终止

# ---- 输出控制 ----
# -p / --print <prompt> : 非交互模式，输入 prompt 直接输出结果到 stdout
# --output-format       : 输出格式 text（默认）| json | stream-json

# ---- 权限模式 ----
# --permission-mode     : 权限模式 default | auto | plan
# --dangerously-skip-permissions : 跳过所有权限检查（仅沙箱环境使用！）

# ---- 上下文注入 ----
# -s / --system-prompt <text>         : 设置系统提示词（覆盖默认）
# --append-system-prompt <text>       : 追加到系统提示词末尾
# --settings <path>                   : 指定自定义 settings.json 路径

# ---- 调试与高级 ----
# -d / --debug          : 开启调试日志（打印 API 请求/响应详情）
# --mcp-config <path>   : 指定 MCP 配置文件路径
# --bare                : 裸模式，不加载任何 Skills 和系统提示词
# --dry-run             : 预检模式，不实际调用 LLM / 工具，仅展示运行时配置

# ---- 子命令 ----
# oh setup              : 交互式配置向导
# oh provider           : Provider Profile 管理（list / use / add）
# oh auth               : 鉴权管理（status / login / logout）
# oh mcp                : MCP 服务器管理
# oh plugin             : 插件管理（list / install / enable / disable）
```

---

## 五、工具系统（43+ Tools）

### 5.1 工具分类总览

| 类别          | 工具名称                                             | 功能说明                     |
| ----------- | ------------------------------------------------ | ------------------------ |
| **文件 I/O**  | `Bash`、`Read`、`Write`、`Edit`、`Glob`、`Grep`      | 核心文件操作，均内置权限检查            |
| **搜索**      | `WebFetch`、`WebSearch`、`ToolSearch`、`LSP`        | 网络搜索与代码语义搜索               |
| **Notebook** | `NotebookEdit`                                   | Jupyter Notebook 单元格编辑   |
| **子 Agent** | `Agent`、`SendMessage`、`TeamCreate`、`TeamDelete` | 子 Agent 生成与团队协作           |
| **后台任务**    | `TaskCreate/Get/List/Update/Stop/Output`         | 后台任务全生命周期管理               |
| **MCP**     | `MCPTool`、`ListMcpResources`、`ReadMcpResource`   | MCP 协议工具调用                |
| **工作流模式**   | `EnterPlanMode`、`ExitPlanMode`、`Worktree`        | 计划模式切换、Git Worktree 操作    |
| **调度**      | `CronCreate/List/Delete`、`RemoteTrigger`         | 定时任务与远程触发                 |
| **元工具**     | `Skill`、`Config`、`Brief`、`Sleep`、`AskUser`       | 技能加载、配置读写、摘要生成、用户交互       |

### 5.2 工具统一特性

每个工具均具备以下保证：

```
Tool（工具）
├── Pydantic InputModel  ← 结构化、类型安全的参数校验
├── JSON Schema          ← 自描述工具接口，模型自动理解
├── PermissionChecker    ← 执行前强制鉴权
└── Hook 集成             ← PreToolUse / PostToolUse 生命周期
```

### 5.3 在代码中直接调用工具（Python API）

> **注意**：以下为内部模块路径示例，用于理解工具执行机制；
> OpenHarness 尚未发布稳定的公开 Python SDK，下列 import 路径仅作参考，
> 建议以源码 `src/openharness/tools/` 下的实际类名为准。

```python
# ============================================================
# 直接在 Python 代码中调用 OpenHarness 工具（高级用法 / 仅供参考）
# 适用于测试、自动化脚本、自定义 Agent 集成等场景
# 注意：以下 import 路径基于源码结构，请以实际安装版本为准
# ============================================================

# 导入工具基类和执行上下文（实际路径以 src/openharness/tools/ 下为准）
from openharness.tools.base import ToolExecutionContext  # 工具执行上下文类
from openharness.tools.file import ReadTool              # 文件读取工具类

# ToolExecutionContext(permission_mode, session_id, ...)
# 构造执行上下文，设置权限模式为 auto（沙箱模式，不弹确认对话框）
context = ToolExecutionContext(permission_mode="auto")  # ToolExecutionContext 对象

# 实例化工具
read_tool = ReadTool()  # ReadTool 对象，实现了 BaseTool 接口

# 调用工具（异步，需在 async 环境中使用）
import asyncio  # 异步事件循环模块

async def demo_read_tool():
    """演示如何调用 ReadTool 读取文件内容。"""
    # ReadTool.execute(arguments, context) -> ToolResult
    #   arguments : ReadToolInput — 包含 file_path(str) 字段的 Pydantic 模型
    #   context   : ToolExecutionContext — 权限上下文
    #   返回值     : ToolResult — 包含 output(str) 字段
    result = await read_tool.execute(
        arguments={"file_path": "README.md"},  # dict: 工具输入参数
        context=context                         # ToolExecutionContext: 执行上下文
    )
    print(result.output)  # str: 文件内容

asyncio.run(demo_read_tool())  # 运行异步函数
```

### 5.4 Web 搜索与代理配置

```bash
# ============================================================
# 配置 Web 搜索行为
# 默认使用 DuckDuckGo HTML 搜索；网络受限地区可替换为
# 自托管 SearXNG 或其他兼容搜索端点
# ============================================================

# 设置自定义搜索端点（SearXNG 或其他兼容实例）
export OPENHARNESS_WEB_SEARCH_URL="https://your-searxng.example/search"

# 设置 HTTP/HTTPS 代理（web_search 和 web_fetch 默认不继承系统代理）
# 注意：代理 URL 必须是 http:// 或 https:// 开头，且不能含内嵌凭证
export OPENHARNESS_WEB_PROXY="http://127.0.0.1:7890"
```

> **安全说明**：`web_search` 和 `web_fetch` 刻意设置 `trust_env=False`，
> 不自动继承系统 `HTTP_PROXY` / `HTTPS_PROXY` 环境变量，以防止 SSRF 攻击。
> 若需代理，必须通过 `OPENHARNESS_WEB_PROXY` 显式指定。

---

## 六、技能系统（Skills）

### 6.1 技能的核心理念

Skills 是**按需加载的领域知识**，以 `.md` 文件形式存在，
仅在模型需要时才注入上下文，避免浪费 Token：

```
技能加载流程：
模型收到任务 → 判断是否需要某技能 → 调用 Skill 工具按名称加载
→ SKILL.md 内容注入 system prompt → 模型获得领域知识 → 完成任务
```

### 6.2 内置技能列表（部分）

```
内置技能（部分）：
- commit      : 创建规范、结构化的 Git commit
- review      : 代码审查（Bug / 安全 / 质量）
- debug       : 系统化调试与修复
- plan        : 编码前制定实现方案
- test        : 编写并运行测试用例
- simplify    : 重构代码使其更简洁
- pdf         : pypdf 处理 PDF（来自 anthropics/skills）
- xlsx        : Excel 操作（来自 anthropics/skills）
- ... 共 40+ 个技能
```

### 6.3 技能搜索目录优先级

```
技能加载优先级（从高到低）：

1. 项目级技能（默认启用，从 CWD 向上追溯到 git root）：
   <project>/.openharness/skills/<skill>/SKILL.md
   <project>/.agents/skills/<skill>/SKILL.md
   <project>/.claude/skills/<skill>/SKILL.md

2. 用户级技能：
   ~/.openharness/skills/<skill>/SKILL.md
   ~/.claude/skills/<skill>/SKILL.md
   ~/.agents/skills/<skill>/SKILL.md

3. 框架内置技能（最低优先级）
```

### 6.4 技能相关命令

```bash
# ============================================================
# 技能相关的 Shell 命令
# ============================================================

# 禁用项目级技能（对不受信任的仓库，防止恶意 SKILL.md 注入）
oh config set allow_project_skills false
```

以下命令在 **TUI 交互界面**中输入（不是 Shell，而是启动 `oh` 后在输入框内键入）：

```
# TUI 斜杠命令
/skills             # 列出所有已加载的技能（含来源路径和来源级别）

/deploy staging     # 调用名为 deploy 的技能，参数为 staging（需技能标记 user_invocable: true）
/review             # 直接调用 review 技能
```

### 6.5 编写自定义技能

```markdown
<!-- ============================================================
     示例：~/.openharness/skills/my-skill/SKILL.md
     最小可用的自定义技能文件格式
     ============================================================ -->

---
name: my-skill
description: 针对我的特定领域的专家指导
---

# My Skill

## When to use（触发时机）
当用户询问 [你的领域] 时使用本技能。

## Workflow（执行流程）
1. 第一步：分析用户意图
2. 第二步：检索相关信息
3. 第三步：输出结构化结果

## Guidelines（行为准则）
- 始终使用中文回答
- 引用来源时注明出处
- 代码示例遵循 PEP8 规范
```

---

## 七、权限与安全治理（Permissions）

### 7.1 三级权限模式

| 模式              | 行为         | 适用场景          |
| --------------- | ---------- | ------------- |
| **default（默认）** | 写操作/命令执行前弹出确认对话框 | 日常开发（推荐）      |
| **auto（自动）**    | 允许所有操作，不弹对话框 | CI/CD、沙箱隔离环境 |
| **plan（计划）**    | 阻断全部写操作，仅允许读 | 大型重构前的代码审阅    |

```bash
# ============================================================
# 通过 CLI 参数指定权限模式（临时，仅本次会话生效）
# ============================================================
oh --permission-mode auto    # 自动模式（CI/CD 环境）
oh --permission-mode plan    # 计划模式（只读审阅）
oh --permission-mode default # 默认交互模式

# 永久更改权限模式（写入 settings.json）
oh config set permission.mode auto
```

### 7.2 路径级别规则配置

```json
// ============================================================
// ~/.openharness/settings.json — 权限规则配置示例
// path_rules     : 数组，每项指定路径匹配模式及是否允许
// denied_commands: 数组，字符串精确匹配，禁止执行的命令模式
// ============================================================
{
  "permission": {
    "mode": "default",
    "path_rules": [
      {
        "pattern": "/etc/*",
        "allow": false
      },
      {
        "pattern": "~/.ssh/*",
        "allow": false
      },
      {
        "pattern": "/tmp/sandbox/*",
        "allow": true
      }
    ],
    "denied_commands": [
      "rm -rf /",
      "DROP TABLE *",
      "curl * | bash"
    ]
  }
}
```

### 7.3 敏感路径内置保护

> OpenHarness 内置 `PermissionChecker.sensitive_path_protection`，
> 对以下路径默认拒绝任何写操作（无论 `permission.mode` 如何设置）：
>
> - `~/.ssh/` — SSH 私钥目录
> - `~/.gnupg/` — GPG 密钥目录
> - `/etc/passwd`、`/etc/shadow` — 系统账户文件
> - `~/.openharness/settings.json` — 框架自身配置文件

---

## 八、记忆与上下文管理（Memory & Context）

### 8.1 四类上下文注入机制

```
上下文注入优先级（从高到低）：

① CLAUDE.md / OPENHARNESS.md 文件
   ← 项目/用户级别的持久指令，每次会话自动注入
   ← 从 CWD 向上发现直到 git root

② Skills（技能）
   ← 按需加载，模型决策何时调用

③ MEMORY.md（持久记忆）
   ← ~/.openharness/memory/MEMORY.md
   ← Agent 可主动读写，跨会话保留重要信息

④ Session History（会话历史）
   ← 当前会话的 messages 列表
   ← 超出上下文窗口时触发 Auto-Compaction
```

### 8.2 CLAUDE.md / OPENHARNESS.md 项目级指令

```markdown
<!-- ============================================================
     示例：<project>/CLAUDE.md  或  <project>/OPENHARNESS.md
     该文件会在每次 oh 启动时自动注入到系统提示词末尾
     适合写入项目规范、架构约定、禁止操作等持久指令
     ============================================================ -->

# 项目约定

## 技术栈
- 后端：FastAPI + SQLAlchemy 2.0
- 前端：React 18 + TypeScript
- 数据库：PostgreSQL 16

## 代码规范
- Python 代码遵循 PEP8，使用 ruff 格式化
- 所有函数必须有类型注解
- 提交信息格式：<type>(<scope>): <subject>

## 禁止操作
- 不允许直接修改 alembic/versions/ 下的历史迁移文件
- 不允许硬编码任何凭证或密钥
```

### 8.3 Auto-Compaction（自动压缩）

当会话消息接近模型上下文窗口上限时，框架自动触发压缩：
1. 保留任务状态和关键信息
2. 保留 channel 日志摘要
3. 丢弃不必要的历史细节

适合多日持续运行的长任务 Agent。在 **TUI 交互界面**输入以下斜杠命令可手动干预：

```
# TUI 斜杠命令（在 oh 启动后的输入框内键入，非 Shell）
/compact    # 手动触发上下文压缩
/status     # 查看当前上下文 Token 使用量与会话状态
```

### 8.4 持久记忆（MEMORY.md）读写

```bash
# ============================================================
# MEMORY.md — 跨会话持久记忆
# 存储路径：~/.openharness/memory/MEMORY.md（用户级）
#            <project>/.openharness/memory/MEMORY.md（项目级）
# Agent 可通过 Bash 或 Write 工具主动读写该文件
# ============================================================

# 在 TUI 中告知 Agent 记住某件事：
# "请把这个项目的数据库密码规范（不包含实际密码）写入记忆"
# Agent 会自动调用 Write 工具写入 MEMORY.md

# 恢复上次会话并读取记忆：
oh -c   # --continue：加载上次会话历史 + 记忆文件
```

### 8.5 会话管理命令

```bash
# ============================================================
# 会话恢复与历史管理（Shell 命令）
# ============================================================

# 继续最近的一次会话（加载上次历史 messages + 记忆文件）
oh --continue
oh -c              # 简写

# 命名当前会话（便于后续检索）
oh --name "feature-auth-redesign"
```

在 **TUI 交互界面**输入以下斜杠命令管理会话历史：

```
# TUI 斜杠命令（在 oh 启动后的输入框内键入，非 Shell）
/resume    # 列出全部历史会话，交互式选择要恢复的会话 ID
```

---

## 九、插件系统（Plugin）

### 9.1 插件类型

| 插件类型         | 存储位置                    | 功能               |
| ------------ | ----------------------- | ---------------- |
| **Commands** | `commands/*.md`         | 新增自定义斜杠命令        |
| **Hooks**    | `hooks/hooks.json`      | 监听工具生命周期事件       |
| **Agents**   | `agents/*.md`           | 定义专用子 Agent       |
| **MCP 服务器** | `plugin.json`           | 注册额外 MCP 工具服务器   |

### 9.2 官方测试通过的插件（部分）

| 插件名称                | 类型               | 功能说明                  |
| ------------------- | ---------------- | --------------------- |
| `commit-commands`   | Commands         | Git commit / push / PR 工作流 |
| `security-guidance` | Hooks            | 文件编辑时触发安全警告           |
| `hookify`           | Commands + Agents | 创建自定义行为 Hook          |
| `feature-dev`       | Commands         | 功能开发标准流程               |
| `code-review`       | Agents           | 多 Agent PR 审查           |
| `pr-review-toolkit` | Agents           | 专业 PR 审查工具集            |

### 9.3 插件管理命令

```bash
# ============================================================
# 插件管理命令
# ============================================================

# 列出所有已安装的插件及其状态
oh plugin list

# 安装插件（支持 GitHub 地址、本地路径）
oh plugin install https://github.com/example/my-plugin
oh plugin install ./local-plugin-dir

# 启用 / 禁用插件
oh plugin enable my-plugin
oh plugin disable my-plugin
```

### 9.4 创建自定义 Hook

```json
// ============================================================
// 示例：.openharness/plugins/my-plugin/hooks/hooks.json
// PreToolUse  : 工具执行前触发的 Hook（可阻断执行）
// PostToolUse : 工具执行后触发的 Hook（仅观测）
// matcher     : 触发条件，支持工具名称过滤
// ============================================================
{
  "hooks": [
    {
      "type": "PreToolUse",
      "matcher": {
        "tool_name": "Bash"
      },
      "command": "echo '[HOOK] 即将执行 Shell 命令，请注意安全' >&2"
    },
    {
      "type": "PostToolUse",
      "matcher": {
        "tool_name": "Write"
      },
      "command": "echo '[HOOK] 文件写入完成' >&2"
    }
  ]
}
```

---

## 十、MCP 集成（Model Context Protocol）

### 10.1 MCP 概述

**MCP（Model Context Protocol）** 是 Anthropic 提出的开放协议，
允许 LLM 通过标准化接口调用外部服务的工具和资源。
OpenHarness 内置完整 MCP 客户端，支持 **STDIO** 和 **HTTP** 两种传输方式。

```
MCP 调用链路：
OpenHarness Agent
  → MCPTool（工具调用）
  → MCP Client（HTTP / STDIO）
  → MCP Server（外部服务，如数据库 / 文件系统 / 搜索 API）
  → 返回结果注入 messages
```

### 10.2 配置 MCP 服务器

```json
// ============================================================
// ~/.openharness/mcp_config.json — MCP 服务器配置示例
// mcpServers : dict — 服务器名称 → 连接配置的映射
// command    : str  — STDIO 模式：启动服务器的命令
// args       : list — STDIO 模式：命令行参数列表
// url        : str  — HTTP 模式：MCP 服务器的 HTTP 端点地址
// ============================================================
{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/tmp/workspace"]
    },
    "sqlite": {
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "/tmp/my_database.db"]
    },
    "remote-search": {
      "url": "https://mcp.example.com/search"
    }
  }
}
```

### 10.3 MCP 命令行管理

```bash
# ============================================================
# MCP 服务器管理命令（Shell）
# ============================================================

# 列出已配置的 MCP 服务器及连接状态
oh mcp list

# 启动时指定 MCP 配置文件
oh --mcp-config ./custom_mcp_config.json

# 使用 dry-run 验证 MCP 配置是否正确
oh --dry-run -p "使用文件系统工具列出 /tmp 目录"
# 若 MCP 配置有误，dry-run 会报告 warning 或 blocked 状态
```

在 **TUI 交互界面**中还可以使用以下斜杠命令：

```
# TUI 斜杠命令（在 oh 启动后的输入框内键入，非 Shell）
/mcp-resources    # 列出所有已连接的 MCP 服务器及其暴露的资源
```

### 10.4 在 Agent 中调用 MCP 工具

```
# 在交互式会话中，Agent 会自动发现并调用已配置的 MCP 工具
# 例如：配置了 filesystem MCP 服务器后，可以这样提问：

用户：请用 filesystem 工具列出 /tmp/workspace 下的所有 Python 文件

Agent 执行流程：
  1. 识别需要调用 MCPTool
  2. PermissionChecker 检查权限（default 模式下弹确认对话框）
  3. MCPTool.execute() → MCP Client → filesystem Server
  4. 返回文件列表，注入 messages
  5. Agent 基于结果给出回答
```

---

## 十一、多智能体协作（Multi-Agent / Swarm）

### 11.1 多 Agent 架构

```
主 Agent（Orchestrator）
├── 后台任务 1（TaskCreate）     ← 独立进程，异步执行
│     └── 子 Agent 执行代码编写
├── 后台任务 2（TaskCreate）     ← 独立进程，异步执行
│     └── 子 Agent 执行单元测试
└── 后台任务 3（TaskCreate）     ← 独立进程，异步执行
      └── 子 Agent 执行文档生成
          ↓
主 Agent 汇总结果 → 输出最终答案
```

### 11.2 子 Agent 生成工具

| 工具名称           | 功能                       |
| -------------- | ------------------------ |
| `Agent`        | 同步创建子 Agent 并等待结果（阻塞）    |
| `TaskCreate`   | 异步创建后台任务（非阻塞，立即返回任务 ID） |
| `TaskGet`      | 查询指定任务的状态和结果             |
| `TaskList`     | 列出所有后台任务                 |
| `TaskUpdate`   | 更新任务参数                   |
| `TaskStop`     | 终止指定任务                   |
| `TaskOutput`   | 读取任务的实时输出流               |
| `SendMessage`  | 向指定子 Agent 发送消息          |
| `TeamCreate`   | 创建多 Agent 团队（含注册表）       |
| `TeamDelete`   | 解散团队                     |

### 11.3 实战：创建并行子 Agent 团队

```
# 在交互式会话中，可以这样指挥主 Agent：

用户：请并行完成以下三个任务：
      1. 审查 src/api/ 目录下的代码安全问题
      2. 为 src/models/ 编写单元测试
      3. 生成项目的 API 文档

主 Agent 执行流程：
  1. TeamCreate — 创建包含 3 个专职子 Agent 的团队
  2. TaskCreate × 3 — 同时启动 3 个后台任务
     - 子 Agent A：专注安全审查（使用 security-guidance 技能）
     - 子 Agent B：专注测试编写（使用 test 技能）
     - 子 Agent C：专注代码简化与文档生成（使用 simplify 技能，或自定义 docs 技能）
  3. TaskGet × 3 — 轮询各任务状态（或等待完成通知）
  4. 汇总三个子 Agent 的输出，生成综合报告
```

### 11.4 子 Agent 配置文件

```markdown
<!-- ============================================================
     示例：.agents/agents/security-reviewer.md
     定义一个专注安全审查的专职子 Agent
     ============================================================ -->

---
name: security-reviewer
description: 专职代码安全审查 Agent
tools:
  - Read
  - Grep
  - Glob
  - WebSearch
skills:
  - review
  - security-guidance
permission_mode: plan
---

# Security Reviewer Agent

你是一名专职代码安全审查专家，负责：
1. 识别 SQL 注入、XSS、CSRF 等常见漏洞
2. 检查硬编码密钥和敏感信息泄露
3. 验证输入校验和输出编码
4. 给出具体的修复建议（含代码示例）

输出格式：
- 风险等级：高 / 中 / 低
- 漏洞位置：文件路径 + 行号
- 漏洞描述：具体问题说明
- 修复建议：修复代码片段
```

---

## 十二、ohmo 个人智能体应用

### 12.1 ohmo 是什么？

**ohmo** 是构建在 OpenHarness 之上的个人 AI 助手应用，
支持通过 **Feishu / Slack / Telegram / Discord** 等即时通讯工具与 AI 交互：

```
用户（Feishu/Slack/Telegram/Discord）
   ↓ 发送消息
ohmo Gateway（消息网关）
   ↓ 解析消息 / 管理会话
ohmo Agent（基于 OpenHarness Harness）
   ↓ 工具调用（fork 分支、写代码、跑测试、开 PR）
代码仓库 / CI 系统
```

> **关键优势**：`ohmo` 复用已有的 **Claude Code** 或 **Codex** 订阅，无需额外 API Key 费用。

### 12.2 ohmo 工作空间结构

```
~/.ohmo/                         # ohmo 个人工作空间根目录
├── soul.md                      # 长期 Agent 人格与行为定义
├── identity.md                  # ohmo 的身份定义
├── user.md                      # 用户个人偏好与背景
├── BOOTSTRAP.md                 # 首次运行的引导仪式
├── memory/                      # 个人记忆存储目录
│   └── MEMORY.md                # 跨会话持久记忆
└── gateway.json                 # Provider Profile + 渠道配置
```

### 12.3 ohmo 快速入门

```bash
# ============================================================
# ohmo 完整启动流程（首次配置）
# ============================================================

# 步骤 1：初始化个人工作空间（只需执行一次）
# 创建 ~/.ohmo/ 目录结构及默认配置文件
ohmo init

# 步骤 2：配置 Provider 和消息渠道
# 交互式向导，选择 Provider（同 oh setup）并配置目标渠道
# 支持：Telegram / Slack / Discord / Feishu
ohmo config

# 步骤 3：启动 ohmo 个人 Agent（本地交互模式）
ohmo

# 步骤 4：启动消息网关（使 ohmo 在即时通讯 App 中可用）
ohmo gateway run      # 前台运行（会阻塞终端）
ohmo gateway start    # 后台运行（推荐生产环境）

# 步骤 5：查看网关状态
ohmo gateway status

# 重启网关（更新配置后执行）
ohmo gateway restart
```

### 12.4 ohmo 渠道配置（以 Telegram 为例）

```bash
# ============================================================
# Telegram 渠道配置流程
# 前提：已通过 @BotFather 创建 Telegram Bot 并获取 Token
# ============================================================

# 执行渠道配置向导
ohmo config
# 向导步骤：
# 1. 选择渠道类型：Telegram
# 2. 输入 Bot Token：<your-bot-token>
# 3. 输入授权用户的 Chat ID（只允许该 ID 与 ohmo 交互）
# 4. 选择 Provider Profile（同 oh provider list 中的 Profile）
# 5. 保存配置到 ~/.ohmo/gateway.json

# 配置完成后启动网关
ohmo gateway start

# 在 Telegram 中向 Bot 发送消息即可开始使用
# 支持文件附件（多模态输入）和斜杠命令
```

### 12.5 ohmo 斜杠命令

```
# 在 Telegram / Slack 等渠道中支持的斜杠命令：

/status     — 查看 ohmo 当前状态和运行中的任务
/resume     — 恢复指定会话
/compact    — 手动压缩上下文
/skills     — 列出已加载的技能
/help       — 显示帮助信息
/memory     — 查看持久记忆摘要
```

---

## 十三、完整端到端实战（从安装到多 Agent 协作一键跑通）

### 13.1 实战目标

本节演示从零开始的完整链路：
**安装 → 配置 Provider → 启动 Agent → 使用 Skills → 触发多 Agent 协作 → 通过 ohmo 在 Telegram 中控制**

### 13.2 第一阶段：基础链路验证

```bash
# ============================================================
# 阶段一：安装与基础验证（约 5 分钟）
# ============================================================

# 步骤 1：安装
pip install openharness-ai

# 步骤 2：配置 Provider（以 DeepSeek 为例，性价比高）
oh setup
# 向导中选择：OpenAI-Compatible API
# 输入 API Key：sk-xxxxxxxxxxxx
# 输入 Base URL：https://api.deepseek.com
# 输入模型名称：deepseek-chat

# 步骤 3：验证连通性（dry-run 预检）
oh --dry-run -p "你好，请介绍一下你自己"
# 预期：输出 ready 状态，显示已配置的 Provider、Tools、Skills 列表

# 步骤 4：首次交互（非交互模式，最快验证）
oh -p "请用一句话解释什么是 Agent Harness"
# 预期：模型直接输出回答，无需工具调用

# 步骤 5：进入交互式 TUI
oh
# 预期：显示 OpenHarness 欢迎界面，可正常输入问题
```

### 13.3 第二阶段：工具调用验证

```bash
# ============================================================
# 阶段二：验证工具系统（约 3 分钟）
# ============================================================

# 在交互式 TUI 中逐步测试以下任务：

# 任务 1：文件系统工具
# "请列出当前目录下的所有 Python 文件并统计行数"
# 预期：Agent 调用 Bash 工具执行 find / wc 命令，弹出权限确认对话框

# 任务 2：Web 搜索工具
# "请搜索 OpenHarness 的最新版本号是多少"
# 预期：Agent 调用 WebSearch 工具，返回搜索结果摘要

# 任务 3：多工具协作
# "请读取 README.md 文件，提取其中所有的代码块，并生成一份代码摘要"
# 预期：Agent 先调用 Read 工具读取文件，再调用 Grep 提取代码块，最后生成摘要
```

### 13.4 第三阶段：Skills 与 Plugin 验证

```bash
# ============================================================
# 阶段三：验证技能与插件（约 5 分钟）
# ============================================================

# 创建一个有 Bug 的示例文件
cat > /tmp/buggy_code.py << 'EOF'
def divide(a, b):
    return a / b  # 没有处理除以零的情况

result = divide(10, 0)
print(result)
EOF

# 在 TUI 中使用 debug 技能修复 Bug
# "请使用 debug 技能分析并修复 /tmp/buggy_code.py 中的问题"
# 预期：
# 1. Agent 调用 Skill 工具加载 debug 技能
# 2. 调用 Read 工具读取文件
# 3. 识别出除以零的 Bug
# 4. 调用 Write 工具写入修复后的代码

# 在 TUI 中使用 review 技能审查代码
# "请对修复后的文件进行 code review"
# 预期：Agent 加载 review 技能，给出结构化的审查报告

# 在 TUI 输入框中键入以下命令列出所有可用技能：
# /skills
```

### 13.5 第四阶段：多 Agent 协作

```bash
# ============================================================
# 阶段四：验证多 Agent 协作（约 10 分钟）
# ============================================================

# 准备一个小型代码库用于测试
mkdir -p /tmp/test-project/src
cat > /tmp/test-project/src/calculator.py << 'EOF'
def add(a, b):
    return a + b

def subtract(a, b):
    return a - b

def multiply(a, b):
    return a * b

def divide(a, b):
    return a / b
EOF

# 进入测试项目目录
cd /tmp/test-project

# 在 TUI 中发起多 Agent 任务：
# "请并行完成以下三件事：
#  1. 用安全审查技能检查 src/calculator.py
#  2. 为 src/calculator.py 编写完整的单元测试文件 tests/test_calculator.py
#  3. 为 src/calculator.py 生成 docstring 文档
#  完成后汇总结果"

# 预期：
# 1. Agent 调用 TaskCreate × 3，创建 3 个并行后台任务
# 2. 三个子 Agent 各自独立执行
# 3. 主 Agent 通过 TaskGet 轮询状态
# 4. 所有任务完成后，主 Agent 汇总生成综合报告
```

### 13.6 第五阶段：ohmo 消息渠道验证

```bash
# ============================================================
# 阶段五：ohmo 个人 Agent 验证（约 10 分钟，需 Telegram Bot）
# ============================================================

# 步骤 1：初始化 ohmo 工作空间
ohmo init

# 步骤 2：配置渠道（Telegram）
ohmo config
# 按向导填入 Bot Token 和授权 Chat ID

# 步骤 3：启动网关
ohmo gateway start

# 步骤 4：在 Telegram 中发送消息测试
# "帮我在 /tmp/test-project 目录下创建一个 README.md 文件，
#  内容包含项目简介和 API 使用示例"

# 预期：
# ohmo 在 Telegram 中回复确认消息，
# 调用 Write 工具生成 README.md，
# 完成后发送成功提示

# 步骤 5：查看网关状态
ohmo gateway status
```

---

## 十四、自定义扩展（Tool / Skill / Plugin 开发）

### 14.1 开发自定义工具

> **说明**：以下 import 路径基于源码结构（`src/openharness/tools/`），
> 开发自定义工具时请先阅读仓库中 `src/openharness/tools/base.py` 的实际接口定义，
> 以当前版本源码为准。

```python
# ============================================================
# 自定义工具开发模板
# 继承 BaseTool，实现 execute() 方法即可注册为可用工具
# ============================================================

from pydantic import BaseModel, Field              # BaseModel: Pydantic 基类；Field: 字段描述
from openharness.tools.base import (
    BaseTool,              # BaseTool: 所有工具的基类，提供 Pydantic 校验和 Schema 生成
    ToolExecutionContext,  # ToolExecutionContext: 工具执行上下文（含权限模式、会话 ID 等）
    ToolResult,            # ToolResult: 工具执行结果容器，包含 output(str) 字段
)


# ---- 1. 定义工具的输入参数模型 ----
class DatabaseQueryInput(BaseModel):
    """数据库查询工具的输入参数模型（Pydantic BaseModel）。"""
    sql: str = Field(description="要执行的 SQL 查询语句")  # str: SQL 语句
    database: str = Field(
        default="default",                              # str: 数据库名称，默认 default
        description="目标数据库名称"
    )


# ---- 2. 实现工具类 ----
class DatabaseQueryTool(BaseTool):
    """自定义数据库查询工具。

    Attributes:
        name        : str — 工具名称（模型调用时使用此名称）
        description : str — 工具功能说明（注入 tools 列表，模型据此决策）
        input_model : Type[BaseModel] — 输入参数的 Pydantic 模型类
    """
    name = "database_query"                           # str: 工具标识名
    description = "执行 SQL 查询并返回结果（只读操作）"  # str: 工具功能描述
    input_model = DatabaseQueryInput                   # Type[BaseModel]: 输入模型类

    async def execute(
        self,
        arguments: DatabaseQueryInput,    # DatabaseQueryInput: 经 Pydantic 校验的输入
        context: ToolExecutionContext,    # ToolExecutionContext: 执行上下文
    ) -> ToolResult:
        """执行 SQL 查询。

        Args:
            arguments : DatabaseQueryInput — 包含 sql(str) 和 database(str) 的输入对象
            context   : ToolExecutionContext — 执行上下文，含权限模式等信息

        Returns:
            ToolResult — output(str) 字段包含查询结果的字符串表示
        """
        # 实际生产中替换为真实数据库连接逻辑
        mock_result = f"查询数据库 [{arguments.database}] 执行：{arguments.sql}"
        return ToolResult(output=mock_result)  # ToolResult: 工具执行结果
```

### 14.2 开发自定义技能

```markdown
<!-- ============================================================
     示例：~/.openharness/skills/sql-expert/SKILL.md
     SQL 专家技能：为 Agent 注入数据库设计和优化领域知识
     ============================================================ -->

---
name: sql-expert
description: 关系型数据库设计与 SQL 查询优化专家知识
user_invocable: true
---

# SQL 专家技能

## 触发时机（When to use）
当用户：
- 询问数据库表设计或 ER 图
- 请求 SQL 查询优化
- 需要索引设计建议
- 处理数据库性能问题

## 核心知识（Core Knowledge）

### 索引设计原则
1. 高基数列（Cardinality 高）优先建索引
2. 联合索引遵循最左前缀原则
3. 避免在频繁更新列上建索引
4. 使用 EXPLAIN 分析查询计划

### 查询优化模式
- SELECT * → 明确列名（减少 I/O）
- 子查询 → JOIN（减少嵌套扫描）
- OR 条件 → UNION（利用索引）
- LIKE '%xxx' → 全文索引或倒排表

## 输出格式（Output Format）
- 提供优化前后的 SQL 对比
- 附上 EXPLAIN 分析结果解读
- 给出预期性能提升估算
```

### 14.3 开发自定义插件

```
# ============================================================
# 自定义插件目录结构
# ============================================================
.openharness/plugins/my-plugin/
├── .claude-plugin/
│   └── plugin.json          # 插件元信息
├── commands/
│   └── deploy.md            # 自定义 /deploy 命令
├── hooks/
│   └── hooks.json           # Hook 配置
└── agents/
    └── deployer.md          # 专职部署子 Agent
```

```json
// ============================================================
// .openharness/plugins/my-plugin/.claude-plugin/plugin.json
// 插件元信息配置
// ============================================================
{
  "name": "my-plugin",
  "version": "1.0.0",
  "description": "自定义部署工作流插件",
  "commands": ["commands"],
  "hooks": ["hooks/hooks.json"],
  "agents": ["agents"]
}
```

```markdown
<!-- ============================================================
     .openharness/plugins/my-plugin/commands/deploy.md
     自定义 /deploy 斜杠命令定义
     ============================================================ -->

---
name: deploy
description: 执行标准部署流程（测试 → 构建 → 推送 → 发布）
---

# Deploy Command

执行以下部署步骤：
1. 运行完整测试套件：`pytest -q`，失败则中止
2. 构建 Docker 镜像：`docker build -t app:latest .`
3. 推送到镜像仓库：`docker push registry.example.com/app:latest`
4. 更新 Kubernetes Deployment：`kubectl rollout restart deployment/app`
5. 验证滚动更新状态：`kubectl rollout status deployment/app`
6. 报告部署结果（成功/失败 + 耗时）

环境参数（通过 $1 传入）：staging | production
```

---

## 十五、补充知识点

### 15.1 Dry-run 预检模式

```bash
# ============================================================
# --dry-run：静态预检，不调用 LLM / 工具 / 子 Agent
# 适用于：首次部署验证 / CI 配置检查 / 排查配置问题
# ============================================================

# 预检交互式会话配置
oh --dry-run
# 输出：Provider 状态 / 已加载 Skills / 可用 Tools / MCP 服务器连通性

# 预检特定 Prompt（不执行模型）
oh --dry-run -p "审查 src/ 目录的代码质量"
# 输出：
#   Readiness: ready
#   Provider: claude-api [authenticated]
#   Skills: review (matched), debug (matched)
#   Tools: Read, Grep, Glob (会被用到)
#   Next action: run directly → oh -p "审查 src/ 目录的代码质量"

# 预检斜杠命令
oh --dry-run -p "/deploy staging"
# 若命令不存在：Readiness: blocked — unknown slash command /deploy

# 获取结构化预检报告（JSON 格式）
oh --dry-run -p "Fix the failing tests" --output-format json
```

**Dry-run 就绪等级说明：**

| 等级          | 含义                            | 典型下一步                              |
| ----------- | ----------------------------- | ---------------------------------- |
| `ready`     | 配置可用，可直接执行                    | 直接运行 `oh -p "..."` 或 `oh`         |
| `warning`   | 可以解析会话，但有潜在问题（如 MCP 配置错误）     | 修复 MCP 配置或忽略警告后运行                  |
| `blocked`   | 当前配置无法成功执行（如未知命令 / 未鉴权）       | 先执行 `oh setup` 重新配置，或修复配置后重试     |

### 15.2 非交互模式（管道与脚本）

```bash
# ============================================================
# 非交互模式：适合 CI/CD 流水线、Shell 脚本自动化
# ============================================================

# 单次 Prompt 输出到 stdout（text 格式）
oh -p "解释 src/main.py 中的主要逻辑"

# JSON 格式输出（含完整消息历史和工具调用记录）
oh -p "列出 src/ 下所有函数名" --output-format json

# 流式 JSON（实时接收每个事件）
oh -p "修复所有 linting 错误" --output-format stream-json

# 在 CI 中使用（全自动模式，不弹权限确认）
oh -p "运行测试并报告覆盖率" \
   --permission-mode auto \
   --output-format json \
   --max-turns 20
```

### 15.3 调试与日志

```bash
# ============================================================
# 调试模式：--debug 开启详细日志
# 输出：API 请求/响应详情、工具调用参数、权限检查结果
# ============================================================
oh --debug -p "帮我调试这个问题"

# 官方已记录的 oh config 子命令（仅 set 有完整文档）：
# oh config set <key> <value> — 写入配置项
oh config set permission.mode default    # str: 权限模式（default / auto / plan）
oh config set allow_project_skills false # bool: 禁用项目级技能（防恶意 SKILL.md）

# 注意：oh config list / get / reset 在官方 README 中尚未明确收录，
# 如需查看完整配置，可直接打开 ~/.openharness/settings.json 文件
```

### 15.4 常见故障排查

| 问题                              | 可能原因                     | 解决方案                                         |
| ------------------------------- | ------------------------ | -------------------------------------------- |
| `oh: command not found`         | 安装路径未加入 PATH              | 将 `~/.local/bin` 加入 PATH，或用 `uv run oh`       |
| Windows 下 `oh` 无响应             | `oh` 被 PowerShell 解析为 `Out-Host` | 改用 `openh`                              |
| API Key 鉴权失败                   | Key 未配置或已过期               | 执行 `oh setup` 重新配置                           |
| MCP 服务器连接失败                    | 服务器未启动或配置错误              | 执行 `oh --dry-run` 查看 MCP 状态                  |
| 上下文窗口超限                        | 会话消息过多                   | 执行 `/compact` 手动压缩                           |
| macOS Terminal.app 退格键异常        | 终端发送的 Delete 序列不兼容        | 升级 OpenHarness 到最新版，或改用 iTerm2             |
| `allow_project_skills` 安全警告     | 进入了不受信任的仓库目录             | 执行 `oh config set allow_project_skills false` |

### 15.5 使用 uv 管理开发环境

```bash
# ============================================================
# uv — 官方推荐的高性能 Python 包管理器
# 相比 pip 依赖解析速度快约 10~100 倍
# ============================================================

# 安装 uv
pip install uv
# 或（macOS/Linux）
curl -LsSf https://astral.sh/uv/install.sh | sh

# 使用 uv 安装 OpenHarness
uv pip install openharness-ai

# 使用 uv 运行 oh（无需激活虚拟环境）
uv run oh

# 开发模式：克隆仓库后同步全部依赖
git clone https://github.com/HKUDS/OpenHarness.git
cd OpenHarness
uv sync --extra dev           # 安装含开发工具的全量依赖
uv run pytest -q              # 运行全量测试（114 条单元/集成测试）
uv run pytest tests/e2e/ -v   # 运行 E2E 测试
```

### 15.6 版本历史速览

| 版本           | 发布日期       | 核心变更                                     |
| ------------ | ---------- | ---------------------------------------- |
| **v0.1.9**   | 2026 最新    | 当前稳定版                                    |
| **v0.1.8**   | —          | 稳定性修复与内部优化（详见仓库 RELEASE_NOTES_v0.1.8.md）  |
| **v0.1.7**   | 2026-04-18 | 安装脚本链接优化；React TUI 支持 Shift+Enter 换行；Windows 动画优化 |
| **v0.1.6**   | 2026-04-10 | Auto-Compaction；Markdown TUI 渲染；ohmo 渠道斜杠命令 |
| **v0.1.5**   | 2026-04-08 | MCP HTTP 传输；Swarm 轮询；多模态文件附件              |
| **v0.1.4**   | 2026-04-08 | Moonshot/Kimi 原生支持；多 Provider 鉴权重构；安全加固   |
| **v0.1.2**   | 2026-04-06 | `oh setup` 工作流统一；`ohmo` 首发（个人 Agent App）   |
| **v0.1.0**   | 2026-04-01 | OpenHarness 开源首发，完整 Harness 架构            |

---

## 十六、总结

### 16.1 核心概念回顾

```
OpenHarness 知识地图：

① 安装：pip install openharness-ai
         oh setup → 配置 Provider Profile

② 核心引擎：Agent Loop（while True + stream + tool_use 判断）

③ 五大支柱：
   Tools      → 43+ 工具，每个都有权限保护
   Skills     → 按需加载的 .md 知识文件
   Memory     → CLAUDE.md + MEMORY.md + Session History
   Permissions→ default / auto / plan 三级模式
   Hooks      → PreToolUse / PostToolUse 生命周期

④ 多 Provider：
   oh provider add / use → 自由切换 Anthropic / OpenAI / Ollama 等

⑤ 多 Agent：
   TaskCreate（后台任务）+ TeamCreate（Agent 团队）+ Agent 委派

⑥ MCP 集成：
   配置 mcp_config.json → 自动发现并调用外部 MCP 服务器的工具

⑦ 个人 Agent（ohmo）：
   ohmo init → ohmo config → ohmo gateway start
   → 在 Telegram/Slack/Feishu 中使用 AI
```

### 16.2 工具命令速查表

| 场景              | 命令                                            |
| --------------- | ----------------------------------------------- |
| 首次安装与配置         | `pip install openharness-ai && oh setup`        |
| 启动交互式 TUI      | `oh`（Windows: `openh`）                         |
| 非交互式 Prompt     | `oh -p "你的问题"`                                |
| 继续上次会话          | `oh -c`                                         |
| 切换 Provider      | `oh provider use <profile>`                     |
| 查看可用技能          | `/skills`（TUI 内）                              |
| 预检配置            | `oh --dry-run -p "你的问题"`                      |
| 开启调试日志          | `oh --debug`                                    |
| 仅读模式（审阅代码）      | `oh --permission-mode plan`                     |
| CI/CD 自动化模式     | `oh -p "任务" --permission-mode auto --output-format json` |
| 添加自定义 Provider   | `oh provider add <name> --provider openai ...`  |
| 管理插件            | `oh plugin list / install / enable`             |
| 启动个人 Agent 网关   | `ohmo gateway start`                            |

### 16.3 与同类框架对比

| 维度           | OpenHarness                      | LangChain                | AutoGen              |
| ------------ | -------------------------------- | ------------------------ | -------------------- |
| **定位**       | 轻量级 Agent Harness 基础设施          | LLM 应用开发框架               | 多 Agent 对话框架         |
| **核心抽象**     | Harness（工具+技能+记忆+权限）            | LCEL 管道（Runnable）        | Agent 对话图             |
| **工具数量**     | 43+ 内置工具                        | 依赖社区包                    | 有限内置                 |
| **权限控制**     | 内置三级权限模式 + 路径规则                 | 无内置权限层                   | 无内置权限层               |
| **CLI 体验**   | 一流（`oh`、React TUI、ohmo）         | 无专用 CLI                  | 无专用 CLI              |
| **本地模型**     | 原生支持 Ollama                     | 通过 langchain-community 支持 | 通过 litellm 支持        |
| **个人 Agent** | ohmo（集成 Feishu/Telegram 等）      | 无                        | 无                    |
| **学习曲线**     | 低（一条命令启动）                       | 中（需学习 LCEL / 组件体系）       | 中高（需理解多 Agent 图）      |

### 16.4 适用场景建议

| 场景                        | 推荐方案                                            |
| ------------------------- | ------------------------------------------------ |
| 快速搭建代码助手 / 仓库分析 Agent    | `oh` 直接启动，使用内置 Tools + Skills                   |
| CI/CD 自动化（测试 / 部署 / 审查）  | `oh -p "..." --permission-mode auto --output-format json` |
| 个人效率助手（连接即时通讯 App）       | `ohmo init && ohmo gateway start`                |
| 本地模型实验（隐私敏感数据）           | Ollama + `oh provider add ollama ...`            |
| 多 Agent 并行任务（大型代码库处理）    | TaskCreate + TeamCreate 并行子 Agent               |
| 研究 Agent 底层实现原理           | 源码阅读 `src/engine/` 和 `src/tools/` 目录           |

> **最终建议**：
> 不论你是研究员、个人开发者还是工程团队，
> OpenHarness 提供了从"玩具级 Agent Demo"到"生产可用 AI 工作流"的完整路径。
> 从 `oh setup && oh` 两条命令出发，按本向导逐章深入，
> 你将在 2 小时内搭建出一个真正能"做事"的 AI Agent。

---

*本向导基于 OpenHarness 官方仓库 `main` 分支（[https://github.com/HKUDS/OpenHarness](https://github.com/HKUDS/OpenHarness)）整理*  
*对应版本：v0.1.x / 2026 年 8 月*
