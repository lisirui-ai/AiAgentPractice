# LangChain 全流程实战向导

**LangChain** 是目前最流行的 LLM 应用开发框架（GitHub ⭐ 142k+）。
本向导基于 **官方仓库 `master` 分支**（[https://github.com/langchain-ai/langchain](https://github.com/langchain-ai/langchain)）
的实际源码整理，所有 API 均已与官方文档对照验证（对应 **LangChain v1.x / 2026**）。

> **版本说明（重要）**：2025 年 10 月 LangChain v1.0 正式发布，是当前 `pip install langchain` 所安装的版本。
> v1.x 精简了核心包命名空间，将遗留类（`AgentExecutor`、`create_tool_calling_agent`、`LLMChain` 等旧式 Chain）
> 移至独立的 `langchain-classic` 包维护；新标准 Agent API 为 `create_agent`。
> v0.3.x 进入维护模式（安全补丁），维护至 2026 年 12 月。

> **核心理念**：LangChain 提供统一的 **Runnable 接口** + **LCEL 管道语法（`|`）**，
> 将 Chat Model / Prompt / Parser / Retriever / Tool / Agent 等组件自由组合，
> 构建从简单问答到复杂多步 Agent 的各类 LLM 应用。


| 章节     | 内容                                                                                                                                          |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 一      | 安装与环境配置                                                                                                                                     |
| 二      | LangChain 架构概览                                                                                                                              |
| 三      | Chat Model（模型接口）                                                                                                                            |
| 四      | Prompt Template（提示词模板）                                                                                                                      |
| 五      | Output Parser（输出解析器）                                                                                                                        |
| 六      | LCEL 链式组合（`\|` 管道）                                                                                                                          |
| 七      | 对话记忆（Memory / History）                                                                                                                      |
| 八      | RAG（检索增强生成）                                                                                                                                 |
| 九      | Tool 与 Agent                                                                                                                                |
| 十      | 结构化输出（Structured Output）                                                                                                                    |
| **十一** | **完整端到端流程（RAG + Agent 一键跑通）**                                                                                                               |
| 十二     | 补充知识点（LLMs/Token/消息类型/异步/多平台/PromptTemplate/RunnablePassthrough.assign/RunnableParallel/RunnableBranch/create_stuff_documents_chain/Callbacks/旧链迁移） |
| 十三     | 总结                                                                                                                                          |


---

## 一、安装与环境配置

### 1.1 安装依赖

```python
# ============================================================
# 安装 LangChain 核心依赖（2026 / v1.x）
# ------------------------------------------------------------
# langchain          : 框架本体，提供 LCEL / Agent / Chain 等高级抽象
# langchain-core     : 核心抽象层（Runnable / BaseMessage / PromptTemplate 等）
#                      langchain 依赖 langchain-core，通常不单独安装
# langchain-openai   : OpenAI 集成包（ChatOpenAI / OpenAIEmbeddings）
# langchain-community: 社区集成包（FAISS / BM25Retriever / 文档加载器等）
# langchain-chroma   : Chroma 向量数据库集成（RAG 常用）
# python-dotenv      : 从 .env 文件加载 API Key 等环境变量
# ============================================================
%pip install langchain langchain-openai langchain-community langchain-chroma langchain-classic python-dotenv --quiet
# --quiet     : 减少冗余安装日志输出
# langchain   : 框架本体（当前为 v1.x）；langchain-classic 保留 v0.3.x 遗留类
#               AgentExecutor / create_tool_calling_agent / LLMChain 等旧式 API 均来自 langchain-classic
# 注意：langchain-text-splitters 是 langchain 的传递依赖，会随 langchain 自动安装，无需单独列出；
#       若安装后 import 报错，可手动补装：pip install langchain-text-splitters
```

```python
# ============================================================
# 验证关键依赖是否安装成功
# ============================================================
import importlib  # 动态导入模块工具，用于版本检查
import sys        # 系统信息模块，用于打印 Python 版本

# 待检查的库列表 — List[Tuple[str, str]]，每项为 (import_name, display_name)
libs = [
    ("langchain",            "LangChain"),           # (str, str): 框架本体（v1.x）
    ("langchain_core",       "LangChain Core"),       # (str, str): 核心抽象层
    ("langchain_openai",     "LangChain OpenAI"),     # (str, str): OpenAI 集成
    ("langchain_community",  "LangChain Community"),  # (str, str): 社区集成
    ("langchain_chroma",     "LangChain Chroma"),     # (str, str): Chroma 向量库
    ("langchain_classic",    "LangChain Classic"),    # (str, str): 遗留 API 兼容包（AgentExecutor 等）
    ("dotenv",               "python-dotenv"),        # (str, str): 环境变量工具
]

print(f"Python 版本: {sys.version}")  # 打印 Python 解释器版本字符串
print("-" * 45)                        # 打印分隔线

for import_name, display_name in libs:  # import_name: str 模块名，display_name: str 展示名
    try:
        mod = importlib.import_module(import_name)           # module 对象：动态导入结果
        ver = getattr(mod, "__version__", "已安装")           # str: 版本号，缺少时返回"已安装"
        print(f"✓ {display_name:<25} {ver}")                 # 打印安装成功的库及版本
    except ImportError:
        print(f"✗ {display_name:<25} 未安装，请重新运行安装命令")  # 打印未安装提示
```

### 1.2 环境配置

```python
# ============================================================
# 加载 API Key — 推荐使用 .env 文件，避免明文写入代码
# ============================================================
import os                       # 操作系统接口，用于读取环境变量
from dotenv import load_dotenv  # load_dotenv(): 从 .env 文件加载键值对到 os.environ

# load_dotenv() : 读取当前目录下的 .env 文件，返回 bool（是否成功加载）
# .env 文件内容示例：
#   OPENAI_API_KEY=sk-xxxx
#   OPENAI_BASE_URL=https://api.openai.com/v1   # 可选，替换为国内中转地址
load_dotenv()  # bool: 成功返回 True，.env 不存在时返回 False

# os.environ.get(key, default) : 读取环境变量，不存在时返回 default 字符串
OPENAI_API_KEY  = os.environ.get("OPENAI_API_KEY",  "your-api-key-here")   # str: OpenAI API 密钥
OPENAI_BASE_URL = os.environ.get("OPENAI_BASE_URL", "https://api.openai.com/v1")  # str: API 根地址

print(f"API Key 前缀: {OPENAI_API_KEY[:8]}...")  # 仅打印前缀，避免泄露完整密钥
print(f"Base URL: {OPENAI_BASE_URL}")             # 打印 API 地址
```

---

## 二、LangChain 架构概览

LangChain 整体数据流（LCEL 管道，按源码实际执行路径）：

```
用户输入（str / dict / HumanMessage）
   ↓ ① ChatPromptTemplate.from_messages()   格式化为 List[BaseMessage]
   ↓ ② ChatOpenAI / init_chat_model()       调用 LLM，返回 AIMessage
   ↓ ③ StrOutputParser / PydanticParser     解析输出为 str / Pydantic 对象
输出（str / dict / Pydantic）

— RAG 扩展路径 —
文档 → TextSplitter → Embeddings → VectorStore → Retriever
   ↓ ④ retriever.invoke(query)              返回 List[Document]
   ↓ 注入 ② 的 context 字段                组合到 Prompt 中

— Agent 扩展路径 —
   ↓ ⑤ @tool 装饰器定义工具函数
   ↓ ⑥ create_agent()（v1.x 推荐）        LLM 自动决策调用哪个工具
   ↓    create_tool_calling_agent()         旧式写法，需 langchain-classic
   ↓ ⑦ agent.invoke({"messages": [...]})  循环执行工具调用，返回最终答案
```

### 2.1 组件一览（均经官方源码验证）


| 类别          | 官方模块路径                                 | 类/函数名                                            |
| ----------- | -------------------------------------- | ------------------------------------------------ |
| 模型统一初始化     | `langchain`                            | `init_chat_model()`                              |
| Chat Model  | `langchain_openai`                     | `ChatOpenAI`                                     |
| Prompt 模板   | `langchain_core.prompts`               | `ChatPromptTemplate` / `PromptTemplate`          |
| 输出解析器       | `langchain_core.output_parsers`        | `StrOutputParser` / `JsonOutputParser`           |
| 结构化输出       | `pydantic`                             | `BaseModel` + `model.with_structured_output()`   |
| 文档加载        | `langchain_community.document_loaders` | `TextLoader` / `WebBaseLoader` / `PyPDFLoader`   |
| 文本分块        | `langchain_text_splitters`             | `RecursiveCharacterTextSplitter`                 |
| 向量数据库       | `langchain_chroma`                     | `Chroma`                                         |
| 嵌入模型        | `langchain_openai`                     | `OpenAIEmbeddings`                               |
| 检索器         | `langchain_core.vectorstores`          | `VectorStore.as_retriever()`                     |
| LCEL 管道     | `langchain_core.runnables`             | `RunnablePassthrough` / `RunnableLambda`         |
| 对话记忆        | `langchain_core.chat_history`          | `InMemoryChatMessageHistory`                     |
| 工具定义        | `langchain_core.tools`                 | `@tool` 装饰器                                      |
| Agent 构建（新） | `langchain.agents`                     | `create_agent`（v1.x 推荐）                          |
| Agent 构建（旧） | `langchain_classic.agents`             | `create_tool_calling_agent`（需 langchain-classic） |
| Agent 执行（旧） | `langchain_classic.agents`             | `AgentExecutor`（需 langchain-classic）             |


### 2.2 包结构说明

```
langchain-core      ← 核心抽象（Runnable / BaseMessage / BasePromptTemplate）
      ↑
langchain           ← v1.x 精简包（create_agent / init_chat_model / @tool 等）
      ↑
langchain-openai    ← OpenAI 具体实现（ChatOpenAI / OpenAIEmbeddings）
langchain-community ← 社区集成（FAISS / BM25 / 文档加载器）
langchain-chroma    ← Chroma 向量库集成
langchain-classic   ← 遗留 API 兼容包（AgentExecutor / create_tool_calling_agent / LLMChain 等）
```

---

## 三、Chat Model（模型接口）

**输入** — `llm.invoke(input)` 的 `input` 参数支持以下三种类型：


| 类型                  | 示例                                                    | 说明                                                        |
| ------------------- | ----------------------------------------------------- | --------------------------------------------------------- |
| `str`               | `llm.invoke("你好")`                                    | 最简单用法，内部自动封装为 `HumanMessage`                              |
| `List[BaseMessage]` | `llm.invoke([SystemMessage(...), HumanMessage(...)])` | 消息列表，可包含 `SystemMessage` / `HumanMessage` / `AIMessage` 等 |
| `dict`              | `chain.invoke({"input": "..."})`                      | 以字典形式传入，常见于 chain / agent_executor 调用，键名由 prompt 模板的占位符决定 |


**返回值** — `llm.invoke()` 返回 `AIMessage` 对象，常用字段如下：


| 字段                    | 类型           | 说明                                                                                                                              |
| --------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| `.content`            | `str`        | 模型生成的文本内容，最常用字段                                                                                                                 |
| `.response_metadata`  | `dict`       | 元数据，含 `model_name`（模型名）、`finish_reason`（停止原因）、`token_usage`（Token 用量，含 `prompt_tokens` / `completion_tokens` / `total_tokens`）等 |
| `.response_metadata["logprobs"]` | `dict \| None` | 当初始化时设置 `logprobs=True` 才返回；每个输出 token 含 `token`（文本）、`logprob`（对数概率，越接近 0 表示越确定）、`bytes`（UTF-8 字节列表）字段；设置 `top_logprobs=N` 后还包含 `top_logprobs` 列表，展示该位置概率最高的前 N 个候选 token；未开启时为 `None` |
| `.id`                 | `str`        | 本次请求的唯一标识符                                                                                                                      |
| `.usage_metadata`     | `dict`       | 标准化的 Token 用量（`input_tokens` / `output_tokens` / `total_tokens`），与 `response_metadata` 中的用量等价，但字段名更统一                           |
| `.tool_calls`         | `List[dict]` | Agent 场景下模型决定调用工具时填充，普通对话为空列表                                                                                                   |
| `.invalid_tool_calls` | `List[dict]` | 模型尝试调用工具但格式解析失败时填充（如输出的 JSON 格式错误），普通对话为空列表，Agent 调试时可检查此字段定位问题                                                                 |
| `.type`               | `str`        | 固定值 `"ai"`，用于区分消息类型（`HumanMessage` 为 `"human"`，`SystemMessage` 为 `"system"`），通常只在消息序列化 / 反序列化时有用                                |
| `.additional_kwargs`  | `dict`       | Provider 返回的其他原始字段，通常无需直接访问                                                                                                     |


> **两种初始化方式的参数说明**
>
> 两种方式均依赖 1.2 节 `load_dotenv()` 预先将 `.env` 中的配置写入环境变量，无需在代码中明文传入密钥。**若显式传入参数，则会覆盖对应环境变量**（优先级：显式参数 > 环境变量）。
>
> | 参数 | 统一接口 `init_chat_model` | 直接实例化 `ChatOpenAI` |
> | --- | --- | --- |
> | `api_key` | 默认读 `OPENAI_API_KEY`；可通过 `api_key=` 显式传入覆盖 | 默认读 `OPENAI_API_KEY`；可通过 `api_key=` 显式传入覆盖 |
> | `base_url` ② | 各厂商实现类自动内置并读取各自的 URL 环境变量，切换 Provider 时底层 URL 随之自动切换，无需手动指定 ① | 默认读 `OPENAI_BASE_URL`；需中转站时可通过 `base_url=` 显式传入覆盖 |
>
> ① 各实现类只读取**自己对应**的环境变量（`ChatOpenAI` → `OPENAI_BASE_URL`，`ChatAnthropic` → `ANTHROPIC_BASE_URL`，互不干扰）。若需中转站等特殊地址，在 `.env` 中为各厂商分别配置对应的环境变量即可，代码层面无需改动；不推荐在代码中硬写 `base_url=`，否则会把代码绑死在单一厂商，失去一键切换的优势。
>
> ② 两种方式的 `base_url` 均填写**根地址**（例如 `https://api.openai.com/v1` 或中转站地址），无需手动拼接 `/chat/completions`；LangChain 内部会自动将请求发往 `{base_url}/chat/completions`。`ChatOpenAI` 仅支持实现了 `POST /v1/chat/completions` 端点（Chat Completions 格式）的服务，不兼容仅提供 `POST /v1/responses`（Responses 格式）的接口。

### 3.1 使用 init_chat_model 统一初始化

```python
# ============================================================
# init_chat_model() — 跨 Provider 统一模型初始化接口
# 格式："provider:model_name"，无需更改其余代码即可切换模型
# ============================================================
from langchain.chat_models import init_chat_model  # init_chat_model: 统一模型初始化函数

# init_chat_model(model, **kwargs) -> BaseChatModel
#   model      : str  — "provider:model_name" 格式字符串
#                       provider 可为 openai / anthropic / google_genai / ollama 等
#   temperature: float — 采样温度，0.0 输出最确定，1.0 最随机
#                        默认值由底层 provider 决定（OpenAI 默认 1.0，不指定时沿用 provider 默认值）
#   返回值      : BaseChatModel — 实现 Runnable 接口的模型对象
llm = init_chat_model(
    "openai:gpt-4o-mini",  # str: 模型标识，格式为 "provider:model_name"
    temperature=0.0,        # float: 采样温度，设为 0.0 保证输出稳定可复现
)

# model.invoke(input) -> AIMessage
#   input  : str / List[BaseMessage] / dict — 输入内容
#   返回值  : AIMessage — 包含 .content(str) 和 .response_metadata(dict) 字段
response = llm.invoke("你好，请用一句话介绍 LangChain 的核心价值。")  # AIMessage 对象
print(response.content)         # str: LLM 实际生成的文本内容
print(response.response_metadata)  # dict: Token 使用量、模型名称等元数据
```

### 3.2 直接使用 ChatOpenAI

```python
# ============================================================
# ChatOpenAI — OpenAI Chat Model 的直接封装类
# 支持 OpenAI API 及所有兼容 /v1/chat/completions 端点的服务
# ============================================================
from langchain_openai import ChatOpenAI  # ChatOpenAI: OpenAI 聊天模型封装类

# ChatOpenAI(**kwargs) -> ChatOpenAI
#   model        : str   — 模型名称（gpt-4o / gpt-4o-mini / gpt-3.5-turbo 等）
#   temperature  : float — 采样温度，默认值为 1.0（不显式设置时服务端使用该值）
#                          对于推理模型（o1 / o3 / gpt-5 等）不支持该参数，
#                          可传默认值 1，传其他值会报错，建议直接不设置
#   base_url     : str   — 自定义 API 端点（用于中转站/私有部署，可选）
#   api_key      : str   — 覆盖环境变量 OPENAI_API_KEY（不推荐明文写入代码）
#   max_tokens   : int   — 单次生成的最大 Token 数，None 表示不限制
#   logprobs     : bool  — 是否返回每个输出 token 的对数概率，默认 False；
#                          开启后可在 response.response_metadata["logprobs"] 中读取
#   top_logprobs : int   — 每个位置返回概率最高的前 N 个候选 token（0~20），
#                          需同时设置 logprobs=True 才生效
#   注意：streaming 参数已废弃；流式/非流式由调用方式决定（.invoke() vs .stream()）
chat_model = ChatOpenAI(
    model="gpt-4o-mini",      # str: 模型名称，gpt-4o-mini 性价比高
    temperature=0.7,           # float: 采样温度，适度随机
    base_url=OPENAI_BASE_URL,  # str: API 地址，支持中转站
    logprobs=True,             # bool: 开启后返回每个 token 的对数概率
    top_logprobs=5,            # int: 每个位置额外返回概率最高的前 5 个候选 token
    # 注意：不需要显式设置 streaming 参数。
    # 流式/非流式由调用方式决定：.invoke() 返回完整结果，.stream() 返回迭代器
)

# 直接传入 str，ChatOpenAI 内部自动封装为 HumanMessage
result = chat_model.invoke("请解释什么是 LCEL？")  # AIMessage 对象
print(result.content)  # str: 模型生成的回答文本

# ---- logprobs 读取示例 ----
# result.response_metadata["logprobs"] : dict
#   OpenAI 将 logprobs 拆为两个子键，以区分不同输出场景：
#     "content" : List[dict] | None — 正常回复时各 token 的概率列表（绝大多数情况使用此键）
#     "refusal" : List[dict] | None — 模型触发安全限制拒绝回复时的 token 概率列表，正常回复时为 None
#
# result.response_metadata["logprobs"]["content"] : List[dict]
#   每个元素对应一个输出 token，包含字段：
#     token        : str        — token 文本
#     logprob      : float      — 该 token 的对数概率（范围 (-∞, 0]，越接近 0 越确定）
#     bytes        : List[int]  — token 的 UTF-8 字节列表
#     top_logprobs : List[dict] — 该位置概率最高的前 N 个候选（设置 top_logprobs>0 时返回）
logprobs_list = result.response_metadata["logprobs"]["content"]  # List[dict]: 每个 token 的概率信息
for token_info in logprobs_list:                    # dict: 单个 token 的概率条目
    token = token_info["token"]                     # str: token 文本
    logprob = token_info["logprob"]                 # float: 对数概率
    top = token_info["top_logprobs"]                # List[dict]: 该位置前 N 个候选 token
    print(f"token={token!r:12s}  logprob={logprob:.4f}")  # 打印 token 及其对数概率
    for candidate in top:                           # dict: 单个候选 token 条目
        print(f"    候选 token={candidate['token']!r:12s}  logprob={candidate['logprob']:.4f}")
```

> `base_url` 与 `model` 为何同时存在？
>
> 两者职责完全不同，缺一不可：
>
>
> | 参数         | 作用层面               | 类比    |
> | ---------- | ------------------ | ----- |
> | `base_url` | 网络层：HTTP 请求发往哪台服务器 | 餐厅的地址 |
> | `model`    | 业务层：服务器用哪个模型处理请求   | 你点的菜名 |
>
>
> - `base_url`决定请求目的地，例如官方 `https://api.openai.com/v1`、中转站、本地 Ollama 或阿里百炼等，只负责**路由到服务器**。
> - `model`写入 HTTP 请求体的 JSON 字段（`{"model": "gpt-4o-mini", "messages": [...]}`），服务器收到后靠它**路由到具体模型**。
>
> 同一个 `base_url` 下往往同时挂载多个模型（如中转站可同时提供 `gpt-4o`、`gpt-4o-mini`、`claude-3-5-sonnet` 等），必须通过 `model` 参数区分——去了餐厅之后还是要点菜。

### 3.3 流式输出

```python
# ============================================================
# 流式输出 — stream() 方法逐 Token 实时打印，适合交互式场景
# ============================================================
from langchain_openai import ChatOpenAI  # ChatOpenAI: OpenAI 聊天模型封装类

stream_model = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI 实例

# model.stream(input) -> Iterator[AIMessageChunk]
#   input    : str / List[BaseMessage] — 输入内容
#   返回值    : Iterator[AIMessageChunk] — 逐步产生的文本片段迭代器
# 每个 chunk.content 是 str 类型的局部文本（一个或多个 Token）
for chunk in stream_model.stream("写一首关于 AI 的五言绝句"):  # AIMessageChunk 对象
    print(chunk.content, end="", flush=True)  # str: 实时打印每个 Token 片段，不换行
print()  # 流式结束后换行
```

### 3.4 批量调用

```python
# ============================================================
# 批量调用 — batch() 并行处理多个输入，比循环 invoke() 更高效
# ============================================================
from langchain_openai import ChatOpenAI  # ChatOpenAI: OpenAI 聊天模型封装类

batch_model = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI 实例

# model.batch(inputs) -> List[AIMessage]
#   inputs  : List[str | List[BaseMessage]] — 多个输入的列表
#   返回值   : List[AIMessage] — 与输入一一对应的响应列表
questions = [             # List[str]: 批量问题列表
    "Python 是什么语言？",
    "RAG 的全称是什么？",
    "向量数据库有什么用？",
]
responses = batch_model.batch(questions)  # List[AIMessage]: 批量响应结果
for q, r in zip(questions, responses):   # q: str 问题，r: AIMessage 响应
    print(f"Q: {q}\nA: {r.content}\n")   # 打印问答对
```

---

## 四、Prompt Template（提示词模板）

填充提示词模板有两种方式：


| 方法                           | 签名                                       | 返回值类型               | 说明                                                                      |
| ---------------------------- | ---------------------------------------- | ------------------- | ----------------------------------------------------------------------- |
| `.format_messages(**kwargs)` | `prompt.format_messages(key=value, ...)` | `List[BaseMessage]` | 以关键字参数传入变量，返回格式化后的消息列表，可直接传给 LLM                                        |
| `.invoke(input_dict)`        | `prompt.invoke({"key": value, ...})`     | `ChatPromptValue`   | 以字典传入变量，返回 `ChatPromptValue`，可调用 `.to_messages()` 转为消息列表，是 LCEL 链中的标准用法 |


### 4.1 ChatPromptTemplate

#### 4.1.1 元组写法

```python
# ============================================================
# ChatPromptTemplate — 多角色对话提示词模板
# 支持 system / human / ai / placeholder 四种消息角色
# ============================================================
from langchain_core.prompts import ChatPromptTemplate  # ChatPromptTemplate: 对话提示词模板类
from langchain_openai import ChatOpenAI                # ChatOpenAI: OpenAI 聊天模型类

# ChatPromptTemplate.from_messages(messages) -> ChatPromptTemplate
#   messages: List[Tuple[str, str]] — (角色, 模板字符串) 的列表
#             角色可选值：
#               "system"      — 系统级指令，设定 AI 的行为和身份
#               "human"       — 用户消息，支持 {变量名} 占位符
#               "ai"          — 预设的 AI 回复（Few-shot 示例）
#               "placeholder" — 动态插入完整消息列表（用于 Memory/Agent）
#   返回值  : ChatPromptTemplate — 可调用的模板对象
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位专业的 {domain} 领域专家，用简洁易懂的语言回答问题。"),
    # {domain} : str — 动态注入的领域名称
    ("human",  "{question}"),
    # {question}: str — 动态注入的用户问题
])

# prompt.format_messages(**kwargs) -> List[BaseMessage]
#   返回值: List[BaseMessage] — 格式化后的消息列表，可直接传给 LLM
formatted = prompt.format_messages(
    domain="人工智能",    # str: 填充 system 消息中的 {domain} 占位符
    question="什么是 Transformer 架构？"  # str: 填充 human 消息中的 {question} 占位符
)
for msg in formatted:              # msg: BaseMessage 对象（SystemMessage / HumanMessage）
    print(f"{msg.type}: {msg.content}")  # str: 消息角色类型 + 消息内容

# prompt.invoke(input_dict) -> ChatPromptValue
#   等价于 format_messages，返回 ChatPromptValue（可直接作为链的输入）
prompt_value = prompt.invoke({"domain": "数据科学", "question": "解释过拟合"})
print(prompt_value.to_messages())  # List[BaseMessage]: 消息列表
```

#### 4.1.2 消息类写法

```python
# ============================================================
# 消息类直接构建模板 — 使用 Message 对象替代 (角色, 内容) 元组
# ------------------------------------------------------------
# ChatPromptTemplate.from_messages() 同时接受两种写法：
#   ① 元组  : ("system", "...{var}...")        — 简洁，适合纯静态或单占位符
#   ② 消息类: SystemMessage(content="...{var}...") — 显式，可携带 additional_kwargs
#
# 消息类一览（均继承自 BaseMessage，来自 langchain_core.messages）：
#   SystemMessage       — 系统指令，对应 "system" 角色
#   HumanMessage        — 用户消息，对应 "human" 角色，支持 {变量} 占位符
#   AIMessage           — AI 回复，对应 "ai"  角色，常用于 Few-Shot 示例
#   ChatMessage         — 自定义角色消息，role 字段可任意指定，适合多角色场景
#   MessagesPlaceholder — 占位符，运行时展开为完整消息列表（Memory / Agent 专用）
# ============================================================
from langchain_core.messages import (
    SystemMessage,   # SystemMessage: 系统指令消息，对应 "system" 角色
    HumanMessage,    # HumanMessage : 用户输入消息，对应 "human" 角色
    AIMessage,       # AIMessage    : AI 回复消息，对应 "ai" 角色
    ChatMessage,     # ChatMessage  : 自定义角色消息，role: str 可自由指定角色名
)
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder   # 模板组件

# ① SystemMessage — 固定系统指令（content 中仍可使用 {变量} 占位符）
system_msg = SystemMessage(
    content="你是一位 {role} 助手，擅长用 {style} 风格回答问题。",
    # content: str — 系统指令文本，支持 {变量} 占位符
)

# ② HumanMessage — 用户消息模板（必须有 {变量} 供运行时填充）
human_msg = HumanMessage(
    content="{question}",  # content: str — 用户问题占位符，invoke 时由 question 键填充
)

# ③ AIMessage — 预设 AI 回复（常用于 Few-Shot：先给几条示例再提真实问题）
ai_example = AIMessage(
    content="好的，我将以 {style} 风格为您解答。",
    # content: str — AI 示例回复，支持占位符（Few-Shot 场景中通常写死，不留占位符）
)

# ④ ChatMessage — 自定义角色消息（role 不限于 system/human/ai，适合多角色对话场景）
custom_msg = ChatMessage(
    role="助教",                              # role: str — 自定义角色名，可为任意字符串
    content="请注意：{topic} 这道题有陷阱。",  # content: str — 消息正文，支持 {变量} 占位符
)

# ⑤ MessagesPlaceholder — 动态插入消息列表（History / Agent scratchpad）
history_placeholder = MessagesPlaceholder(
    variable_name="chat_history",  # str: invoke 时从同名键取 List[BaseMessage] 展开
    optional=True,                 # bool: True 时该键缺失也不报错（首轮无历史场景）
)

# 混合写法：消息类 + 元组，from_messages 均接受
mixed_prompt = ChatPromptTemplate.from_messages([
    system_msg,           # SystemMessage     : 系统指令（消息类写法）
    history_placeholder,  # MessagesPlaceholder: 动态历史（消息类写法）
    custom_msg,           # ChatMessage        : 自定义角色消息（消息类写法）
    human_msg,            # HumanMessage       : 用户消息（消息类写法）
])

# mixed_prompt.invoke(input_dict) -> ChatPromptValue
#   input_dict 键对应模板中的所有占位符变量名：
#     "role"         : str              — 填充 SystemMessage.content 中的 {role}
#     "style"        : str              — 填充 SystemMessage.content 中的 {style}
#     "chat_history" : List[BaseMessage]— 展开 MessagesPlaceholder（optional，可省略）
#     "topic"        : str              — 填充 ChatMessage.content 中的 {topic}
#     "question"     : str              — 填充 HumanMessage.content 中的 {question}
result_msgs = mixed_prompt.invoke({
    "role": "数学",                   # str: 填充系统角色描述
    "style": "简洁",                  # str: 填充回答风格
    "chat_history": [],               # List[BaseMessage]: 首轮传空列表
    "topic": "贝叶斯定理",             # str: 填充助教消息中的主题
    "question": "什么是贝叶斯定理？",  # str: 用户实际问题
})
for m in result_msgs.to_messages():  # List[BaseMessage]: 格式化后的完整消息列表
    print(f"[{m.type:10s}] {m.content}")  # str: 角色类型 + 消息内容
```

### 4.2 Few-Shot 提示词（少样本示例）

```python
# ============================================================
# FewShotChatMessagePromptTemplate — Few-Shot 提示词
# 通过提供示例，引导模型输出符合期望格式的回答
# ============================================================
from langchain_core.prompts import (
    ChatPromptTemplate,            # ChatPromptTemplate: 多角色对话模板
    FewShotChatMessagePromptTemplate,  # FewShotChatMessagePromptTemplate: Few-Shot 模板
)

# 示例数据 — List[Dict[str, str]]，每条包含 input 和 output 键
examples = [  # List[Dict]: 示例列表，每条为一个输入/输出对
    {"input": "苹果",  "output": "水果，红色，甜"},  # Dict: 单条 Few-Shot 示例
    {"input": "香蕉",  "output": "水果，黄色，软"},  # Dict: 单条 Few-Shot 示例
    {"input": "西红柿", "output": "蔬果，红色，酸"},  # Dict: 单条 Few-Shot 示例
]

# 定义单条示例的模板格式 — 输入为 human 角色，输出为 ai 角色
example_prompt = ChatPromptTemplate.from_messages([
    ("human", "{input}"),   # str: 示例中用户输入的模板
    ("ai",    "{output}"),  # str: 示例中 AI 输出的模板
])

# FewShotChatMessagePromptTemplate(examples, example_prompt) -> FewShotChatMessagePromptTemplate
#   examples      : List[Dict] — 示例数据列表
#   example_prompt: ChatPromptTemplate — 单条示例的格式模板
few_shot_prompt = FewShotChatMessagePromptTemplate(
    examples=examples,          # List[Dict]: Few-Shot 示例数据
    example_prompt=example_prompt,  # ChatPromptTemplate: 示例格式模板
)

# 拼接完整提示词
final_prompt = ChatPromptTemplate.from_messages([
    ("system", "请仿照示例格式，描述给定事物的属性（类别、颜色、特点）。"),
    few_shot_prompt,    # FewShotChatMessagePromptTemplate: 可直接传入，因为它继承自
                        # BaseMessagePromptTemplate，是合法的消息模板对象；
                        # ① 构建阶段（from_messages()）：元组按角色名转为对应的 MessagePromptTemplate，
                        #    few_shot_prompt 则原样保留为模板对象，变量尚未填入；
                        # ② 格式化阶段（invoke() / format_messages()）：两者均产出实际消息对象，
                        #    few_shot_prompt 展开为多条 HumanMessage + AIMessage（示例对），
                        #    元组模板格式化为对应的单条消息对象（如 HumanMessage / SystemMessage）
    ("human", "{input}"),  # str: 真实用户输入的占位符
])

# invoke 测试效果
print(final_prompt.format_messages(input="草莓"))  # List[BaseMessage]: 完整消息列表
```

---

## 五、Output Parser（输出解析器）

### 5.1 StrOutputParser — 提取纯文本

```python
# ============================================================
# StrOutputParser — 最常用的输出解析器
# 将 AIMessage 对象转换为纯字符串（提取 .content 字段）
# ============================================================
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串输出解析器
from langchain_core.prompts import ChatPromptTemplate      # ChatPromptTemplate: 对话模板
from langchain_openai import ChatOpenAI                    # ChatOpenAI: OpenAI 模型

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
prompt = ChatPromptTemplate.from_messages([                 # ChatPromptTemplate: 提示词模板
    ("human", "{input}")  # str: 用户输入占位符
])
parser = StrOutputParser()  # StrOutputParser: 字符串解析器实例，无需参数

# LCEL 管道：prompt → llm → parser
# `|` 运算符的本质：构建 RunnableSequence，而非立即执行
#   chain = prompt | llm | parser
#   等价于：RunnableSequence([prompt, llm, parser])
#   此行仅"组装"流水线，不触发任何调用
#
# 真正执行发生在 chain.invoke(input) 时，内部自动依次执行：
#   ① prompt.invoke({"input": ...})  → List[BaseMessage]（格式化后的消息列表）
#   ② llm.invoke(List[BaseMessage])  → AIMessage（模型返回的消息对象）
#   ③ parser.invoke(AIMessage)       → str（提取 AIMessage.content 字段）
#
# 每一步的返回值自动作为下一步的输入，无需手动传递
#
# StrOutputParser.invoke(message) -> str
#   输入  : AIMessage — LLM 输出的消息对象
#   返回值 : str — AIMessage.content 字段的字符串值
chain = prompt | llm | parser  # RunnableSequence: LCEL 管道对象，输入 dict，输出 str

result = chain.invoke({"input": "用一句话解释向量数据库"})  # str: 解析后的纯文本
print(result)        # str: 直接可用的文本，不含 AIMessage 包装
print(type(result))  # <class 'str'>
```

### 5.2 JsonOutputParser — 解析 JSON 输出

```python
# ============================================================
# JsonOutputParser — 解析模型输出的 JSON 字符串为 Python 字典
# 适用于需要结构化输出（如列表、键值对）的场景
# ============================================================
from langchain_core.output_parsers import JsonOutputParser  # JsonOutputParser: JSON 解析器
from langchain_core.prompts import ChatPromptTemplate       # ChatPromptTemplate: 对话模板
from langchain_openai import ChatOpenAI                     # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: temperature=0.0 完全确定性输出
# temperature : float — 控制模型输出的随机性（采样温度）
#               0.0        : 完全确定性，每次输出几乎相同，倾向于选最高概率的 token
#               0.1 ~ 0.3  : 低随机性，输出稳定、格式规范，适合结构化输出（JSON/XML）
#               0.7        : 中等随机性，回答更有创意，通用对话的常用默认值
#               1.0+       : 高随机性，输出更多样但容易格式错乱
# 解析 JSON 必须用低温的原因：
#   高温时模型可能随机在 JSON 前后附加 Markdown 代码块、多余说明文字、
#   或输出键名无引号 / 尾逗号等不合法格式，导致 JsonOutputParser 解析失败；
#   temperature=0.0 使模型严格按最高概率路径输出，格式稳定，解析成功率最高。
parser = JsonOutputParser()  # JsonOutputParser: JSON 输出解析器，无需参数

# 提示词中明确要求输出 JSON 格式
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个数据提取助手，请严以 JSON 格式输出，不要包含 Markdown 代码块。"),
    ("human",  '从以下文本提取信息：{text}\n输出格式：{{"姓名": string, "年龄": number, "职业": string}}'),
    # 注意：{{}} 是 Python f-string 中字面量花括号的转义，输出为 {}
])

chain = prompt | llm | parser  # Runnable: 管道，输入 dict，输出 dict

result = chain.invoke({"text": "张伟，28岁，软件工程师，在北京工作。"})
# result: Dict — 解析后的 Python 字典对象
print(result)          # {'姓名': '张伟', '年龄': 28, '职业': '软件工程师'}
print(type(result))    # <class 'dict'>
print(result["姓名"])  # str: 直接按键访问
```

### 5.3 CommaSeparatedListOutputParser — 解析逗号分隔列表

```python
# ============================================================
# CommaSeparatedListOutputParser — 将模型逗号分隔输出解析为 Python 列表
# 相比 JsonOutputParser 更轻量，适合枚举类列表场景
# 使用步骤：
#   ① 调用 get_format_instructions() 获取格式说明字符串
#   ② 将格式说明注入 Prompt，告知模型以逗号分隔格式输出
#   ③ 解析器自动将输出字符串按逗号切分为 List[str]
# ============================================================
from langchain_core.output_parsers import CommaSeparatedListOutputParser  # 列表输出解析器
from langchain_core.prompts import ChatPromptTemplate                       # 对话模板
from langchain_openai import ChatOpenAI                                     # OpenAI 模型

# CommaSeparatedListOutputParser() -> CommaSeparatedListOutputParser
#   无需参数；内部以英文逗号（可带空格）拆分输出字符串
#   parse(text) -> List[str]: 解析逗号分隔文本为字符串列表
parser = CommaSeparatedListOutputParser()  # CommaSeparatedListOutputParser: 列表解析器实例

# parser.get_format_instructions() -> str
#   返回格式说明字符串，注入 Prompt 后可显著提高模型格式遵从率
#   典型内容："Your response should be a list of comma separated values,
#              eg: `foo, bar, baz` or `foo,bar,baz`"
format_instructions = parser.get_format_instructions()  # str: 模型格式说明
print(format_instructions)  # str: 打印格式说明供调试查看

llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温保证格式稳定

# 将格式说明注入提示词——temperature=0.0 + 明确格式说明可大幅提高解析成功率
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位知识丰富的助手，请严格按格式要求输出，不要添加序号或任何额外说明。"),
     # {format_instructions}: str — 解析器生成的格式说明，告知模型以逗号分隔输出
    ("human",  "列出 5 种常见的 Python 数据结构。\n\n{format_instructions}"),
])

chain = prompt | llm | parser  # Runnable: 输入 dict，输出 List[str]

result = chain.invoke({"format_instructions": format_instructions})
# result: List[str] — 解析后的字符串列表（逗号自动作为分隔符移除）
print(result)          # ['列表', '元组', '字典', '集合', '字符串']（示例输出）
print(type(result))    # <class 'list'>: 确认为 Python 列表类型
print(len(result))     # int: 列表元素数量（通常与要求数量一致）

# 若需要整数/浮点数列表，在 chain 末尾追加 RunnableLambda 进行类型转换
# 例如：chain | RunnableLambda(lambda lst: [int(x) for x in lst])
```

---

## 六、LCEL 链式组合（`|` 管道）

LCEL（LangChain Expression Language）的核心是 **`Runnable` 接口**。LangChain 中所有组件（LLM、Prompt、Parser、Retriever 等）均实现该接口，因此可以用 `|` 管道运算符自由串联，每个组件的输出自动成为下一个组件的输入。

**六种调用方式：**


| 方法                | 说明              | 返回值                       | 适用场景                    |
| ----------------- | --------------- | ------------------------- | ----------------------- |
| `.invoke(input)`  | 同步调用            | 单个结果                      | 一次性调用、脚本、测试             |
| `.stream(input)`  | 同步流式，逐 chunk 返回 | `Iterator[chunk]`         | 终端/UI 实时打印，减少首字等待       |
| `.batch(inputs)`  | 批量并行调用          | `List[result]`            | 处理多条输入，比循环 invoke 更高效   |
| `.ainvoke(input)` | 异步同步调用          | 单个结果（awaitable）           | FastAPI / asyncio 等异步服务 |
| `.astream(input)` | 异步流式            | `AsyncIterator[chunk]`    | 异步服务中的实时流式输出            |
| `.abatch(inputs)` | 异步批量            | `List[result]`（awaitable） | 异步服务中的高并发批量处理           |


**常见注意事项：**

- `|` 两侧的对象必须都实现 `Runnable` 接口，普通函数需用 `RunnableLambda` 包裹后才能接入管道
- `.batch()` 内部并发执行，可通过 `max_concurrency` 参数限制最大并发数，避免触发 API 限流
- `.stream()` 返回的是迭代器，需用 `for chunk in chain.stream(...)` 逐块消费，不能直接打印
- 链定义（`chain = a | b | c`）本身不执行任何计算，只有调用 `.invoke()` 等方法时才真正运行
- 异步方法必须在 `async` 函数内用 `await` 调用，不能在同步上下文中直接使用
- 调用方式由链的入口统一决定并自动传播到链中所有组件；若某组件未原生实现对应方法（如 `.ainvoke()`），LangChain 会自动降级——在线程池中运行其同步版本（`.invoke()`）以兼容，不会报错；`.stream()` 同理，不支持流式的组件会缓冲输出后再向下传递

### 6.1 LCEL 核心概念

```python
# ============================================================
# LCEL（LangChain Expression Language）核心说明
# ------------------------------------------------------------
# 所有 LangChain 组件均实现 Runnable 接口，提供以下统一方法：
#   .invoke(input)          → 同步调用，返回单个结果
#   .stream(input)          → 流式调用，返回 Iterator[chunk]
#   .batch(inputs)          → 批量并行调用，返回 List[result]
#   .ainvoke(input)         → 异步调用（async/await）
#   .astream(input)         → 异步流式
#   .abatch(inputs)         → 异步批量
#
# | 运算符将两个 Runnable 串联：left | right
#   等价于：RunnableSequence([left, right])
#   left 的输出直接作为 right 的输入
# ============================================================
from langchain_core.runnables import RunnablePassthrough, RunnableLambda  # 辅助 Runnable 组件
from langchain_core.prompts import ChatPromptTemplate                       # 提示词模板
from langchain_core.output_parsers import StrOutputParser                   # 字符串解析器
from langchain_openai import ChatOpenAI                                     # OpenAI 模型

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 字符串解析器

# ---- 基础链：prompt | llm | parser ----
basic_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个翻译助手，将中文翻译成英文。"),
    ("human",  "{text}"),  # str: 待翻译文本的占位符
])
translate_chain = basic_prompt | llm | parser  # RunnableSequence: 翻译链

result = translate_chain.invoke({"text": "人工智能正在改变世界"})  # str: 翻译结果
print(f"翻译结果: {result}")  # str: 英文翻译
```

### 6.2 RunnablePassthrough / RunnableLambda / RunnableParallel — 透传、函数包装与并行执行


| 组件                    | 返回值              | 说明                                                                                                                                                                 |
| --------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `RunnablePassthrough` | 与输入类型相同          | 将输入原样传递给下游，不做任何变换，常用于在 RAG 链中同时保留原始问题（`{"context": retriever, "question": RunnablePassthrough()}`）                                                                 |
| `RunnableLambda`      | 由包装的函数决定         | 将普通函数或 lambda 包装为 Runnable，使其可接入管道运算符（`\|`）链中 |
| `RunnableParallel`    | `Dict[str, Any]` | 并行执行多个 Runnable，将同一输入同时传给多个分支，各分支结果以分支名为键合并为字典返回；`Any` 表示各分支自身的返回类型，不同分支可返回不同类型（如 `str`、`AIMessage`、`list` 等），无法用一个具体类型统一描述；常用于同时生成多路输出（如摘要 + 关键词）；支持显式和隐式两种写法，见下表 |


`RunnableParallel` 的两种写法：


| 写法  | 示例                                                                                                                                                    | 说明                                                                                                                                                                                                           |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 显式  | `RunnableParallel({"context": retriever, "question": RunnablePassthrough()})` 或 `RunnableParallel(context=retriever, question=RunnablePassthrough())` | 明确包裹为 `RunnableParallel`；**值的类型为 `RunnableLike`**，即 Runnable 对象或普通 callable（函数/lambda），`RunnableParallel` 内部通过 `coerce_to_runnable()` 将 callable 自动包裹成 `RunnableLambda`，因此两种类型均可直接传入；支持字典和关键字参数两种传参方式，两种写法等价 |
| 隐式  | `{"context": retriever, "question": RunnablePassthrough()}`                                                                                           | 在管道链中直接使用 dict，LangChain 自动将其识别为 `RunnableParallel`，无需显式包裹 |


```python
# ============================================================
# RunnablePassthrough — 将输入原样传递给下游，不做任何变换
# 常用于 RAG 等需要将原始 query 同时送入多个分支的场景
# ============================================================
from langchain_core.runnables import RunnablePassthrough  # RunnablePassthrough: 透传 Runnable
from langchain_core.prompts import ChatPromptTemplate     # ChatPromptTemplate: 提示词模板
from langchain_core.output_parsers import StrOutputParser # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                   # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 字符串解析器

# RunnablePassthrough 用于构造多分支输入 dict
# 场景：需要将原始 question 传给 Prompt 的同时，也把 context 注入进去
rag_prompt = ChatPromptTemplate.from_messages([
    ("system", "请根据以下上下文回答问题：\n\n{context}"),
    ("human",  "{question}"),  # str: 用户问题占位符
])

# 模拟检索函数 — 实际场景替换为向量检索
def fake_retriever(query: str) -> str:  # query: str 检索词，返回 str 相关文档片段
    """模拟检索器，返回与 query 相关的上下文文档（实际应替换为向量数据库检索）。"""
    return f"[模拟检索结果] 关于 '{query}' 的相关文档内容：LangChain 是一个 LLM 应用框架。"

# RunnableLambda — 将普通 Python 函数显式包装为 Runnable
# 背景：`|` 管道运算符要求两侧对象均实现 Runnable 接口（具备 .invoke()/.stream()/.batch() 等方法）
# 在 LangChain v1.x 中，RunnableParallel 的 dict 值和 `|` 管道的右侧均接受 "RunnableLike"：
#   即 Runnable 对象 或 普通 callable（内部通过 coerce_to_runnable() 自动包裹为 RunnableLambda）
# 因此，{"context": fake_retriever} 中的裸函数实际上也可以直接传入，无需手动包裹；
# 但显式使用 RunnableLambda(fn) 有以下好处：
#   ① 意图更清晰，代码可读性更高
#   ② 可为包装后的 Runnable 传入额外配置（如 name、afunc 等）
#   ③ 在老版本 LangChain（< 0.2）中，裸函数不支持自动包裹，需手动包裹
from langchain_core.runnables import RunnableLambda  # RunnableLambda: 函数 → Runnable 适配器

# 构建 RAG 式链：
# LCEL 中，管道首位的 dict 会被自动识别为 RunnableParallel（并行执行器）
# 它将同一份输入同时分发给 dict 中的每个值（每个值为 RunnableLike：
#   Runnable 对象或普通 callable，callable 会被自动包裹为 RunnableLambda），
# 并行执行后将所有分支的结果按 key 合并成一个新 dict，再整体传给下一个组件
#
# 本例执行流程（输入为 str，如 "LangChain 是什么？"）：
#   ┌─ context  分支：fake_retriever("LangChain 是什么？") → "[模拟检索结果]..."  (str)
#   └─ question 分支：原样透传                            → "LangChain 是什么？" (str)
#                ↓ 两路并行执行完毕后合并
#   {"context": "[模拟检索结果]...", "question": "LangChain 是什么？"}  (dict)
#                ↓ 传给 rag_prompt，填充 {context} 和 {question} 占位符
#
# 这是 RAG 的核心模式：同一份用户输入，同时走"检索"和"透传"两条路，
# 拼合后一起送给模型，使模型能基于检索到的知识回答问题
rag_chain = (
    {
        "context":  RunnableLambda(fake_retriever),  # Runnable: 检索分支，输入 str，输出检索文档 str
        "question": RunnablePassthrough(),            # Runnable: 透传分支，原样输出用户问题 str
    }                                                 # dict 整体被识别为 RunnableParallel，并行执行后合并为 dict
    | rag_prompt  # ChatPromptTemplate: 接收 dict，将 context/question 填入模板，返回 List[BaseMessage]
    | llm         # ChatOpenAI: 接收 List[BaseMessage]，调用模型，返回 AIMessage
    | parser      # StrOutputParser: 提取 AIMessage.content，返回 str
)

answer = rag_chain.invoke("LangChain 是什么？")  # str: 最终回答
print(answer)  # str: 基于检索结果生成的回答
```

### 6.3 串联多个管道（链）

```python
# ============================================================
# 多步顺序链 — 第一步输出作为第二步输入
# 适合需要「先做 A，再基于 A 的结果做 B」的工作流
# ============================================================
from langchain_core.prompts import ChatPromptTemplate     # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                   # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 解析器

# 第一步：根据主题生成文章大纲
outline_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位内容策划师，擅长为技术文章设计清晰的结构。"),
    ("human",  "请为主题「{topic}」生成一个包含 3 个要点的文章大纲。"),
    # {topic}: str — 文章主题
])
outline_chain = outline_prompt | llm | parser  # Runnable: 大纲生成链，输出 str

# 第二步：根据大纲扩写第一段
expand_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位技术写作专家，擅长把大纲扩写为流畅的段落。"),
    ("human",  "根据以下大纲，扩写第一个要点：\n\n{outline}"),
    # {outline}: str — 第一步生成的大纲文本
])
expand_chain = expand_prompt | llm | parser  # Runnable: 段落扩写链，输出 str

# 串联两步 — outline_chain 的 str 输出直接作为 expand_chain 的 {outline} 输入
# 注意：expand_chain 需要 dict 输入，故使用 lambda 包装
from langchain_core.runnables import RunnableLambda  # RunnableLambda: 函数 → Runnable

full_chain = (
    outline_chain                                           # Runnable: 第一步，输出 str
    | RunnableLambda(lambda outline: {"outline": outline})  # Runnable: 将 str 包装为 dict
    | expand_chain                                          # Runnable: 第二步，输入 dict，输出 str
)

result = full_chain.invoke({"topic": "LangChain LCEL 的设计哲学"})  # str: 最终扩写段落
print(result)  # str: 基于大纲扩写的文章第一段
```

---

## 七、对话记忆（Memory / History）

### 7.1 使用 InMemoryChatMessageHistory 管理对话历史

```python
# ============================================================
# InMemoryChatMessageHistory — 内存对话历史管理器（langchain_core 现代写法）
# 将每轮对话存储为 List[BaseMessage]，实现多轮上下文记忆
# 注：旧版 ChatMessageHistory 来自 langchain_community，功能相同但已不推荐；
#     现代写法统一使用 langchain_core.chat_history.InMemoryChatMessageHistory
# ============================================================
from langchain_core.chat_history import InMemoryChatMessageHistory          # 现代内存历史记录器（langchain_core）
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder  # 提示词组件
from langchain_core.output_parsers import StrOutputParser                   # 字符串解析器
from langchain_openai import ChatOpenAI                                     # OpenAI 模型

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 字符串解析器

# InMemoryChatMessageHistory() -> InMemoryChatMessageHistory
#   无参数，创建空的内存历史存储器
#   .add_user_message(content)    : 追加 HumanMessage，content: str
#   .add_ai_message(content)      : 追加 AIMessage，content: str
#   .messages                     : List[BaseMessage] — 所有历史消息
history = InMemoryChatMessageHistory()  # InMemoryChatMessageHistory: 对话历史容器，内部维护 List[BaseMessage]
# 其中 BaseMessage 的子类包括：
#   HumanMessage  — 用户发送的消息（add_user_message() 追加）
#   AIMessage     — 模型返回的消息（add_ai_message() 追加）
#   SystemMessage — 系统提示消息（一般不直接追加到 history）
# 通过 history.messages 可获取完整的 List[BaseMessage]，按时间顺序排列

# MessagesPlaceholder — 在 ChatPromptTemplate 中动态插入消息列表的占位符
# variable_name: str — 对应 invoke() 时传入的键名
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位友好的 AI 助手，能记住对话历史并基于上下文回答。"),
    MessagesPlaceholder(variable_name="history"),  # 动态插入历史消息列表
    ("human", "{question}"),  # str: 当前用户问题占位符
])

chain = prompt | llm | parser  # Runnable: 带记忆的对话链

def chat_with_memory(question: str) -> str:
    """
    带记忆的对话函数，自动维护历史消息。

    Args:
        question: str — 用户输入的问题

    Returns:
        str — AI 的回答文本
    """
    # chain.invoke(input_dict) -> str
    #   history  : List[BaseMessage] — 历史消息列表，传给 MessagesPlaceholder
    #   question : str — 当前用户问题
    answer = chain.invoke({
        "history":  history.messages,  # List[BaseMessage]: 当前所有历史消息
        "question": question,          # str: 用户当前问题
    })
    history.add_user_message(question)  # 将用户消息追加到历史，None 无返回值
    history.add_ai_message(answer)      # 将 AI 回答追加到历史，None 无返回值
    return answer  # str: AI 的回答文本

# 多轮对话测试
print(chat_with_memory("我叫小明，今年 23 岁，是一名 AI 工程师。"))  # str: 第一轮回答
print(chat_with_memory("你还记得我叫什么名字吗？"))                   # str: 应能正确回答"小明"
print(chat_with_memory("根据我的情况，推荐一个适合我的 LangChain 学习路径。"))  # str: 个性化建议
print(f"\n共 {len(history.messages)} 条历史消息")  # int: 当前历史消息总数
```

### 7.2 RunnableWithMessageHistory — 自动化历史管理（已废弃）

> **⚠️ 废弃说明**：`RunnableWithMessageHistory` 已于 `langchain_core` **1.3.3** 中标记为 deprecated，
> 并将在 **2.0.0** 中移除。官方建议新项目改用 **LangGraph 内置持久化（Checkpointer）**。
> 现有 v1.x 代码继续可用但会触发 `DeprecationWarning`，入门学习了解原理即可，
> 生产环境请参考 [LangGraph 记忆迁移指南](https://python.langchain.com/docs/versions/migrating_memory/)。

```python
# ============================================================
# RunnableWithMessageHistory — 自动注入/更新对话历史的 Runnable 包装器
# ⚠️ deprecated since langchain_core 1.3.3，将于 2.0.0 移除
# 通过 session_id 区分不同用户/会话，适合多用户场景（仍可用，但建议迁移至 LangGraph）
# ============================================================
from langchain_core.runnables.history import RunnableWithMessageHistory  # 历史自动注入包装器
from langchain_core.chat_history import InMemoryChatMessageHistory          # 现代内存历史记录器（langchain_core）
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder  # 提示词组件
from langchain_core.output_parsers import StrOutputParser                   # 字符串解析器
from langchain_openai import ChatOpenAI                                     # OpenAI 模型

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 字符串解析器

# 用字典模拟多用户会话存储 — Dict[str, InMemoryChatMessageHistory]
# 生产环境可替换为 Redis / 数据库等持久化方案
store: dict = {}  # Dict[str, InMemoryChatMessageHistory]: 会话 ID → 历史记录的映射

def get_session_history(session_id: str) -> InMemoryChatMessageHistory:
    """
    根据会话 ID 获取或创建对应的 InMemoryChatMessageHistory。

    Args:
        session_id: str — 用户/会话的唯一标识字符串

    Returns:
        InMemoryChatMessageHistory — 对应会话的历史记录对象（不存在时自动创建）
    """
    if session_id not in store:                              # 会话不存在时创建新的历史记录
        store[session_id] = InMemoryChatMessageHistory()    # InMemoryChatMessageHistory: 新建历史容器
    return store[session_id]  # InMemoryChatMessageHistory: 返回对应会话的历史记录

prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位专业的技术顾问。"),
    MessagesPlaceholder(variable_name="chat_history"),  # 注意：键名须与下方 history_messages_key 一致
    ("human", "{question}"),  # str: 用户当前问题占位符
])

base_chain = prompt | llm | parser  # Runnable: 基础对话链

# RunnableWithMessageHistory 包装器
#   runnable             : Runnable — 被包装的基础链
#   get_session_history  : Callable[[str], BaseChatMessageHistory] — 历史获取函数
#   input_messages_key   : str — invoke() 输入中用户消息字段的键名
#   history_messages_key : str — 注入历史时对应 MessagesPlaceholder 的变量名
chain_with_history = RunnableWithMessageHistory(
    base_chain,                              # Runnable: 被包装的基础对话链
    get_session_history,                     # Callable: 历史记录获取函数
    input_messages_key="question",           # str: 用户消息在 input dict 中的键名
    history_messages_key="chat_history",     # str: 与 MessagesPlaceholder 的 variable_name 一致
)

# invoke 时通过 config 传入 session_id
# config["configurable"]["session_id"]: str — 当前会话标识
r1 = chain_with_history.invoke(
    {"question": "我在学习 LangChain，有什么入门建议？"},
    config={"configurable": {"session_id": "user_001"}},  # dict: 配置，指定会话 ID
)
print(r1)  # str: AI 第一轮回答

r2 = chain_with_history.invoke(
    {"question": "能具体说说 LCEL 部分吗？"},
    config={"configurable": {"session_id": "user_001"}},  # str: 相同 session_id，延续上下文
)
print(r2)  # str: AI 基于上下文的第二轮回答（应能联系第一轮的 LangChain 建议）
```

### 7.3 会话管理 — 消息裁剪与窗口控制

> **为什么需要会话管理？**
>
> 多轮对话时历史消息会不断累积，直接将全量历史送入 LLM 会面临两个问题：
>
> 1. **Token 超限**：超过模型上下文窗口（如 gpt-4o-mini 的 128k）导致报错；
> 2. **成本激增**：每次请求携带大量冗余历史，Token 费用线性增长。
>
> LangChain 提供 `trim_messages` 工具对历史消息进行裁剪，保留最近的 N 条或 N 个 Token 以内的消息，同时支持保护 `SystemMessage`、保证消息对称性等策略。
>
>
> | 参数               | 类型                  | 说明                                                                     |
> | ---------------- | ------------------- | ---------------------------------------------------------------------- |
> | `messages`       | `List[BaseMessage]` | 待裁剪的消息列表                                                               |
> | `max_tokens`     | `int`               | 裁剪阈值上限；含义由 `token_counter` 决定：计数器返回 Token 数时表示最大 Token 数，返回消息条数时表示最大条数 |
> | `token_counter`  | `BaseLanguageModel  | Callable`                                                              |
> | `strategy`       | `"first"            | "last"`                                                                |
> | `allow_partial`  | `bool`              | 是否允许截断单条消息内容（默认 `False`，整条保留或整条丢弃）                                     |
> | `include_system` | `bool`              | `True` 时始终将 `SystemMessage` 保留在结果头部（默认 `False`）                        |
> | `start_on`       | `"human"            | "ai"                                                                   |
>

#### 7.3.1 基础用法 — 按 Token 数 / 消息条数裁剪

```python
# ============================================================
# trim_messages — 裁剪历史消息
# token_counter=llm  : 按 Token 数裁剪（精确，需调用模型计数接口）
# token_counter=len  : 按消息条数裁剪（轻量，无需 API 调用）
# ============================================================
from langchain_core.messages import (
    SystemMessage,   # 系统提示消息
    HumanMessage,    # 用户消息
    AIMessage,       # AI 回复消息
    trim_messages,   # 消息裁剪工具函数
)
from langchain_core.chat_history import InMemoryChatMessageHistory  # 内存对话历史容器
from langchain_openai import ChatOpenAI  # OpenAI 模型，用于 Token 计数

llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)  # ChatOpenAI: 模型实例，同时充当 Token 计数器

# InMemoryChatMessageHistory() -> InMemoryChatMessageHistory
#   内部维护 List[BaseMessage]，通过 add_user_message / add_ai_message 追加消息
history = InMemoryChatMessageHistory()  # InMemoryChatMessageHistory: 对话历史容器

# 模拟多轮对话，向历史中追加消息
history.add_messages([
    HumanMessage(content="Python 中的列表和元组有什么区别？"),  # 第 1 轮问
    AIMessage(content="列表（list）是可变序列，支持增删改；元组（tuple）是不可变序列，创建后不能修改。列表用方括号 []，元组用圆括号 ()。"),  # 第 1 轮答
    HumanMessage(content="什么时候应该用元组而不是列表？"),      # 第 2 轮问
    AIMessage(content="当数据不需要修改时优先用元组：1) 作为字典键；2) 函数返回多个值；3) 保护数据不被意外修改；4) 元组比列表占用内存略少、访问略快。"),  # 第 2 轮答
    HumanMessage(content="能给我一个具体的代码示例吗？"),        # 第 3 轮问
    AIMessage(content="当然！\npoint = (3, 5)  # 元组：不可变坐标\ncart = ['苹果', '香蕉']  # 列表：可动态增删"),  # 第 3 轮答
    HumanMessage(content="谢谢，再问一下生成器和迭代器的区别？"),  # 第 4 轮问
])

# history.messages : List[BaseMessage] — 完整历史消息列表（不含 SystemMessage）
print(f"原始消息数: {len(history.messages)}")  # int: 裁剪前消息总数

# ── 方式一：按 Token 数裁剪 ──────────────────────────────────
# trim_messages(messages, max_tokens, token_counter, strategy, include_system, start_on)
#   messages      : List[BaseMessage] — 传入 history.messages 获取完整历史
#   max_tokens    : int               — 最多保留 100 Token 以内的消息
#   token_counter : BaseLanguageModel — 传入模型实例，调用其 Token 计数接口（需网络/API）
#   strategy      : str               — "last" 保留最新消息（从尾部向头部选取）
#   include_system: bool              — True: 始终将 SystemMessage 保留在结果头部
#   start_on      : str               — 裁剪后第一条须是 HumanMessage（确保角色对称）
#   返回值         : List[BaseMessage] — 裁剪后的消息列表
trimmed_by_token = trim_messages(
    history.messages,       # List[BaseMessage]: 从历史容器中取出完整消息列表
    max_tokens=100,         # int: Token 上限
    token_counter=llm,      # BaseLanguageModel: 按 Token 计数（调用模型计数接口）
    strategy="last",        # str: 保留最新消息
    include_system=False,   # bool: history 中无 SystemMessage，设为 False
    start_on="human",       # str: 裁剪后以 HumanMessage 开头
)

# ── 方式二：按消息条数裁剪 ───────────────────────────────────
# token_counter 签名为 Callable[[List[BaseMessage]], int]，接收消息列表，返回整数
# 传入 len 时：len(messages) 返回列表长度（消息条数），max_tokens 即最多保留的消息条数
# 无需调用 API，适合对延迟敏感或不需要精确 Token 控制的场景
trimmed_by_count = trim_messages(
    history.messages,       # List[BaseMessage]: 从历史容器中取出完整消息列表
    max_tokens=4,           # int: 最多保留 4 条消息（max_tokens 语义变为"最大条数"）
    token_counter=len,      # Callable[[List[BaseMessage]], int]: 传入内置 len，按条数计
    strategy="last",        # str: 保留最新消息
    include_system=False,   # bool: history 中无 SystemMessage，设为 False
    start_on="human",       # str: 裁剪后以 HumanMessage 开头
)

# 对比输出两种裁剪结果
print(f"\n── 按 Token 数裁剪（max_tokens=100）──")
print(f"裁剪后消息数: {len(trimmed_by_token)}")    # int: 按 Token 裁剪后的消息总数
for msg in trimmed_by_token:                        # msg: BaseMessage
    role = msg.__class__.__name__                   # str: 消息类名
    print(f"  [{role}] {msg.content[:50]}...")      # str: 内容前 50 字预览

print(f"\n── 按消息条数裁剪（max_tokens=4，token_counter=len）──")
print(f"裁剪后消息数: {len(trimmed_by_count)}")    # int: 按条数裁剪后的消息总数
for msg in trimmed_by_count:                        # msg: BaseMessage
    role = msg.__class__.__name__                   # str: 消息类名
    print(f"  [{role}] {msg.content[:50]}...")      # str: 内容前 50 字预览
```

---

## 八、RAG（检索增强生成）

### 8.1 文档加载

> **常见文档加载器速查**
>
> LangChain 提供 50+ 种加载器，统一返回 `List[Document]`。每个 `Document` 包含两个字段：
>
> - `.page_content`：`str`，文档正文文本
> - `.metadata`：`dict`，来源路径、页码等元数据
>   - 本质是普通 Python `dict`，LangChain 不强制任何 schema，值须可序列化（`str` / `int` / `float` / `bool` / `list` / `dict`）
>   - 内置加载器会自动填充以下软约定键（非强制规范，仅为惯例）：
>
>     | 键名       | 填充的加载器          | 含义           |
>     | -------- | --------------- | ------------ |
>     | `source` | 几乎所有加载器         | 文档来源路径或 URL  |
>     | `page`   | `PyPDFLoader`   | 0-indexed 页码 |
>     | `title`  | `WebBaseLoader` | 网页标题         |
>     | `row`    | `CSVLoader`     | CSV 数据行号     |
>
>   - 手动创建 `Document` 时键名完全自定义，无任何限制
>
>
> | 加载器               | 适用格式               | 返回粒度                          | 额外依赖             | 说明                                                                |
> | ----------------- | ------------------ | ----------------------------- | ---------------- | ----------------------------------------------------------------- |
> | `TextLoader`      | 任意纯文本文件（`.txt` / `.md` / `.py` / `.html` / `.log` 等） | 整个文件 = 1 个 Document           | 无                | 最基础；按原始字节读取，不解析格式；`metadata` 含 `source`（文件路径）；中文须指定 `encoding="utf-8"` |
> | `WebBaseLoader`   | 网页 URL             | 每个 URL = 1 个 Document         | `beautifulsoup4` | 底层用 requests + BeautifulSoup 抓取；`metadata` 额外含 `title`（网页标题）；需网络  |
> | `PyPDFLoader`     | `.pdf`             | 每页 = 1 个 Document             | `pypdf`          | `metadata["page"]` 为 0-indexed 页码；可选 `extract_images=True` 启用 OCR |
> | `CSVLoader`       | `.csv`             | 每数据行 = 1 个 Document           | 无                | `page_content` 为 "列名: 值" 拼接；`source_column` 指定溯源列                 |
> | `JSONLoader`      | `.json` / `.jsonl` | 按 jq 路径提取，每条记录 = 1 个 Document | `jq`             | `jq_schema` 指定提取路径（如 `.[]`）；`json_lines=True` 逐行解析 `.jsonl`       |
> | `PythonLoader`    | `.py` Python 源码    | 整个文件 = 1 个 Document           | 无                | 读取 Python 源文件原始文本；常用于代码库问答场景                                      |
> | `Docx2txtLoader`  | `.docx` Word       | 整个文件 = 1 个 Document           | `docx2txt`       | 仅提取正文文本，不支持表格/图片/页眉页脚                                             |
> | `DirectoryLoader` | 整个文件夹              | 视子加载器而定                       | 视子加载器而定          | 组合其他加载器批量加载；`glob` 参数过滤文件类型                                       |
> | `ArxivLoader`     | ArXiv 论文 ID        | 每篇论文 = 1 个 Document           | `arxiv`          | 按 ID 直接拉取摘要+正文，学术场景常用                                             |
> | `WikipediaLoader` | 词条关键词              | 每个词条 = 1 个 Document           | `wikipedia`      | 按关键词检索词条；`load_max_docs` 控制数量                                     |
>
>
> **说明：**
>
> - **额外依赖**：填"无"表示仅需 `langchain-community`，其余均需单独 `pip install <包名>`。
> - **jq**：`JSONLoader` 的路径查询依赖。`jq` 是一套轻量 JSON 路径语言（如 `.messages[].content` 表示提取 `messages` 数组每个元素的 `content` 字段），Python 侧通过同名 pip 包 `jq` 提供绑定；`jq_schema` 参数即接受该语法的路径字符串。
> - **返回粒度**：决定向量检索时的最小可引用单元——粒度越细，召回越精准，但 `Document` 数量也越多。
> - **DirectoryLoader** 本身不直接解析文件，需通过 `loader_cls` 参数指定子加载器（如 `loader_cls=TextLoader`），`glob` 参数（默认 `"**/*"`）用于过滤文件类型。

#### 8.1.1 TextLoader — 纯文本文件（任意扩展名）

```python
# ============================================================
# TextLoader — 加载本地纯文本文件（任意扩展名，如 .txt / .md / .py / .log 等）
# LangChain 提供 50+ 种文档加载器，统一返回 List[Document]
# Document 对象包含两个字段：
#   .page_content : str  — 文档文本内容
#   .metadata     : dict — 来源、页码等元数据
# ============================================================
from langchain_community.document_loaders import TextLoader  # TextLoader: 纯文本文件加载器
from langchain_core.documents import Document                # Document: 文档数据类
import os  # 操作系统接口，用于文件路径操作

# 创建示例文本文件（实际场景替换为真实文档路径）
sample_text = """
LangChain 是什么？
LangChain 是一个开源的 LLM 应用开发框架，由 Harrison Chase 于 2022 年创建。
它提供了一套标准化的组件接口，帮助开发者快速构建基于大语言模型的应用。

LCEL 是什么？
LCEL（LangChain Expression Language）是 LangChain 的链式组合语言。
它使用 Python 的 | 运算符将组件连接成管道，支持流式输出、异步执行和批量处理。

LangChain 的主要用途有哪些？
1. RAG（检索增强生成）应用：将外部知识库与 LLM 结合
2. 多步 Agent：让 LLM 自动决策并调用工具完成复杂任务
3. 对话机器人：具有记忆功能的多轮对话应用
4. 结构化数据提取：从非结构化文本中提取结构化信息
""".strip()  # str: 去除首尾空白的示例文本内容

# 将文本写入临时文件，用于演示 TextLoader
sample_file = "langchain_intro.txt"  # str: 临时文件名
with open(sample_file, "w", encoding="utf-8") as f:  # 以 UTF-8 编码写入文件
    f.write(sample_text)  # 写入示例文本，None 无返回值

# TextLoader(file_path, encoding) -> TextLoader
#   file_path : str — 文本文件路径
#   encoding  : str — 文件编码，中文文件须指定 "utf-8"
loader = TextLoader(sample_file, encoding="utf-8")  # TextLoader: 文本加载器实例

# loader.load() -> List[Document]
#   返回值: List[Document] — 每个 Document 包含整个文件的文本内容
raw_docs = loader.load()  # List[Document]: 加载的原始文档列表
print(f"加载文档数量: {len(raw_docs)}")              # int: 文档总数（TextLoader 通常为 1）
print(f"文档内容前 100 字: {raw_docs[0].page_content[:100]}")  # str: 前 100 字预览
print(f"文档元数据: {raw_docs[0].metadata}")          # dict: 来源文件路径等信息
```

#### 8.1.2 WebBaseLoader — 网页 URL 加载

```python
# ============================================================
# WebBaseLoader — 从网页 URL 抓取正文并封装为 Document
# 安装依赖：pip install langchain-community beautifulsoup4
# 底层原理：使用 requests 发起 HTTP 请求，
#           再用 BeautifulSoup 解析 HTML，提取可见文本
# 适合：从公开网页（文档站、博客、新闻）抓取内容构建 RAG 知识库
# ============================================================
from langchain_community.document_loaders import WebBaseLoader  # WebBaseLoader: 网页加载器
import bs4  # BeautifulSoup4: HTML 解析库（WebBaseLoader 底层依赖）

# ── 用法一：加载单个 URL ──────────────────────────────────────
# WebBaseLoader(web_path) -> WebBaseLoader
#   web_path: str | List[str] — 单个 URL 或 URL 列表
#   返回值   : WebBaseLoader  — 网页加载器实例
single_loader = WebBaseLoader(
    web_path="https://python.langchain.com/docs/introduction/"
    # str: LangChain 官方文档首页（公开可访问的演示用 URL）
)  # WebBaseLoader: 单 URL 加载器实例

# single_loader.load() -> List[Document]
#   发起 HTTP GET 请求，抓取网页 HTML，用 BeautifulSoup 提取可见文本
#   返回值: List[Document] — 每个 URL 对应 1 个 Document，包含：
#     .page_content: str  — 网页正文文本（去除 HTML 标签后的纯文本）
#     .metadata    : dict — {"source": url, "title": 网页标题, "description": meta 描述, "language": 语言代码}
single_docs = single_loader.load()  # List[Document]: 加载结果，长度 = URL 数量
print(f"加载文档数: {len(single_docs)}")                          # int: 1（单 URL）
print(f"文档来源 : {single_docs[0].metadata['source']}")         # str: 原始 URL
print(f"网页标题 : {single_docs[0].metadata.get('title', '')}")  # str: <title> 标签内容
print(f"内容前 200 字:\n{single_docs[0].page_content[:200]}")    # str: 正文文本预览

# ── 用法二：批量加载多个 URL ─────────────────────────────────
# 传入 URL 列表，每个 URL 对应 1 个 Document
multi_loader = WebBaseLoader(
    web_path=[
        "https://python.langchain.com/docs/introduction/",
        "https://python.langchain.com/docs/concepts/",
    ]  # List[str]: 要抓取的 URL 列表
)  # WebBaseLoader: 多 URL 加载器实例

multi_docs = multi_loader.load()  # List[Document]: 长度 = URL 数量
print(f"\n批量加载文档数: {len(multi_docs)}")  # int: 2（与 URL 数量相同）
for doc in multi_docs:
    print(f"  • {doc.metadata['source'][:60]}  内容长度: {len(doc.page_content)}")
    # str: URL 前 60 字符；int: 正文字符数

# ── 用法三：用 bs4_strainer 精准提取指定 HTML 标签 ────────────
# 默认情况下 WebBaseLoader 提取所有可见文本（含导航栏、页脚等噪音）
# 通过 bs4_strainer 只保留目标 HTML 标签的内容，提升文本质量
#
# bs4.SoupStrainer(name, attrs) -> SoupStrainer
#   name : str | List[str] | None — 要保留的 HTML 标签名
#   attrs: dict                   — 标签属性过滤条件（如 class_、id）
#   返回值: SoupStrainer — BeautifulSoup 的解析过滤器
strainer = bs4.SoupStrainer(
    name=["article", "main", "div"],  # List[str]: 只保留这些标签的内容
    attrs={"class": ["content", "main-content", "post-body"]},
    # dict: 进一步过滤，只保留 class 含以上值的标签（多值取并集）
)  # SoupStrainer: HTML 解析过滤器

filtered_loader = WebBaseLoader(
    web_path="https://python.langchain.com/docs/introduction/",
    bs_kwargs={"parse_only": strainer},  # dict: 传给 BeautifulSoup 的额外参数
    # parse_only 指定解析范围，只处理 strainer 匹配的标签，减少噪音文本
)  # WebBaseLoader: 带过滤器的加载器实例

filtered_docs = filtered_loader.load()  # List[Document]: 仅含目标标签文本的文档
print(f"\n过滤后内容长度: {len(filtered_docs[0].page_content)}")  # int: 通常比未过滤小
print(f"过滤后内容预览:\n{filtered_docs[0].page_content[:300]}")  # str: 精简后的正文
```

#### 8.1.3 PyPDFLoader — PDF 文件加载

```python
# ============================================================
# PyPDFLoader — 加载 PDF 文件，每页独立为一个 Document
# 安装依赖：pip install langchain-community pypdf
# 底层原理：使用 pypdf 库逐页提取文本内容
# 适合：论文、报告、合同、书籍等 PDF 格式知识库构建
# ============================================================
from langchain_community.document_loaders import PyPDFLoader  # PyPDFLoader: PDF 加载器
import urllib.request  # urllib: 标准库，用于下载演示用 PDF

# ── 准备演示 PDF（下载公开样例文件）──────────────────────────
demo_pdf_url = "https://arxiv.org/pdf/1706.03762"  # str: Attention Is All You Need 论文
demo_pdf_path = "demo_attention.pdf"               # str: 本地保存路径

# urllib.request.urlretrieve(url, filename) — 下载文件到本地
# 返回值: (filename, headers) — 本地路径和响应头（通常只用文件名）
urllib.request.urlretrieve(demo_pdf_url, demo_pdf_path)  # 下载 PDF（约 2MB）
print(f"PDF 已下载至: {demo_pdf_path}")  # str: 本地文件路径

# ── PyPDFLoader 基本用法 ──────────────────────────────────────
# PyPDFLoader(file_path) -> PyPDFLoader
#   file_path: str — PDF 文件路径（本地绝对或相对路径）
#   返回值   : PyPDFLoader — PDF 加载器实例
pdf_loader = PyPDFLoader(demo_pdf_path)  # PyPDFLoader: PDF 加载器实例

# pdf_loader.load() -> List[Document]
#   逐页提取文本，每页生成一个 Document
#   返回值: List[Document] — 长度 = PDF 总页数，每个 Document：
#     .page_content: str  — 该页的纯文本内容（pypdf 自动去除 PDF 格式标记）
#     .metadata    : dict — {"source": 文件路径, "page": 0-indexed 页码}
#   注意：扫描版 PDF（图片 PDF）无法提取文本，需配合 OCR 工具（如 pytesseract）
pdf_docs = pdf_loader.load()  # List[Document]: 每页一个 Document
print(f"PDF 总页数 : {len(pdf_docs)}")                            # int: PDF 页数
print(f"第 1 页内容:\n{pdf_docs[0].page_content[:300]}")          # str: 第 1 页前 300 字
print(f"第 1 页元数据: {pdf_docs[0].metadata}")                   # dict: source + page

# ── 按页范围加载（大文件节省内存）────────────────────────────
# PyPDFLoader 支持惰性加载，逐页处理避免一次性加载全部内容
# pdf_loader.lazy_load() -> Iterator[Document]
#   返回值: Iterator[Document] — 惰性迭代器，按需逐页生成 Document
for i, page_doc in enumerate(pdf_loader.lazy_load()):
    if i >= 3:  # int: 只处理前 3 页（演示用）
        break
    print(f"  第 {page_doc.metadata['page'] + 1} 页 | 字符数: {len(page_doc.page_content)}")
    # int: metadata["page"] 为 0-indexed，+1 转为 1-indexed 页码
    # int: 该页正文字符数

# ── 提取图片中的文字（启用 OCR，需额外依赖）─────────────────
# PyPDFLoader(file_path, extract_images=True) — 针对图片型 PDF 启用 OCR 提取
# 安装依赖：pip install rapidocr-onnxruntime
# ocr_loader = PyPDFLoader(demo_pdf_path, extract_images=True)
# ocr_docs = ocr_loader.load()  # 含 OCR 识别文字的 Document 列表
# （此处注释掉避免未安装 rapidocr 时报错）
```

### 8.2 文本分块

> **常见分块器速查**
>
>
> | 分块器                              | 分割依据                                              | 适用场景                     | 额外依赖           |
> | -------------------------------- | ------------------------------------------------- | ------------------------ | -------------- |
> | `RecursiveCharacterTextSplitter` | 按字符数递归分割，依次尝试 `\n\n` → `\n` → `。` → 字符级，优先在语义边界切断 | **通用首选**，适合大多数纯文本        | 无              |
> | `CharacterTextSplitter`          | 按**单一**分隔符切割，不递归；超长块强制按字符数截断                      | 格式规整、分隔符固定的文本（如日志、CSV 行） | 无              |
> | `TokenTextSplitter`              | 按 **Token 数**分割（tiktoken BPE 编码）；字符数 ≠ Token 数    | 需要精确控制送入 LLM 的 Token 预算时 | `tiktoken`     |
> | `MarkdownHeaderTextSplitter`     | 按 `#` / `##` / `###` 标题边界切割；标题层级自动写入 `metadata`   | Markdown 文档、Wiki、技术文档    | 无              |
> | `HTMLHeaderTextSplitter`         | 按 HTML `<h1>`~`<h6>` 标签切割；标签内容写入 `metadata`             | 网页、HTML 格式报告             | 无              |
> | `SpacyTextSplitter`              | 按 spacy 分词器识别的句子边界切割；块大小仍以字符数衡量                   | 需要语言学级别句子边界的场景           | `spacy` + 语言模型 |
> | `SemanticChunker`                | 用 Embedding 计算相邻句子的语义相似度，在**语义断点**处切割；无固定字符数限制   | 长篇非结构化文档、需要最高语义连贯性的场景    | `langchain-experimental` + Embedding 模型 |
>
>
> **选型建议：** 绝大多数 RAG 场景直接用 `RecursiveCharacterTextSplitter`；若输入是 Markdown/HTML 结构化文档，优先用对应的 Header 分块器保留标题 metadata；需要 Token 预算控制时，**中文文本禁止直接用 `TokenTextSplitter`**（BPE 编码可能在多字节字符 Token 边界处截断，导致乱码），应改用 `RecursiveCharacterTextSplitter.from_tiktoken_encoder()`；追求最高语义连贯性时，用 `SemanticChunker`，但每个句子都需调用一次 Embedding，速度最慢、成本最高。

> **文本分块器的三种常用接口**
>
> | 方法 | 输入类型 | 输出类型 | 说明 |
> |------|----------|----------|------|
> | `split_text(text)` | `str` | `List[str]` | 最底层接口，单段纯文本进、纯文本块出，无 metadata |
> | `create_documents(texts, metadatas=...)` | `List[str]` | `List[Document]` | 在 `split_text` 基础上封装为 `Document`，可手动传入 metadata |
> | `split_documents(docs)` | `List[Document]` | `List[Document]` | 最常用，内部调用 `create_documents`，metadata 自动从原始 `Document` 继承 |
>
> 调用链：`split_documents` → `create_documents` → `split_text`（`split_text` 是核心，其余两个均为其封装）。

#### 8.2.1 RecursiveCharacterTextSplitter — 递归字符分块

```python
# ============================================================
# 文本分块 — RecursiveCharacterTextSplitter
# 将长文档递归切割为小块，保证每块不超过 chunk_size 个字符
# 同时保留 chunk_overlap 字符的重叠，避免语义断裂
# ============================================================
from langchain_text_splitters import RecursiveCharacterTextSplitter  # 递归字符分块器
from langchain_core.documents import Document                          # Document: 文档数据类

# RecursiveCharacterTextSplitter(chunk_size, chunk_overlap, separators) -> TextSplitter
#   chunk_size    : int       — 每个块的最大字符数，建议 200~1000
#   chunk_overlap : int       — 相邻块的重叠字符数，建议 chunk_size 的 10%~20%
#   separators    : List[str] — 分割优先级列表，从左到右依次尝试
#                              先尝试按段落("\n\n")→句子("\n")→句子内(".","。")→字符级("")分割
#   length_function: Callable — 计算文本长度的函数，默认 len（字符数），可替换为 token 计数
splitter = RecursiveCharacterTextSplitter(
    chunk_size=200,          # int: 每块最多 200 个字符（中文约 100 个汉字）
    chunk_overlap=30,        # int: 相邻块重叠 30 个字符，减少语义截断
    separators=["\n\n", "\n", "。", "，", " ", ""],  # List[str]: 分割符优先级
)

# splitter.split_documents(documents) -> List[Document]
#   documents: List[Document] — 原始文档列表（由 Loader 返回，含 metadata）
#   返回值   : List[Document] — 分块后的文档列表，每块自动继承原始 metadata（source/page 等）
#
# 【与 create_documents 的区别】
#   split_documents(docs)          — 输入 List[Document]，metadata 自动继承，推荐在已有 Loader 结果时使用
#   create_documents(texts)        — 输入 List[str]（裸字符串），metadata 全部为空 {}
#   create_documents(texts, metadatas=...) — 手动传入 metadata，等价于 split_documents，但需自行拆装：
#     texts     = [doc.page_content for doc in raw_docs]   # List[str]: 提取纯文本
#     metadatas = [doc.metadata     for doc in raw_docs]   # List[dict]: 提取 metadata
#     chunks    = splitter.create_documents(texts, metadatas=metadatas)
chunks = splitter.split_documents(raw_docs)  # List[Document]: 分块后的文档列表，metadata 自动继承
print(f"分块数量: {len(chunks)}")             # int: 总分块数
for i, chunk in enumerate(chunks):            # i: int 索引，chunk: Document 文档块
    print(f"\n--- 块 {i+1} ({len(chunk.page_content)} 字符) ---")
    print(chunk.page_content)  # str: 每个块的文本内容
```

#### 8.2.2 from_tiktoken_encoder — Token 数精确控制

```python
# ============================================================
# from_tiktoken_encoder — 以 Token 数为单位的分块（中文安全版）
# 直接使用 TokenTextSplitter 在中文场景可能在多字节字符的 Token
# 边界处截断，导致乱码；from_tiktoken_encoder 内部以 Token 数
# 计量 chunk_size，但最终仍在字符边界处切割，避免乱码问题
# ============================================================
from langchain_text_splitters import RecursiveCharacterTextSplitter  # 递归字符分块器

# RecursiveCharacterTextSplitter.from_tiktoken_encoder(
#     encoding_name, model_name, chunk_size, chunk_overlap, separators
# ) -> RecursiveCharacterTextSplitter
#   encoding_name : str  — tiktoken 编码名称，与 model_name 二选一
#                          常用值："cl100k_base"（GPT-4/3.5）、"o200k_base"（GPT-4o）
#   model_name    : str  — 直接指定模型名，tiktoken 自动推断编码；与 encoding_name 二选一
#                          如 "gpt-4o"、"gpt-3.5-turbo"
#   chunk_size    : int  — 每块最大 Token 数（非字符数）；GPT-4o 上下文 128k，建议 256~512
#   chunk_overlap : int  — 相邻块重叠 Token 数，建议 chunk_size 的 10%~20%
#   separators    : List[str] — 分割符优先级，同 RecursiveCharacterTextSplitter
splitter = RecursiveCharacterTextSplitter.from_tiktoken_encoder(
    encoding_name="cl100k_base",                              # str: GPT-4 / GPT-3.5 通用编码
    chunk_size=300,                                           # int: 每块最多 300 个 Token
    chunk_overlap=30,                                         # int: 相邻块重叠 30 个 Token
    separators=["\n\n", "\n", "。", "，", " ", ""],           # List[str]: 中文友好分割符
)

# split_documents 用法与普通 RecursiveCharacterTextSplitter 完全一致
# 返回值: List[Document]，每块保留原始 metadata，实际切割点在字符边界而非 Token 边界
chunks = splitter.split_documents(raw_docs)  # List[Document]: 按 Token 数控制的分块列表
print(f"分块数量: {len(chunks)}")             # int: 总分块数

# 【from_tiktoken_encoder vs TokenTextSplitter】
#   from_tiktoken_encoder  — 以 Token 数计量大小，但在字符边界切割，中文安全，推荐
#   TokenTextSplitter      — 直接在 Token 边界切割，中文多字节字符可能被截断导致乱码，不推荐用于中文
```

#### 8.2.3 SemanticChunker — 语义相似度分块

```python
# ============================================================
# SemanticChunker — 基于 Embedding 语义相似度的智能分块
# 原理：先将文本按句子切开，再用 Embedding 模型计算相邻句子的
#       余弦相似度，在相似度骤降（语义断点）处切割成块
# 优点：块内语义高度连贯，不会在话题中间截断
# 缺点：每个句子都要调用一次 Embedding，速度慢、成本高；
#       块大小不固定，无法精确控制 Token 预算
# 来源：langchain_experimental（非核心包，需单独安装）
# ============================================================
from langchain_experimental.text_splitter import SemanticChunker  # 语义分块器
from langchain_openai import OpenAIEmbeddings                      # Embedding 模型

# SemanticChunker(embeddings, breakpoint_threshold_type, breakpoint_threshold_amount)
#   embeddings                   : Embeddings  — 任意 LangChain Embedding 模型实例
#   breakpoint_threshold_type    : str         — 断点判定策略，控制"语义距离"的度量方式
#       "percentile"   （默认）  — 先统计文档内所有相邻句对的语义距离，排序后取第 X% 位的值作为切割阈值；
#                                  例如 amount=95：100 个句对中距离排第 95 大的那个值就是阈值，
#                                  某对句子的距离超过该值（即差异比 95% 的句对都大），说明话题发生明显转换，在此处切割成新块
#       "standard_deviation"     — 均值 + X 倍标准差作为阈值
#       "interquartile"          — 四分位距（IQR）方法
#       "gradient"               — 对距离序列求梯度，在梯度突变处切割
#   breakpoint_threshold_amount  : float       — 配合 threshold_type 的阈值参数
#       percentile 模式下默认 95（取第 95 百分位）
#       直觉理解：假设文档有 9 对相邻句子，距离排序后取第 95% 位的值作为阈值
#         amount=95 → 阈值 ≈ 第 8/9 大的距离，只有差异极大的句对才被切割 → 块少块大
#         amount=50 → 阈值 ≈ 中位数，轻微话题变化也会被切割               → 块多块小
embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",  # str: 嵌入模型，维度 1536
    base_url=OPENAI_BASE_URL,        # str: API 端点
)
splitter = SemanticChunker(
    embeddings=embeddings,                        # Embeddings: 用于计算句子语义向量
    breakpoint_threshold_type="percentile",       # str: 断点策略，percentile 最常用
    breakpoint_threshold_amount=95,               # float: 取第 95 百分位为断点阈值
)

# split_documents 用法与其他分块器一致，返回 List[Document]
# 注意：每个句子均需调用一次 Embedding API，文档越长 API 调用次数越多
chunks = splitter.split_documents(raw_docs)  # List[Document]: 按语义断点切割的分块列表
print(f"分块数量: {len(chunks)}")             # int: 总分块数（块大小不固定）
for i, chunk in enumerate(chunks):
    print(f"\n--- 块 {i+1} ({len(chunk.page_content)} 字符) ---")
    print(chunk.page_content[:100])           # str: 每块前 100 字符预览
```

### 8.3 构建向量存储与检索

#### 8.3.1 OpenAIEmbeddings + Chroma 向量存储

```python
# ============================================================
# 向量化 + 存储 — OpenAIEmbeddings + Chroma
# 将文本块转换为高维向量，存入 Chroma 向量数据库，支持语义检索
# ============================================================
from langchain_openai import OpenAIEmbeddings    # OpenAIEmbeddings: OpenAI 嵌入模型封装
from langchain_chroma import Chroma              # Chroma: Chroma 向量数据库封装

# OpenAIEmbeddings(model) -> OpenAIEmbeddings
#   model   : str — 嵌入模型名称
#             text-embedding-3-small : 维度 1536，性价比高，推荐
#             text-embedding-3-large : 维度 3072，精度更高，成本更高
#             text-embedding-ada-002 : 旧版，已不推荐使用
embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",  # str: 嵌入模型，1536 维
    base_url=OPENAI_BASE_URL,        # str: API 端点，支持中转站
)

# Chroma.from_documents(documents, embedding, collection_name, collection_metadata, persist_directory) -> Chroma
#   documents           : List[Document] — 待向量化的文档块列表
#   embedding           : Embeddings     — 嵌入模型实例，用于将文本转为向量
#   collection_name     : str            — 集合名称，相当于数据库表名，默认 "langchain"
#                                          多项目共用同一 persist_directory 时务必指定不同名称，否则互相覆盖
#   collection_metadata : dict           — 集合级配置，最常用的键为 "hnsw:space"（距离度量）
#                                          "hnsw:space" 须与 Embedding 的归一化设置匹配：
#                                            "l2"     — 欧氏距离，适合未归一化向量（OpenAIEmbeddings 默认）
#                                            "cosine" — 余弦距离，适合已归一化向量（HuggingFaceEmbeddings 开启 normalize_embeddings=True）
#                                            "ip"     — 内积，已归一化时与余弦等价，未归一化时慎用
#   persist_directory   : str            — 向量库持久化目录路径（None 则仅内存存储，重启丢失）
vectorstore = Chroma.from_documents(
    documents=chunks,                          # List[Document]: 分块后的文档列表
    embedding=embeddings,                      # OpenAIEmbeddings: 嵌入模型（未归一化）
    collection_name="rag_openai",              # str: 集合名称，避免与其他项目共用默认名 "langchain" 冲突
    collection_metadata={"hnsw:space": "l2"},  # str: 欧氏距离，与 OpenAIEmbeddings 未归一化输出匹配
    persist_directory="./chroma_db",           # str: 向量库本地存储目录
)
print(f"向量库已创建，共存入 {len(chunks)} 条向量")  # int: 分块后的文档数量，与实际写入向量库的条目数相同
```

#### 8.3.2 HuggingFaceEmbeddings — 本地嵌入（无需 API Key）

```python
# ============================================================
# 备选方案 — HuggingFaceEmbeddings（本地运行，无需 API Key）
# OpenAIEmbeddings 的 model 参数仅支持 OpenAI 官方模型名称，
# 若要使用 HuggingFace Hub 上的嵌入模型，需改用 HuggingFaceEmbeddings。
# 安装依赖: pip install langchain-huggingface sentence-transformers
# ============================================================
from langchain_huggingface import HuggingFaceEmbeddings  # HuggingFaceEmbeddings: HF 本地嵌入封装

# HuggingFaceEmbeddings(model_name, model_kwargs, encode_kwargs) -> HuggingFaceEmbeddings
#   model_name      : str  — HuggingFace Hub 模型 ID，首次运行自动下载到本地缓存
#                     常用中文模型：
#                       "BAAI/bge-small-zh-v1.5" : 维度 512，轻量快速，推荐入门
#                       "BAAI/bge-base-zh-v1.5"  : 维度 768，精度与速度均衡
#                       "BAAI/bge-large-zh-v1.5" : 维度 1024，精度最高，显存占用大
#                       "shibing624/text2vec-base-chinese" : 维度 768，社区常用
#   model_kwargs    : dict — 传递给底层 sentence-transformers 模型的参数
#                     device : str — 推理设备，"cpu" 或 "cuda"（需安装 PyTorch GPU 版）
#   encode_kwargs   : dict — 文本编码阶段的参数
#                     normalize_embeddings : bool — 是否对输出向量做 L2 归一化
#                                           余弦相似度检索场景建议设为 True
hf_embeddings = HuggingFaceEmbeddings(
    model_name="BAAI/bge-small-zh-v1.5",          # str: HF Hub 模型 ID，自动下载
    model_kwargs={"device": "cuda"},                # dict: 推理设备
    encode_kwargs={"normalize_embeddings": True},  # dict: L2 归一化，余弦检索推荐开启
)

# 替换嵌入模型后，Chroma 用法完全相同，其余代码无需修改
# 注意：hf_embeddings 开启了 normalize_embeddings=True（L2 归一化），
#       须将 hnsw:space 设为 "cosine"，与归一化向量的距离度量匹配
vectorstore_hf = Chroma.from_documents(
    documents=chunks,                              # List[Document]: 分块后的文档列表
    embedding=hf_embeddings,                       # HuggingFaceEmbeddings: 本地嵌入模型（已归一化）
    collection_name="rag_bge",                     # str: 独立集合名，与 8.3.1 的 "rag_openai" 区分
    collection_metadata={"hnsw:space": "cosine"},  # str: 余弦距离，与已归一化向量匹配
    persist_directory="./chroma_db_hf",            # str: 单独的持久化目录，避免与 OpenAI 向量库混用
)
print(f"HF 向量库已创建，共存入 {len(chunks)} 条向量")  # int: 与 chunks 长度一致
```

#### 8.3.3 相似度检索 — as_retriever

```python
# ============================================================
# 相似度检索 — vectorstore.as_retriever()
# ============================================================
from langchain_core.vectorstores import VectorStoreRetriever  # 类型提示

# vectorstore.as_retriever(**kwargs) -> VectorStoreRetriever
#   search_type : str — 检索策略
#     "similarity"（默认）
#         纯相似度排序，返回与 query 最近的 k 个文档；可能出现多个块来自同一段落的冗余问题
#
#     "mmr" — Maximal Marginal Relevance（最大边际相关性）
#         解决"相关但冗余"问题：先候选 fetch_k 个相关文档，再从中贪心选出 k 个——
#         每轮选择时同时考虑"与 query 的相似度"和"与已选文档的差异度"，
#         用 lambda_mult 在两者之间权衡：
#           lambda_mult=1.0 → 完全按相似度选，等同于 similarity
#           lambda_mult=0.0 → 完全按多样性选，忽略与 query 的相关性
#           lambda_mult=0.5 → 推荐默认值，相关性与多样性各占一半
#         适用场景：知识库中有大量相似段落（如同一文档被分成多块），
#                  用 similarity 会返回一堆重复内容，改用 mmr 可显著提升结果多样性
#         search_kwargs 专属参数：
#           fetch_k    : int   — 候选池大小，先从向量库取 fetch_k 个，再从中挑 k 个；默认 20
#           lambda_mult: float — 相关性与多样性权衡系数，取值 0.0~1.0，默认 0.5
#
#     "similarity_score_threshold"
#         带分数过滤的相似度检索；search_kwargs 须配合 score_threshold 使用
#
#   search_kwargs: dict — 检索参数，所有键均可组合使用
#                 k               : int   — 最终返回的文档数量，默认 4
#                 fetch_k         : int   — mmr 模式候选池大小，默认 20（仅 mmr 有效）
#                 lambda_mult     : float — mmr 多样性系数 0~1，默认 0.5（仅 mmr 有效）
#                 score_threshold : float — 最低相似度分数（仅 similarity_score_threshold 有效）
#                 filter          : dict  — 按 metadata 字段过滤，只在命中条件的文档中检索
#                                   语法：
#                                     精确匹配     {"source": "data/langchain.txt"}
#                                     多条件 AND   {"source": "data/a.txt", "page": 0}
#                                     比较运算符   {"page": {"$gte": 2}}        # page >= 2
#                                                  {"page": {"$in": [0, 1, 2]}} # page 在列表中
#                                                  {"source": {"$ne": "draft"}} # source != "draft"
#                                   注意：filter 的键必须是文档 metadata 中实际存在的字段，
#                                         否则过滤条件不生效（不报错但结果为空）

# 示例一：similarity（默认，速度快）
retriever = vectorstore.as_retriever(
    search_type="similarity",          # str: 纯相似度排序
    search_kwargs={
        "k": 3,                        # int: 返回最相似的 3 个文档块
        # "filter": {"source": "data/langchain.txt"},  # 按来源文件过滤（按需开启）
    },
)

# 示例二：mmr（多样性更好，适合知识库有大量相似段落的场景）
retriever_mmr = vectorstore.as_retriever(
    search_type="mmr",                 # str: 最大边际相关性，相关+多样性兼顾
    search_kwargs={
        "k": 3,                        # int: 最终返回 3 个文档块
        "fetch_k": 15,                 # int: 候选池先取 15 个，再从中挑 3 个
        "lambda_mult": 0.5,            # float: 0.5 = 相关性与多样性各半（推荐默认值）
    },
)

# retriever.invoke(query) -> List[Document]
#   query   : str — 检索查询文本
#   返回值   : List[Document] — 最相关的文档块列表，按相似度降序排列
results = retriever.invoke("LangChain 有什么主要用途？")  # List[Document]: 检索结果
for i, doc in enumerate(results):   # i: int 序号，doc: Document 文档块
    print(f"\n--- 检索结果 {i+1} ---")
    print(doc.page_content)          # str: 文档块的文本内容
    print(f"元数据: {doc.metadata}")  # dict: 来源文件等元数据
```

#### 8.3.4 FAISS — 高性能内存向量存储

```python
# ============================================================
# FAISS（Facebook AI Similarity Search）— 高性能向量检索库
# 与 Chroma 的主要区别：
#   Chroma : 带持久化服务器，适合开发阶段和小型项目
#   FAISS  : 纯内存 + 可序列化到磁盘，无需启动服务器，生产环境常用
# 安装依赖：pip install faiss-cpu（CPU 版）或 faiss-gpu（GPU 版，需 CUDA）
# ============================================================
from langchain_community.vectorstores import FAISS  # FAISS: Facebook AI 向量存储封装
from langchain_openai import OpenAIEmbeddings        # OpenAIEmbeddings: 嵌入模型

# base_url 须指向兼容 OpenAI API 格式的服务（必须实现 POST /v1/embeddings 端点）：
#   官方 OpenAI（默认）         : https://api.openai.com/v1          （不填即使用此默认值）
#   国内中转站                  : https://api.chatanywhere.tech/v1
#   阿里百炼（OpenAI 兼容模式） : https://dashscope.aliyuncs.com/compatible-mode/v1
#   本地代理（LiteLLM/OneAPI）  : http://localhost:4000/v1
#   HF TEI 服务（兼容模式）     : http://your-tei-server:8080/v1
# 判断地址是否可用：向 {base_url}/embeddings 发请求，
#   响应须为 {"object":"list","data":[{"embedding":[...],"index":0}],...}，
#   格式与 OpenAI 官方完全一致才可使用；
#   HF 标准推理 API 格式不兼容，须改用 HuggingFaceEmbeddings（见 8.3.2）。
embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",  # str: 嵌入模型名称，1536 维，性价比首选
    base_url=OPENAI_BASE_URL,        # str: 兼容 OpenAI API 格式的服务地址
)

# FAISS.from_documents(documents, embedding) -> FAISS
#   documents : List[Document] — 待向量化的文档块列表
#   embedding : Embeddings     — 嵌入模型实例，用于文本→向量转换
#   返回值     : FAISS          — 内存中的 FAISS 向量库实例（不自动持久化）
faiss_store = FAISS.from_documents(
    documents=chunks,      # List[Document]: 分块后的文档列表（复用 8.2 生成的 chunks）
    embedding=embeddings,  # OpenAIEmbeddings: 嵌入模型实例
)
print(f"FAISS 向量库已构建，共 {len(chunks)} 条向量")  # int: 写入向量库的条目数

# ---- 持久化到磁盘 ----
# faiss_store.save_local(folder_path) -> None
#   folder_path: str — 保存目录路径（自动创建）
#   保存后目录下会生成两个文件：
#     index.faiss : 二进制向量索引文件（FAISS 原生格式）
#     index.pkl   : 文档元数据序列化文件（pickle 格式）
faiss_store.save_local("./faiss_index")  # None: 保存到本地目录，无返回值
print("FAISS 索引已保存到 ./faiss_index")

# ---- 从磁盘加载 ----
# FAISS.load_local(folder_path, embeddings, allow_dangerous_deserialization) -> FAISS
#   folder_path                    : str       — 保存目录路径
#   embeddings                     : Embeddings — 必须与保存时使用的嵌入模型一致
#   allow_dangerous_deserialization: bool      — v0.3.9+ 新增安全参数
#                                               加载 pickle 文件须显式设为 True
#                                               （仅加载自己保存的可信文件时使用）
faiss_loaded = FAISS.load_local(
    "./faiss_index",                         # str: 向量库保存目录
    embeddings,                              # Embeddings: 与保存时相同的嵌入模型
    allow_dangerous_deserialization=True,    # bool: 信任本地 pickle 文件时设为 True
)
print("FAISS 索引加载成功")  # 确认加载正常

# ---- 合并两个 FAISS 索引（增量扩充知识库）----
# faiss_store.merge_from(other) -> None
#   other: FAISS — 另一个 FAISS 实例，将其向量和文档合并到当前实例
# 适用场景：分批构建知识库后合并，或动态追加新文档时更新索引
# faiss_store.merge_from(faiss_loaded)  # None: 将 faiss_loaded 并入 faiss_store

# ---- 检索用法与 Chroma 完全相同 ----
faiss_retriever = faiss_store.as_retriever(
    search_type="similarity",  # str: 余弦相似度检索（默认策略）
    search_kwargs={"k": 3},    # dict: 返回最相似的 3 条结果
)
# retriever.invoke(query) -> List[Document]
#   query  : str — 检索查询文本
#   返回值  : List[Document] — 按相似度降序排列的检索结果列表
faiss_results = faiss_retriever.invoke("LangChain 有什么主要用途？")  # List[Document]: FAISS 向量检索结果
for i, doc in enumerate(faiss_results):  # i: int 序号，doc: Document 文档块
    print(f"FAISS 结果 {i+1}: {doc.page_content[:60]}...")  # str: 前 60 字符预览
```

### 8.4 构建完整 RAG 链

```python
# ============================================================
# 完整 RAG 链 — 检索 + 生成的端到端 LCEL 管道
# ============================================================
from langchain_core.prompts import ChatPromptTemplate     # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_core.runnables import RunnablePassthrough  # RunnablePassthrough: 透传组件
from langchain_openai import ChatOpenAI                   # ChatOpenAI: OpenAI 模型

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温保证事实性
parser = StrOutputParser()                                   # StrOutputParser: 字符串解析器

# RAG 提示词模板：明确要求模型「只基于给定上下文回答」
rag_prompt = ChatPromptTemplate.from_messages([
    ("system",
     "你是一位知识问答助手。请严格基于以下检索到的上下文内容回答用户问题，"
     "不要使用上下文以外的知识。如果上下文中没有相关信息，请直接说「我不知道」。\n\n"
     "上下文：\n{context}"),
    # {context}: str — 检索到的文档片段，拼接为字符串
    ("human", "{question}"),  # str: 用户问题占位符
])

def format_docs(docs: list) -> str:
    """
    将 List[Document] 格式化为单个字符串，用于注入 Prompt 的 context 字段。

    Args:
        docs: List[Document] — 检索到的文档块列表

    Returns:
        str — 用双换行符分隔的拼接文本
    """
    return "\n\n".join(doc.page_content for doc in docs)  # str: 所有文档块用换行分隔拼接

# 完整 RAG LCEL 链：
# {"context": retriever | format_docs, "question": RunnablePassthrough()}
#   → RunnableParallel（并行两个分支）
#     context 分支 : 将 question 送入 retriever 检索，再 format_docs 格式化为字符串
#     question 分支: RunnablePassthrough 原样传递 question 字符串
#   → rag_prompt  : 注入 context 和 question，格式化为消息列表
#   → llm         : 调用 OpenAI 模型，返回 AIMessage
#   → parser      : 提取 AIMessage.content 为 str
rag_chain = (
    {
        "context":  retriever | format_docs,  # Runnable: 检索→格式化，输入 str，输出 str
        # retriever 之所以能使用 | 运算符：
        #   VectorStoreRetriever 继承自 Runnable 基类，Runnable 实现了 __or__ 方法，
        #   因此所有 Runnable 实例（retriever / llm / prompt 等）均支持 | 串联。
        # format_docs 是普通函数，LCEL 的自动包装机制会将其隐式转换为 RunnableLambda：
        #   retriever | format_docs  等价于  retriever | RunnableLambda(format_docs)
        #   只要 | 右侧是可调用对象，LangChain 自动完成包装，无需手动声明。
        # 该子链数据流：str → retriever.invoke() → List[Document] → format_docs() → str
        "question": RunnablePassthrough(),     # Runnable: 透传，输入 str，输出 str
    }
    | rag_prompt   # ChatPromptTemplate: 输入 dict，输出 ChatPromptValue
    | llm          # ChatOpenAI: 输入 ChatPromptValue，输出 AIMessage
    | parser       # StrOutputParser: 输入 AIMessage，输出 str
)

# 测试 RAG 链
test_questions = [     # List[str]: 测试问题列表
    "LangChain 是什么时候创建的？",
    "LCEL 的 | 运算符有什么特点？",
    "LangChain 可以用来做哪些类型的应用？",
]
for q in test_questions:                  # q: str 测试问题
    print(f"\n问题：{q}")
    answer = rag_chain.invoke(q)          # str: RAG 链生成的回答
    print(f"回答：{answer}")
```

### 8.5 高级检索器

#### 8.5.1 MultiQueryRetriever — 多路查询提升召回率

```python
# ============================================================
# MultiQueryRetriever — 用 LLM 自动生成多个查询视角，合并检索结果
# 解决问题：单一查询措辞受限，不同角度的问法可能检索到不同文档
# 工作原理：
#   ① 用 LLM 将原始 query 改写为 N 个不同角度的子查询
#   ② 每个子查询分别调用底层检索器，得到多组候选文档
#   ③ 对所有结果按 page_content 去重合并，扩大召回范围
# ============================================================
from langchain_classic.retrievers import MultiQueryRetriever  # MultiQueryRetriever: 多查询检索封装（v1.x 已迁至 langchain-classic）
from langchain_openai import ChatOpenAI                        # ChatOpenAI: 用于生成多路查询的 LLM
import logging                                                  # logging: 标准日志库
# v1.x 迁移说明：MultiQueryRetriever 在 LangChain v1.0 中已从 langchain.retrievers
# 迁移至 langchain_classic.retrievers，需先安装：pip install langchain-classic

# 开启 MultiQueryRetriever 的调试日志，可打印 LLM 生成的子查询内容
logging.basicConfig()
logging.getLogger("langchain_classic.retrievers.multi_query").setLevel(logging.INFO)
# 设为 INFO 级别后，invoke 时会在控制台打印生成的子查询列表，便于调试
# 注意：langchain_classic 模块内部以 __name__ 注册 logger，
# 因此名称为 "langchain_classic.retrievers.multi_query"（非旧版 "langchain.retrievers.multi_query"）

base_retriever = faiss_store.as_retriever(
    search_kwargs={"k": 3}  # dict: 每个子查询返回 3 条候选结果
)

# MultiQueryRetriever.from_llm(retriever, llm) -> MultiQueryRetriever
#   retriever: BaseRetriever — 底层检索器（VectorStoreRetriever 等）
#   llm      : BaseChatModel — 用于生成多路查询改写的语言模型
#              内部使用固定提示词让 LLM 输出多个改写查询（默认 3 个）
#   返回值    : MultiQueryRetriever — 多查询检索器实例
multi_query_retriever = MultiQueryRetriever.from_llm(
    retriever=base_retriever,                               # BaseRetriever: 底层检索器
    llm=ChatOpenAI(model="gpt-4o-mini", temperature=0.0),  # ChatOpenAI: 查询改写模型
)

# retriever.invoke(query) -> List[Document]
#   query  : str — 原始用户查询文本
#   内部流程：LLM 改写 → 分别检索 → 去重合并
#   返回值  : List[Document] — 去重后的合并结果列表（通常多于单次检索）
mqr_docs = multi_query_retriever.invoke("LangChain 的主要功能是什么？")  # List[Document]: 多路查询合并去重的结果集，召回率高于单次检索
print(f"MultiQueryRetriever 检索到 {len(mqr_docs)} 条结果（去重后）")  # int: 结果数量
for doc in mqr_docs:
    print(f"  - {doc.page_content[:60]}...")  # str: 每条结果前 60 字符预览
```

#### 8.5.2 ContextualCompressionRetriever — 上下文压缩去噪

```python
# ============================================================
# ContextualCompressionRetriever — 检索后用 LLM 压缩/过滤冗余内容
# 解决问题：向量检索返回的文档块常含与问题无关的噪音文本，
#           注入 Prompt 后既浪费 Token 又可能干扰模型判断
# 工作原理：
#   ① base_retriever 召回 k 条候选文档（较多，保证覆盖率）
#   ② compressor 对每条候选文档调用 LLM，仅保留与 query 相关的句子
#   ③ 返回压缩精简后的文档列表（page_content 变短，信噪比更高）
# 常用压缩器（按速度排序，速度越快精度越低）：
#   EmbeddingsFilter   : 用嵌入相似度过滤整块文档，无需 LLM 调用（最快）
#   LLMChainFilter     : 用 LLM 判断整块文档是否相关并过滤（较快）
#   LLMChainExtractor  : 用 LLM 逐文档提取相关句子（最精准，但调用次数多）
# ============================================================
from langchain_classic.retrievers import ContextualCompressionRetriever                   # 压缩检索器（v1.x 已迁至 langchain-classic）
from langchain_classic.retrievers.document_compressors import LLMChainExtractor           # LLM 内容提取压缩器（v1.x 已迁至 langchain-classic）
from langchain_openai import ChatOpenAI                                                    # OpenAI 模型
# v1.x 迁移说明：ContextualCompressionRetriever 和 LLMChainExtractor 在 LangChain v1.0 中
# 已从 langchain.retrievers / langchain.retrievers.document_compressors
# 迁移至 langchain_classic.retrievers / langchain_classic.retrievers.document_compressors
# 需先安装：pip install langchain-classic

compressor_llm = ChatOpenAI(
    model="gpt-4o-mini", temperature=0.0  # ChatOpenAI: 低温保证压缩结果稳定、不编造
)

# LLMChainExtractor.from_llm(llm) -> LLMChainExtractor
#   llm    : BaseChatModel — 用于内容提取的语言模型
#   返回值  : LLMChainExtractor — 文档压缩器实例
#   工作方式：对每条候选文档，让 LLM 提取与 query 相关的片段；
#             若整块文档均不相关，返回空字符串（该文档被丢弃）
compressor = LLMChainExtractor.from_llm(compressor_llm)  # LLMChainExtractor: 内容提取压缩器

# ContextualCompressionRetriever(base_compressor, base_retriever) -> ContextualCompressionRetriever
#   base_compressor : BaseDocumentCompressor — 文档压缩器（先召回，再压缩）
#   base_retriever  : BaseRetriever          — 底层检索器（负责初始召回）
#   返回值           : ContextualCompressionRetriever — 压缩检索器实例
compression_retriever = ContextualCompressionRetriever(
    base_compressor=compressor,                                        # 压缩器：提取相关句子
    base_retriever=faiss_store.as_retriever(search_kwargs={"k": 4}),  # 底层检索器：多召回 4 条候选
    # 召回数量（k=4）建议略大于最终期望结果数，给压缩器充足的候选集
)

# retriever.invoke(query) -> List[Document]
#   内部步骤：base_retriever 召回 → compressor 逐条压缩/过滤 → 返回精简文档
#   注意：每条候选文档均触发一次 LLM 调用（n 条候选 → n 次压缩调用），
#         Token 消耗较高；适合答案质量要求高、候选文档量适中（k≤8）的场景
compressed_docs = compression_retriever.invoke("LangChain 的 RAG 流程是什么？")  # List[Document]: 经 LLM 压缩的精简文档列表，page_content 仅含相关句子
print(f"压缩后共 {len(compressed_docs)} 条结果")  # int: 压缩后剩余文档数（≤ 初始 k）
for doc in compressed_docs:
    print(f"  压缩内容: {doc.page_content[:80]}...")  # str: 压缩后的相关片段（更短更精准）
```

#### 8.5.3 EnsembleRetriever — 混合稠密 + 稀疏检索

```python
# ============================================================
# EnsembleRetriever — 融合向量检索（稠密）与关键词检索（稀疏）
# 解决问题：
#   向量检索（稠密）: 语义理解强，但对专有名词/代码/精确词命中弱
#   BM25 检索（稀疏）: 关键词精准匹配，但无法理解语义
# 工作原理：
#   两路检索器分别召回候选文档，用 RRF（Reciprocal Rank Fusion）融合排名，
#   按 weights 权重加权后重排，取二者之长
# 适用场景：混合中文专有术语 + 自然语言描述的知识库，效果明显优于单路检索
# 安装依赖：pip install rank_bm25
# ============================================================
from langchain_classic.retrievers import EnsembleRetriever    # EnsembleRetriever: 混合检索器（v1.x 已迁至 langchain-classic）
from langchain_community.retrievers import BM25Retriever      # BM25Retriever: 关键词稀疏检索器（仍在 langchain-community，路径不变）
from langchain_core.documents import Document                 # Document: 文档数据类
# v1.x 迁移说明：EnsembleRetriever 在 LangChain v1.0 中
# 已从 langchain.retrievers 迁移至 langchain_classic.retrievers
# BM25Retriever 保留在 langchain_community.retrievers，导入路径不变

# BM25Retriever.from_documents(documents, k) -> BM25Retriever
#   documents: List[Document] — 建立 BM25 倒排索引的文档列表（须与向量库使用相同文档）
#   k        : int            — 返回最相关的文档数量
#   返回值    : BM25Retriever  — 基于 BM25 算法的关键词检索器（无需嵌入模型）
bm25_retriever = BM25Retriever.from_documents(
    chunks,  # List[Document]: 分块后的文档列表（与 faiss_store 使用相同文档集）
    k=3,     # int: BM25 返回前 3 条关键词匹配结果
)

# 稠密检索器（向量语义检索）
vector_retriever = faiss_store.as_retriever(
    search_kwargs={"k": 3}  # dict: 向量检索返回前 3 条语义相似结果
)

# EnsembleRetriever(retrievers, weights) -> EnsembleRetriever
#   retrievers: List[BaseRetriever] — 参与融合的检索器列表（顺序对应 weights）
#   weights   : List[float]         — 各检索器的权重，须加和为 1.0
#                                     权重越高，该路结果在最终 RRF 排名中影响越大
#   返回值     : EnsembleRetriever  — 混合检索器实例
ensemble_retriever = EnsembleRetriever(
    retrievers=[bm25_retriever, vector_retriever],  # List[BaseRetriever]: 两路检索器
    weights=[0.4, 0.6],  # List[float]: BM25 权重 0.4，向量权重 0.6
    # 权重调优建议：
    #   知识库以专有名词/代码/产品名为主 → 提高 BM25 权重（如 [0.6, 0.4]）
    #   知识库以自然语言文章/文档为主    → 提高向量权重（如 [0.3, 0.7]）
)

# retriever.invoke(query) -> List[Document]
#   内部步骤：
#     ① bm25_retriever.invoke(query)  → 3 条关键词匹配结果
#     ② vector_retriever.invoke(query)→ 3 条语义相似结果
#     ③ RRF 算法按 weights 加权融合两路排名，去重后重排
#   返回值: List[Document] — 融合排名后的去重文档列表，兼顾语义和关键词命中
ensemble_docs = ensemble_retriever.invoke("LangChain LCEL 管道")  # List[Document]: 混合检索的融合结果
print(f"EnsembleRetriever 检索到 {len(ensemble_docs)} 条结果")  # int: 融合后文档数
for doc in ensemble_docs:
    print(f"  - {doc.page_content[:60]}...")  # str: 每条结果前 60 字符预览
```

---

## 九、Tool 与 Agent

### 9.1 定义工具（@tool 装饰器）

```python
# ============================================================
# @tool 装饰器 — 将 Python 函数包装为 LangChain Tool
# Tool 对象包含：名称、描述（从 docstring 提取）、参数 schema（从类型注解生成）
# Agent 通过工具描述自动决策何时调用哪个工具
#
# 【多参数支持】：@tool 完全支持多参数，参数 schema 从函数类型注解自动推断；
#   但推断结果只含参数名和类型，不含每个参数的语义说明（Field description），
#   参数含义不直观时 LLM 可能填错；参数少、含义显而易见时使用 @tool 即可。
#
# 【与 StructuredTool 的核心差异】：
#   @tool         : schema 自动推断，LLM 看不到每个参数的语义描述；写法简洁
#   StructuredTool: 用 Pydantic Field(description=...) 显式定义，LLM 可获得
#                   完整的参数语义说明，多参数且含义复杂时调用准确率更高
# ============================================================
from langchain_core.tools import tool  # tool: 将函数装饰为 Tool 的装饰器
import math                            # math: 数学运算标准库
import json                            # json: JSON 处理标准库

@tool
def calculator(expression: str) -> str:
    """
    安全的数学表达式计算器。支持加减乘除、幂运算、平方根等基本运算。

    Args:
        expression: str — 数学表达式字符串，如 "2 + 3 * 4" 或 "sqrt(16)"

    Returns:
        str — 计算结果，失败时返回错误信息
    """
    try:
        # eval 安全限制：只允许访问 math 模块中的函数
        allowed_names = {k: v for k, v in math.__dict__.items() if not k.startswith("_")}  # Dict[str, Any]: 允许 eval 访问的名称空间（仅 math 模块函数）
        result = eval(expression, {"__builtins__": {}}, allowed_names)
        # eval(expr, globals, locals): 在受限命名空间中执行表达式，返回计算结果
        return str(result)  # str: 将计算结果转换为字符串返回
    except Exception as e:  # Exception: 捕获所有计算异常
        return f"计算错误: {e}"  # str: 返回错误描述，让 Agent 感知失败

@tool
def get_weather(city: str) -> str:
    """
    获取指定城市的天气信息（模拟实现，实际场景替换为真实天气 API）。

    Args:
        city: str — 城市名称，如"北京"、"上海"、"广州"

    Returns:
        str — JSON 格式的天气信息字符串
    """
    # 模拟天气数据 — Dict[str, Dict]: 城市 → 天气信息的映射
    weather_data = {
        "北京": {"温度": "25°C", "天气": "晴天", "湿度": "40%"},
        "上海": {"温度": "28°C", "天气": "多云", "湿度": "65%"},
        "广州": {"温度": "32°C", "天气": "雷阵雨", "湿度": "80%"},
    }
    data = weather_data.get(city, {"错误": f"未找到城市 '{city}' 的天气数据"})  # Dict: 获取对应城市的天气信息，不存在时返回错误提示
    return json.dumps(data, ensure_ascii=False)  # str: JSON 格式的天气数据字符串

@tool
def search_knowledge(query: str) -> str:
    """
    在 LangChain 知识库中检索相关信息（使用前面构建的 RAG 检索器）。

    Args:
        query: str — 检索查询词

    Returns:
        str — 检索到的相关文档内容
    """
    docs = retriever.invoke(query)       # List[Document]: 检索结果文档列表
    return format_docs(docs)             # str: 格式化后的文档内容字符串

# 打印工具信息
print(f"工具名称: {calculator.name}")   # str: 工具名称，来自函数名
print(f"工具描述: {calculator.description[:80]}...")  # str: 工具描述，来自 docstring
print(f"工具参数: {calculator.args}")   # dict: 工具参数 schema，来自类型注解

tools = [calculator, get_weather, search_knowledge]  # List[Tool]: 工具列表，传给 Agent
```

### 9.2 构建 Tool-Calling Agent

Tool-Calling Agent 的核心依赖是模型对**工具调用协议**的原生支持。目前主流协议分为三类：


| 协议             | 代表                                                                 | 解决的问题                                               | 工具调用返回位置                                                                                                                | 工具定义格式                     | 现状                         |
| -------------- | ------------------------------------------------------------------ | --------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | -------------------------- | -------------------------- |
| **各厂商原生结构化协议** | OpenAI Function Calling Anthropic Tool Use Google Function Calling | 模型如何可靠表达"调哪个工具、传什么参数"                               | OpenAI：`message.tool_calls[]`（独立字段） Anthropic：`content[]` 中的 `tool_use` 块 Google：`content.parts[]` 中的 `functionCall` 条目 | JSON Schema（各厂商请求字段名略有差异）① | 主流，推荐                      |
| **MCP**        | Anthropic 发起的开放标准 （已被多家厂商采纳）                                       | 工具服务如何标准化注册和暴露给模型 （与上面正交：上面管"模型怎么调用"，MCP 管"工具怎么提供"） | 不定义调用位置，属于工具服务层协议                                                                                                       | 标准化 Server/Client 协议②      | 新兴标准，持续扩展                  |
| **ReAct**      | LangChain 早期纯文本解析版 Agent                                           | 模型无原生结构化能力时，如何用提示词约定格式驱动工具调用                        | 模型在文本中按 `Thought → Action → Observation` 格式输出，框架正则解析                                                                    | 无 schema，纯提示词约定            | 纯文本解析版已被取代③ LangGraph 版仍活跃 |


> ① LangChain **不以 OpenAI 协议为内部标准**，有自己的统一抽象层：用户用 `@tool` 定义工具，`bind_tools()` 负责转换为各厂商格式，响应统一解析为 `AIMessage.tool_calls`（其中 `args` 为 dict 对象，非 JSON 字符串）。`create_tool_calling_agent` 因此对 GPT / Claude / Gemini 均可用。
>
> ② LangChain 通过 MCP 插件（`langchain-mcp-adapters` 等）接入 MCP Server，将 MCP 工具自动转换为 LangChain `Tool` 对象后即可与原生 Tool Calling 协议配合使用。
>
> ③ `langchain.agents.create_react_agent` 为旧版（纯文本正则解析）；`langgraph.prebuilt.create_react_agent` 为过渡版（底层已改用原生 Tool Calling，但在 v1.x 中已废弃）；v1.x 现推荐使用 `langchain.agents.create_agent`（见 9.3 节），其内部由 LangGraph 驱动，并通过 middleware 系统替代旧版钩子。

#### 9.2.1 单轮对话

```python
# ============================================================
# create_tool_calling_agent — 构建工具调用 Agent
# 依赖模型的 Function Calling / Tool Calling 能力（GPT-4o 等）
# Agent 在每一步自动决策：回答用户 OR 调用某个工具
# ============================================================
from langchain_classic.agents import create_tool_calling_agent, AgentExecutor
# create_tool_calling_agent / AgentExecutor 在 LangChain v1.x 中已迁至 langchain-classic；
# 新项目推荐使用 langchain.agents.create_agent（见九、3），旧代码迁移时改用此导入。
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder  # 提示词组件
from langchain_openai import ChatOpenAI                                     # OpenAI 模型

# Agent 用的模型必须支持 Tool Calling（gpt-4o / gpt-4o-mini / claude-3 等）
# 「支持 Tool Calling」的含义：
#   普通模型只能输出自由文本；支持 Tool Calling 的模型在 API 层面额外实现了
#   Function Calling 协议——调用方把工具的名称和参数 schema（JSON Schema 格式）
#   随请求一起发给模型；模型需要调用工具时，不在文本 content 字段回复，
#   而是在响应体的 tool_calls 专用字段中写入结构化信息，格式如下：
#     tool_calls[0].function.name      = "calculator"          # str: 工具名
#     tool_calls[0].function.arguments = '{"expr":"sqrt(144)"}' # str: 参数（JSON 字符串，非嵌套对象）
#   AgentExecutor 读取该字段、解析参数、执行函数，再把结果追加到对话继续推理。
#   不支持该协议的模型（如早期 text-davinci）无法可靠地生成上述结构化输出，
#   导致 AgentExecutor 无法解析工具调用，Agent 循环会直接失败。
# 支持 Tool Calling 的常见模型：gpt-4o / gpt-4o-mini / claude-3 系列 / gemini-1.5 等
# temperature=0.0：关闭随机采样，模型每步都选概率最高的 token（贪心解码）。
# 注意：原生 Tool Calling 模型的工具调用结果由 API 的 tool_calls 专用字段返回，
# 底层微调已保证 JSON 格式合法，temperature 不会破坏其结构。
# temperature=0 的真正意义是保证「决策行为」的确定性：
#   - 该不该调用工具、调用哪个工具、传什么参数，每次都给出相同答案
#   - 避免因随机性导致「同一问题时而调工具时而直接回答」的不可复现行为
#   - 便于调试：相同输入下 Agent 的推理路径完全一致
agent_llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # temperature=0 → 贪心解码，保证 Agent 决策行为确定且可复现

# Agent 的 Prompt 须包含以下三个占位符：
#   {chat_history}    : MessagesPlaceholder — 对话历史（多轮场景）
#   {input}           : str — 用户当前输入
#   {agent_scratchpad}: MessagesPlaceholder — Agent 思考过程（工具调用记录）
agent_prompt = ChatPromptTemplate.from_messages([
    ("system",
     "你是一位功能强大的 AI 助手，配备了数学计算、天气查询和知识库检索工具。"
     "请根据用户需求，智能选择合适的工具来回答问题。"
     "如果不需要工具，直接回答即可。"),
    # optional=True: chat_history 不存在时不报错，便于无记忆场景使用
    MessagesPlaceholder(variable_name="chat_history", optional=True),
    ("human", "{input}"),                                  # str: 用户当前输入
    MessagesPlaceholder(variable_name="agent_scratchpad"), # Agent 工具调用记录（必须）
])

# create_tool_calling_agent(llm, tools, prompt) -> Runnable（Agent 核心逻辑）
#   llm    : BaseChatModel — 支持 Tool Calling 的模型
#   tools  : List[Tool]   — 工具列表
#   prompt : ChatPromptTemplate — 包含上述三个占位符的提示词模板
#   返回值  : Runnable — Agent 的单步推理逻辑（不含执行循环）
agent = create_tool_calling_agent(
    llm=agent_llm,        # ChatOpenAI: 工具调用模型
    tools=tools,          # List[Tool]: 工具列表（calculator / get_weather / search_knowledge）
    prompt=agent_prompt,  # ChatPromptTemplate: Agent 提示词模板
)

# AgentExecutor — Agent 执行器，负责循环执行「推理 → 工具调用 → 观察 → 推理」直到完成
# AgentExecutor(agent, tools, verbose, max_iterations, handle_parsing_errors)
#   agent            : Runnable — create_tool_calling_agent 返回的 Agent 核心逻辑
#   tools            : List[Tool] — 与创建 Agent 时相同的工具列表
#   verbose          : bool — True 打印每步的推理和工具调用过程，便于调试
#   max_iterations   : int — 最大循环轮次，防止无限循环，默认 15
#   handle_parsing_errors: bool — True 时遇到解析错误自动重试，而非抛出异常
agent_executor = AgentExecutor(  # 返回值: AgentExecutor(Runnable) — 实现 Runnable 接口，可用 | 接入 LCEL 链作为链中一个节点，支持 .invoke()/.stream()/.batch()
    agent=agent,               # Runnable: Agent 核心推理逻辑
    tools=tools,               # List[Tool]: 可用工具列表
    verbose=True,              # bool: 开启详细日志，打印 Agent 的思考过程
    max_iterations=10,         # int: 最多循环 10 步防止失控
    handle_parsing_errors=True # bool: 解析错误时自动重试
)

# AgentExecutor.invoke(input_dict) -> Dict[str, Any]
#   input_dict : dict — 用户输入字典，包含以下键：
#     "input"        : str              — 用户当前输入（必须）
#     "chat_history" : List[BaseMessage]— 历史消息列表（可选，首次传空列表）
#   注意：prompt 中虽有 agent_scratchpad 占位符，但此处不传，原因如下：
#     agent_scratchpad 记录的是单次调用内部的「工具调用→结果→再推理」草稿，
#     由 AgentExecutor 在每轮循环中自动追加和清空，用户无需也不应手动传入。
#   返回值     : Dict — {"input": str, "output": str, ...}，output 为 Agent 最终回答
#
# 【工具参数的来源：LLM 从自然语言中提取，而非代码解析】
#   以下面的 input 为例，用户用自然语言描述了两个需求：
#     "帮我计算 sqrt(144) + 2^8 的结果，然后告诉我北京今天的天气"
#   整个参数填充过程分为以下四步：
#
#   Step 1 — @tool 生成 JSON Schema 并随请求发给 LLM
#     @tool 装饰器自动把函数的「类型注解 + docstring 整体描述」转成 JSON Schema，例如：
#       {
#         "name": "calculator",
#         "description": "安全的数学表达式计算器...",  ← 来自 docstring 整体描述
#         "parameters": {
#           "properties": {
#             "expression": {"type": "string"}        ← 仅有类型，无参数语义说明
#           }
#         }
#       }
#     注意：@tool 只能把「函数整体描述」放入 description，
#           各参数的语义说明（如"填入数学表达式"）不会出现在 schema 中，
#           LLM 只能依赖参数名 + 函数整体描述来推断如何填参。
#           若参数含义复杂或多参数，应改用 StructuredTool + Pydantic Field(description=...)
#           以便 LLM 获得完整的逐参数语义说明，提升调用准确率。
#     LLM 同时收到：用户输入 + 所有 Tool 的 JSON Schema。
#
#   Step 2 — LLM 凭语义理解决策并填参
#     LLM 不在 content 文本字段回复，而是输出专用的 tool_calls 结构化字段：
#       tool_calls[0].function.name      = "calculator"
#       tool_calls[0].function.arguments = '{"expression": "sqrt(144) + 2**8"}'
#       tool_calls[1].function.name      = "get_weather"
#       tool_calls[1].function.arguments = '{"city": "北京"}'
#     "sqrt(144) + 2**8" 是 LLM 从自然语言中理解并抽取出的数学表达式，
#     "北京" 是 LLM 识别出的城市名——两者均由 LLM 完成，代码层无任何字符串解析。
#
#   Step 3 — AgentExecutor 解析 tool_calls 并调用函数
#     AgentExecutor 读取 tool_calls 字段，解析 JSON 参数，
#     实际调用：calculator(expression="sqrt(144) + 2**8")
#              get_weather(city="北京")
#
#   Step 4 — 工具结果追加到上下文，LLM 汇总生成最终回答
#     两个工具的返回值被追加到 agent_scratchpad，LLM 综合两个结果输出最终答案。
#
#   结论：工具参数中的具体值（数学表达式、城市名等）完全由 LLM 语义提取并填入，
#         因此模型必须支持 Function Calling（如 gpt-4o-mini）才能使用此 Agent 模式。
result = agent_executor.invoke({
    "input": "帮我计算 sqrt(144) + 2^8 的结果，然后告诉我北京今天的天气",
    "chat_history": [],  # List[BaseMessage]: 首次对话传空列表
})
print(f"\n最终回答: {result['output']}")  # str: Agent 的最终回答
```

#### 9.2.2 多轮对话

```python
# ============================================================
# 多轮对话 — 手动维护 chat_history，agent_scratchpad 始终不传
# ============================================================
from langchain_core.messages import HumanMessage, AIMessage  # 消息类型

chat_history = []  # List[BaseMessage]: 跨轮次手动维护的对话历史

def chat(user_input: str) -> str:
    """
    封装单轮对话，自动维护 chat_history 供下一轮使用。

    Args:
        user_input: str — 用户当前输入

    Returns:
        str — Agent 的最终回答
    """
    result = agent_executor.invoke({
        "input": user_input,
        "chat_history": chat_history,  # List[BaseMessage]: 传入历史，agent_scratchpad 不传
        # agent_scratchpad 由 AgentExecutor 内部自动管理，每次 invoke 开始时清空，
        # 结束时丢弃（不携带到下一轮），因此多轮时也不需要传。
    })
    answer = result["output"]  # str: Agent 最终回答（不含中间工具调用过程）

    # 本轮结束后追加到历史，供下一轮 invoke 使用
    # 注意：只追加最终回答，不追加 scratchpad 中的工具调用中间步骤，避免 Prompt 膨胀
    chat_history.append(HumanMessage(content=user_input))  # HumanMessage: 用户输入
    chat_history.append(AIMessage(content=answer))         # AIMessage: Agent 最终回答

    return answer

# 多轮对话示例
print(chat("帮我计算 sqrt(144) + 2^8"))           # 首轮：chat_history 为空
print(chat("刚才的计算结果是多少？"))              # 第二轮：chat_history 含上一轮内容
```

### 9.3 使用新版 create_agent API

**CompiledGraph vs AgentExecutor invoke 接口对比**


| 接口                              | invoke 参数                                                                                                                                  |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| `AgentExecutor`（旧式）             | `{"input": str, "chat_history": List[BaseMessage]}` → `"input"`/`"chat_history"` 对应 PromptTemplate 中的占位符，即「给模板赋值」                          |
| `CompiledGraph`（`create_agent`） | `{"messages": List[dict/BaseMessage]}` → `"messages"` 是 LangGraph 图的「状态字段」，不对应任何模板占位符；图内部直接将整个消息列表作为对话上下文传给 LLM，每次工具调用的结果也会自动 append 到该列表中 |


**AgentExecutor scratchpad vs LangGraph Agent 中间结果存储对比**


| 对比维度   | LangChain 旧版 AgentExecutor | LangGraph Agent（`create_agent`） |
| ------ | -------------------------- | ------------------------------- |
| 中间结果存储 | `scratchpad`（字符串拼接）        | `ToolMessage`（消息对象追加到列表）        |
| 可见性    | 对外隐藏，无法直接访问                | 全程可查：`result["messages"]`       |
| 多轮工具调用 | 顺序拼接到 prompt 字符串           | 每轮各有独立的 `ToolMessage`           |


**多轮工具调用具体示意：**

旧版 AgentExecutor 将所有中间步骤拼成一条 scratchpad 字符串追加到 prompt 末尾，格式为：`...Thought → Action → Observation → Thought → Action → Observation → Final Answer`，所有轮次混为一体，无法单独访问某轮结果。

LangGraph Agent 则将每一步单独存为消息对象，按顺序追加到 `messages` 列表中：

- `[0]` `HumanMessage` — 用户输入
- `[1]` `AIMessage(tool_calls=[...])` — 第 1 次工具调用指令
- `[2]` `ToolMessage` — 第 1 轮工具返回（独立对象，可单独访问）
- `[3]` `AIMessage(tool_calls=[...])` — 第 2 次工具调用指令
- `[4]` `ToolMessage` — 第 2 轮工具返回（独立对象，可单独访问）
- `[5]` `AIMessage(content="...")` — 最终回答

可精确访问任意一轮：`result["messages"][2].content` 即第 1 轮工具结果。

```python
# ============================================================
# create_agent() — LangChain v1.0 正式引入的标准 Agent 创建接口
# 底层由 LangGraph 驱动，取代旧式 create_tool_calling_agent + AgentExecutor 组合
# 相比旧式写法更简洁，通过 middleware 系统实现自定义（before_model / after_model 钩子）
# ============================================================
from langchain.agents import create_agent  # create_agent: 高层 Agent 工厂函数

# create_agent(model, tools, *, system_prompt, middleware, response_format) -> CompiledGraph
#   model         : str | BaseChatModel — "provider:model_name" 格式或模型实例；
#                   传字符串时内部调用 init_chat_model() 初始化，
#                   base_url / api_key 自动从环境变量 OPENAI_BASE_URL / OPENAI_API_KEY 读取
#                   （即第 2 节统一设置的环境变量），无需在此处手动传入；
#                   若需显式指定 base_url，改传模型实例：
#                   model=ChatOpenAI(model="gpt-4o-mini", base_url="https://...")
#   tools         : List[BaseTool | Callable | dict] | None — 工具列表，None 时无工具调用循环
#   system_prompt : str | SystemMessage | None — 系统指令（str 自动转为 SystemMessage）
#   middleware    : Sequence[AgentMiddleware] | None — 自定义中间件（before_model/after_model 钩子）
#   response_format: ResponseFormat | type | dict | None — 结构化输出格式（可选）
#   返回值        : CompiledGraph — LangGraph 编译图，实现 Runnable 接口，可用 | 接入 LCEL 链作为链中一个节点；
#                   支持 .invoke() / .stream() / .batch()；无需额外 AgentExecutor 包装；
#                   注意：.invoke() 输入为 {"messages": [...]}，而非 AgentExecutor 的 {"input": "..."}
simple_agent = create_agent(
    model="openai:gpt-4o-mini",           # str: 模型标识，base_url 自动从环境变量读取
    tools=[calculator, get_weather],       # List[Tool]: 工具列表
    system_prompt="你是一位智能助手，善用工具解决问题。",  # str: 系统指令
)

# simple_agent.invoke(input_dict) -> Dict[str, List[BaseMessage]]
#   input_dict: dict — 输入必须包含 "messages" 键，值为消息列表；
#               【为何传消息列表而非普通字符串】：
#               create_agent 底层由 LangGraph 驱动，LangGraph 的状态（State）以
#               消息列表为核心数据结构，每条消息记录角色（role）和内容（content），
#               Agent 执行过程中产生的所有中间消息（工具调用、工具结果）也追加到此列表，
#               最终整个对话历史都保存在 "messages" 中；
#               role 可为 "user"（用户）/ "assistant"（模型）/ "tool"（工具结果）
#
# messages 列表中每条消息用 dict 表示，必须包含：
#   "role"   : str — 消息角色，"user"（用户）/ "assistant"（模型）/ "system"（系统）
#   "content": str — 消息正文
#   返回值     : Dict[str, List[BaseMessage]] — 同样包含 "messages" 键，
#               值为完整消息链（含用户消息、工具调用消息、工具结果消息、最终回答），
#               取最后一条 result["messages"][-1].content 即为 Agent 最终回答字符串
result = simple_agent.invoke({
    "messages": [
        # dict: role="user" 表示用户发起的消息，content 为消息文本
        {"role": "user", "content": "25 的平方是多少？"}
    ]
})
# Dict[str, List[BaseMessage]]: {"messages": [HumanMessage, AIMessage(工具调用), ToolMessage, AIMessage(最终回答)]}
print(result["messages"][-1].content)  # str: 取最后一条消息内容即为 Agent 最终回答
```

### 9.4 StructuredTool — 显式参数语义描述的工具定义

```python
# ============================================================
# StructuredTool — 处理多个参数的工具定义方式
# 与 @tool 装饰器的对比：
#   @tool         : 适合单参数或少参数、逻辑简单的工具，写法最简洁；
#                   schema 从类型注解自动推断，LLM 看不到参数语义描述
#   StructuredTool: 用 Pydantic Field(description=...) 显式定义每个参数的语义，
#                   LLM 可获得完整参数说明，多参数复杂场景调用准确率更高；
#                   或需要动态（工厂模式）创建工具实例的场景
#   注意：两者都支持多参数，选择依据是"是否需要给 LLM 提供参数语义说明"
#
# 【重要】StructuredTool 本身没有 llm 参数，它只负责封装函数（"怎么执行"）；
#   llm 参数属于 Agent 构建层（create_tool_calling_agent(llm, tools, prompt)），
#   Agent 负责"何时调用/调用哪个工具"，LLM 只归 Agent 管，Tool 不感知 LLM。
# ============================================================
from langchain_core.tools import StructuredTool  # StructuredTool: 结构化多参数工具类
from pydantic import BaseModel, Field            # BaseModel: 参数 schema 基类；Field: 字段描述
from typing import Optional                      # Optional: 可选类型标注

# 步骤 1：用 Pydantic 定义工具的输入 schema
# LangChain 使用此 schema 自动生成 JSON Schema，供 LLM 填写参数
class KnowledgeSearchInput(BaseModel):
    """知识库检索工具的输入参数 schema。"""
    query: str = Field(description="检索查询词，描述需要查找的具体信息")
    # query: str（必填）— LLM 根据 description 决定填入什么内容
    max_results: Optional[int] = Field(default=3, description="返回结果数量，默认 3，最大 10")
    # max_results: int（可选）— 控制返回的最大文档条数；省略时使用 default=3

# 步骤 2：定义工具函数（参数名称须与 schema 中的字段名完全一致）
def search_knowledge_base(query: str, max_results: int = 3) -> str:
    """
    在知识库中检索与 query 最相关的文档片段。

    Args:
        query      : str — 检索查询词
        max_results: int — 返回最大文档数，默认 3

    Returns:
        str — 检索结果的拼接文本，未找到时返回提示信息
    """
    retriever_k = faiss_store.as_retriever(
        search_kwargs={"k": max_results}  # dict: 动态设置返回数量
    )
    docs = retriever_k.invoke(query)      # List[Document]: 检索到的文档块列表
    if not docs:                          # 无结果时直接返回提示，让 Agent 感知
        return "未找到相关文档。"           # str: 空结果提示
    return "\n\n".join(doc.page_content for doc in docs)  # str: 所有文档内容拼接

# StructuredTool.from_function(func, name, description, args_schema) -> StructuredTool
#   func       : Callable        — 工具实际执行函数
#   name       : str             — 工具名称（LLM 用于识别调用哪个工具；建议用下划线命名）
#   description: str             — 工具功能描述（LLM 据此决策何时调用；描述越清晰效果越好）
#   args_schema: Type[BaseModel] — Pydantic schema 类，定义参数名称/类型/描述
#                                  【非严格必须】：省略时 LangChain 会从函数类型注解自动推断 schema，
#                                  但推断结果不含 Field(description=...) 中的语义说明，
#                                  LLM 将无法得知每个参数的用途，导致调用准确率下降；
#                                  显式指定可确保 LLM 获得完整的参数描述，强烈建议保留
#   返回值      : StructuredTool  — 可直接传入 Agent 的工具对象
structured_search_tool = StructuredTool.from_function(
    func=search_knowledge_base,           # Callable: 工具执行函数
    name="search_knowledge_base",         # str: 工具名称（避免空格，LLM 按此名调用）
    description="在 LangChain 知识库中检索相关文档，支持自定义返回数量。当需要查询 LangChain 框架相关知识时使用此工具。",  # str: 工具功能说明，影响 LLM 调用时机
    args_schema=KnowledgeSearchInput,     # Type[BaseModel]: 参数 schema，自动生成 JSON Schema
)

# 打印工具信息验证配置
print(f"工具名称: {structured_search_tool.name}")          # str: 工具名称
print(f"工具描述: {structured_search_tool.description}")   # str: 工具描述
print(f"参数 Schema: {structured_search_tool.args}")       # dict: JSON Schema 格式的参数定义
# {'query': {'description': '...', 'title': 'Query', 'type': 'string'}, ...}

# 直接调用工具验证功能（不经过 Agent）
# 【为何传字典而非直接传参数】：
#   StructuredTool 继承自 LangChain Runnable 接口，invoke() 统一接收字典输入，
#   内部流程为：① Pydantic 验证字典字段类型 → ② func(**validated_dict) 解包传给函数，
#   等价于 search_knowledge_base(query="RAG 检索流程", max_results=2)。
#   设计为字典的目的是让所有 Runnable 组件（LLM/Chain/Tool）保持统一接口，
#   上一组件的输出字典可直接作为下一组件的输入，无需手动拆包。
# 【为何不需要 Agent】：
#   Agent 的职责是让 LLM 决策"调用哪个工具、传什么参数"；
#   此处已明确知道要调用什么、传什么，可绕过 Agent 直接调用，
#   常用于单独测试工具逻辑是否正确，与 Agent 无关。
direct_result = structured_search_tool.invoke({
    "query": "RAG 检索流程",  # str: 查询词
    "max_results": 2,          # int: 返回 2 条结果
})
print(direct_result[:200])  # str: 直接调用的检索结果（前 200 字符）

# ============================================================
# 多工具注册：一个 StructuredTool 实例只能绑定一个函数
# 需要多个工具时，分别创建实例后组成列表传给 Agent
# ============================================================

# 示例：再定义一个摘要工具的 schema 和函数
class SummarizeInput(BaseModel):
    """摘要工具的输入参数 schema。"""
    text: str = Field(description="需要摘要的原始文本内容")
    # text: str（必填）— 待摘要的文本
    max_length: Optional[int] = Field(default=200, description="摘要最大字符数，默认 200")
    # max_length: int（可选）— 控制摘要输出长度上限

def summarize_text(text: str, max_length: int = 200) -> str:
    """对输入文本生成简短摘要（示例实现，实际替换为真实摘要逻辑）。"""
    return text[:max_length] + "..."  # str: 截断模拟摘要，实际场景调用摘要模型

# 每个 StructuredTool 实例只绑定一个函数
# StructuredTool 不支持在单个实例内注册多个函数；
# 多工具场景下，逐一创建实例后组成 tools 列表即可
summarize_tool = StructuredTool.from_function(
    func=summarize_text,                  # Callable: 绑定摘要函数（一个实例只能绑定一个）
    name="summarize_text",                # str: 工具名称
    description="对给定文本生成简短摘要，适合内容过长需要压缩时调用。",  # str: 工具描述
    args_schema=SummarizeInput,           # Type[BaseModel]: 参数 schema
)

# 将多个 StructuredTool 实例组成列表，统一传给 Agent
# Agent 会根据工具 name + description 自动决定调用哪个
tools = [structured_search_tool, summarize_tool]  # List[StructuredTool]: 多工具列表

# ============================================================
# 将工具与 LLM 绑定：StructuredTool 本身无 llm 参数
# llm 在 Agent 构建层传入，Tool 只负责"怎么执行"，Agent 负责"何时调用/调用哪个"
# 完整调用链：LLM 决策 → Agent 触发 tool.invoke() → 函数执行 → 返回结果给 LLM
# 使用 9.3 介绍的新式 create_agent API（底层由 LangGraph 驱动，推荐写法）
# ============================================================
from langchain.agents import create_agent  # create_agent: v1.x 推荐的新式 Agent 工厂

# create_agent(model, tools, system_prompt) -> CompiledGraph
#   model        : str            — 模型标识字符串（须支持 Tool Calling，如 openai:gpt-4o-mini）
#                                   内部调用 init_chat_model() 初始化模型，
#                                   base_url / api_key 自动从环境变量读取
#                                   （即第 2 节设置的 OPENAI_BASE_URL / OPENAI_API_KEY），
#                                   无需在此处手动传入；
#                                   若需要显式指定 base_url，可改为先创建 ChatOpenAI 实例
#                                   再传入：model=ChatOpenAI(model="gpt-4o-mini", base_url="...")
#   tools        : List[BaseTool] — 工具列表，Agent 内部将 LLM 与所有工具绑定
#   system_prompt: str            — 系统提示词，指导 Agent 的行为风格和调用策略
#   返回值        : CompiledGraph  — 底层由 LangGraph 驱动的 Agent 实例，直接调用 .invoke() 即可，无需额外 AgentExecutor 包装
agent = create_agent(
    model="openai:gpt-4o-mini",                        # str: 模型标识（须支持 Tool Calling）
    tools=tools,                                        # List[StructuredTool]: 多工具列表
    system_prompt="你是一位知识助手，能检索文档并生成摘要。请优先使用工具获取准确信息，再综合回答。",
    # str: 系统提示词，告知 Agent 何时使用工具
)

# agent.invoke(input_dict) -> Dict[str, Any]
#   input_dict: {"messages": List[dict]} — 包含用户消息的输入字典
#   返回值     : dict — 含 "messages" 字段，最后一条消息为 Agent 最终回答
result = agent.invoke({
    "messages": [{"role": "user", "content": "帮我检索 RAG 流程并给出摘要"}]
    # dict: role="user" 表示用户消息，content 为消息内容
})
final_msg = result["messages"][-1]  # BaseMessage: 取最后一条消息即 Agent 最终回答
print(final_msg.content)            # str: Agent 经过工具调用后给出的最终回答
```

### 9.5 内置工具（Built-in Tools）

#### 9.5.1 DuckDuckGoSearchRun — 无需 Key 的网络搜索工具

```python
# ============================================================
# DuckDuckGoSearchRun — 无需 API Key 的网络实时搜索工具
# 安装依赖：pip install duckduckgo-search
# 适合：快速为 Agent 添加实时网络搜索能力，无需注册任何账号
# ============================================================
from langchain_community.tools import DuckDuckGoSearchRun  # DuckDuckGo 搜索工具封装

# DuckDuckGoSearchRun() -> DuckDuckGoSearchRun
#   无需参数，底层使用 duckduckgo_search 库调用 DuckDuckGo 搜索 API（无需 Key）
#   返回值: DuckDuckGoSearchRun — 继承自 BaseTool 的工具实例，具备以下属性和方法：
#     .name       : str — 工具唯一标识符，固定为 "duckduckgo_search"（库内部预设，无需手动传入）
#                         LLM 通过 Tool Calling API 调用工具时按此名指定目标工具
#     .description: str — 工具功能说明（库内部预设），LLM 在决策阶段读取此文本，
#                         判断当前问题是否需要调用该工具；描述越准确，调用时机越正确
#     .invoke(query): str — 执行搜索并返回结果文本（详见下方注释）
#   注意：.name 和 .description 是所有 LangChain Tool 继承自 BaseTool 的通用属性；
#         内置工具由库预设这两个值，自定义工具（@tool / StructuredTool）则由开发者定义
ddg_search = DuckDuckGoSearchRun()  # DuckDuckGoSearchRun: 搜索工具实例

# ddg_search.invoke(query) -> str
#   query  : str — 搜索查询词（支持自然语言描述）
#   返回值  : str — 搜索结果文本（包含多条结果的标题、摘要和链接拼接）
result = ddg_search.invoke("LangChain v1.0 最新特性")  # str: 搜索结果文本
print(result[:400])  # str: 打印前 400 字符预览

print(f"工具名称: {ddg_search.name}")             # str: "duckduckgo_search"
print(f"工具描述: {ddg_search.description[:60]}") # str: 工具功能说明（前 60 字符）
```

#### 9.5.2 WikipediaQueryRun — Wikipedia 知识查询工具

```python
# ============================================================
# WikipediaQueryRun — Wikipedia 百科全书知识查询工具
# 安装依赖：pip install wikipedia
# 适合：为 Agent 添加结构化百科知识查询，补充 LLM 知识截止日期后的信息
# ============================================================
from langchain_community.tools import WikipediaQueryRun        # Wikipedia 工具封装
from langchain_community.utilities import WikipediaAPIWrapper  # Wikipedia API 底层封装

# WikipediaAPIWrapper — 底层 API 封装，控制语言、数量和字符数限制
wrapper = WikipediaAPIWrapper(
    lang="zh",               # str: 查询语言，"zh" 为中文维基百科，"en" 为英文（默认）
    top_k_results=2,         # int: 返回最相关的 2 篇文章摘要（数量越多 Token 越多）
    doc_content_chars_max=1000,  # int: 每篇文章最多截取的字符数，控制 Token 消耗
)

# WikipediaQueryRun(api_wrapper) -> WikipediaQueryRun
#   api_wrapper: WikipediaAPIWrapper — 底层 API 封装实例
#   返回值      : WikipediaQueryRun  — 可直接传入 Agent 的工具对象
wiki_tool = WikipediaQueryRun(api_wrapper=wrapper)  # WikipediaQueryRun: Wikipedia 查询工具

# wiki_tool.invoke(query) -> str
#   query  : str — 查询关键词（词条名称或自然语言描述）
#   返回值  : str — Wikipedia 文章摘要文本（中文版 top_k_results 篇）
wiki_result = wiki_tool.invoke("大型语言模型")  # str: 维基百科摘要文本
print(wiki_result[:400])  # str: 打印前 400 字符预览

print(f"工具名称: {wiki_tool.name}")             # str: "wikipedia"
print(f"工具描述: {wiki_tool.description[:80]}") # str: 工具功能说明（前 80 字符），LLM 据此判断何时调用
print(f"工具参数: {wiki_tool.args}")             # dict: 工具输入参数的 JSON Schema，LLM 调用时按此填参
# wiki_tool.args 的典型输出结构（dict）：
# {
#   "query": {                         ← 参数名，LLM 调用时传入形如 {"query": "LangChain"}
#     "description": "...",            ← 参数语义说明，告诉 LLM 该字段应填什么内容
#     "title": "Query",                ← 显示标题（UI / 文档展示用，不影响模型调用）
#     "type": "string"                 ← 参数类型，LLM 据此确认应传字符串而非数字或列表
#   }
# }
# WikipediaQueryRun 底层基于 Pydantic 模型定义参数，因此 args 中包含完整的
# description 语义说明（不同于 @tool 只有 type 而无 description 的情况）；
# LLM 通过 description 精准理解「query 应填词条名称或搜索关键词」，调用准确率更高。
```

#### 9.5.3 混合工具列表 — 内置工具与自定义工具传入 Agent

```python
# ============================================================
# 内置工具和自定义工具在 Agent 中用法完全相同
# 统一传入 tools 列表，Agent 自动根据 description 决策调用时机
# ============================================================
from langchain.agents import create_agent    # create_agent: v1.x 推荐 Agent 工厂
from langchain_community.tools import DuckDuckGoSearchRun  # 内置搜索工具
from langchain_community.tools import WikipediaQueryRun    # 内置 Wikipedia 工具
from langchain_community.utilities import WikipediaAPIWrapper  # Wikipedia API 封装
from langchain_core.tools import tool                      # @tool: 自定义工具装饰器

# 自定义工具：获取当前北京时间
@tool
def get_beijing_time() -> str:
    """
    获取当前北京时间（UTC+8）。无需参数。

    Returns:
        str — 格式化的当前时间字符串，如 "2026-07-25 13:38:00"
    """
    from datetime import datetime, timezone, timedelta
    tz = timezone(timedelta(hours=8))  # timezone: UTC+8 北京时区
    now = datetime.now(tz)             # datetime: 当前北京时间
    return now.strftime("%Y-%m-%d %H:%M:%S")  # str: 格式化时间字符串

# 混合工具列表：内置工具实例 + @tool 自定义函数
mixed_tools = [
    DuckDuckGoSearchRun(),                                         # 内置：网络实时搜索
    WikipediaQueryRun(api_wrapper=WikipediaAPIWrapper(lang="zh")), # 内置：中文维基百科
    get_beijing_time,                                              # 自定义：北京时间查询
]

# create_agent 与混合工具列表的用法和纯自定义工具完全一致
mixed_agent = create_agent(
    model="openai:gpt-4o-mini",    # str: 模型标识（须支持 Tool Calling）
    tools=mixed_tools,             # List[BaseTool]: 内置 + 自定义混合工具列表
    system_prompt="你是一位实时信息助手，能搜索网络、查询维基百科和获取当前时间。请优先使用工具获取最新/准确信息，再综合回答。",  # str: 系统提示词
)

# mixed_agent.invoke(input_dict) -> Dict[str, Any]
#   input_dict: {"messages": List[dict]} — LangGraph 状态输入（invoke 接口详见 9.3 节）
#   返回值     : Dict — 含 "messages" 字段，result["messages"][-1] 为 Agent 最终回答
result = mixed_agent.invoke({
    "messages": [
        {"role": "user", "content": "现在北京几点？同时查一下「检索增强生成」的维基百科介绍。"}
        # dict: role="user" 表示用户消息，content 为消息正文
    ]
})
final_msg = result["messages"][-1]  # BaseMessage(AIMessage): 最后一条消息即 Agent 最终回答
print(final_msg.content)            # str: Agent 整合工具结果后的综合回答
```

#### 9.5.4 SQLDatabaseToolkit — SQL 数据库工具集

```python
# ============================================================
# SQLDatabaseToolkit — LangChain 内置的 SQL 数据库工具集
# 安装依赖：pip install langchain-community sqlalchemy
# 适合：让 Agent 自主完成「查表结构 → 生成 SQL → 执行查询 → 解读结果」全流程
# SQLDatabaseToolkit 与单个 Tool 的核心区别：
#   单个 Tool  : 只暴露一种能力（如只能执行 SQL）
#   Toolkit    : 一次性暴露多个协作工具，Agent 自主编排调用顺序，
#                无需开发者手动拼 SQL 或管理多步骤逻辑
# ============================================================
from langchain_community.utilities import SQLDatabase              # SQLDatabase: 数据库连接封装
from langchain_community.agent_toolkits import SQLDatabaseToolkit  # SQLDatabaseToolkit: SQL 工具集
from langchain_openai import ChatOpenAI                            # ChatOpenAI: OpenAI 模型
from langchain import hub                                          # hub: 从 LangChain Hub 拉取提示词
from langchain.agents import create_react_agent, AgentExecutor    # Agent 工厂和执行器

# ── Step 1：创建数据库连接 ────────────────────────────────────
# SQLDatabase.from_uri(database_uri) -> SQLDatabase
#   database_uri: str — SQLAlchemy 标准连接字符串，格式如下：
#     SQLite  : "sqlite:///./mydb.db"       本地文件（相对路径）
#     SQLite  : "sqlite:///:memory:"        内存数据库（测试用）
#     MySQL   : "mysql+pymysql://user:pwd@host:3306/dbname"
#     PostgreSQL: "postgresql://user:pwd@host:5432/dbname"
#   返回值: SQLDatabase — 封装了 SQLAlchemy engine 的数据库对象，
#           提供 .run(sql) 执行查询、.get_table_info() 获取表结构等方法
db = SQLDatabase.from_uri("sqlite:///./demo_employees.db")  # SQLDatabase: 连接本地 SQLite 文件

# 也可在连接时限制 Agent 只能访问指定表（安全隔离）：
# db = SQLDatabase.from_uri(
#     "sqlite:///./demo_employees.db",
#     include_tables=["employees", "departments"],  # List[str]: 白名单表，未列出的表 Agent 不可见
# )

# ── Step 2：准备演示数据（首次运行时建表并插入示例数据）────────
import sqlite3  # sqlite3: Python 内置 SQLite 驱动

conn = sqlite3.connect("demo_employees.db")  # Connection: SQLite 数据库连接
cursor = conn.cursor()                        # Cursor: 用于执行 SQL 语句

# 建表：employees（员工表）
cursor.execute("""
    CREATE TABLE IF NOT EXISTS employees (
        id         INTEGER PRIMARY KEY,  -- 员工 ID（主键）
        name       TEXT NOT NULL,        -- 员工姓名
        department TEXT NOT NULL,        -- 所属部门
        salary     REAL NOT NULL,        -- 月薪（元）
        hire_date  TEXT NOT NULL         -- 入职日期（YYYY-MM-DD）
    )
""")

# 建表：departments（部门表）
cursor.execute("""
    CREATE TABLE IF NOT EXISTS departments (
        id         INTEGER PRIMARY KEY,  -- 部门 ID（主键）
        name       TEXT NOT NULL,        -- 部门名称
        manager    TEXT NOT NULL,        -- 部门负责人
        budget     REAL NOT NULL         -- 年度预算（元）
    )
""")

# 插入示例数据（仅首次）
cursor.execute("SELECT COUNT(*) FROM employees")  # 查询现有行数，避免重复插入
if cursor.fetchone()[0] == 0:                      # int: 当前表中的行数
    cursor.executemany(
        "INSERT INTO employees VALUES (?,?,?,?,?)",  # 参数化 SQL，防止注入
        [
            (1, "张伟",   "研发部", 18000, "2021-03-15"),
            (2, "李娜",   "市场部", 15000, "2020-07-01"),
            (3, "王强",   "研发部", 22000, "2019-11-20"),
            (4, "赵敏",   "人事部", 12000, "2022-05-10"),
            (5, "陈浩",   "研发部", 25000, "2018-01-08"),
            (6, "刘芳",   "市场部", 14000, "2023-02-28"),
            (7, "杨洋",   "财务部", 13000, "2021-09-03"),
            (8, "周佳",   "研发部", 20000, "2020-04-17"),
        ]
    )
    cursor.executemany(
        "INSERT INTO departments VALUES (?,?,?,?)",
        [
            (1, "研发部", "陈浩",  5000000),
            (2, "市场部", "李娜",  2000000),
            (3, "人事部", "赵敏",  800000),
            (4, "财务部", "杨洋",  600000),
        ]
    )
    conn.commit()  # 提交事务，将数据持久化到磁盘

conn.close()  # 关闭连接，释放资源

# ── Step 3：创建 SQLDatabaseToolkit ────────────────────────────
# SQLDatabaseToolkit(db, llm) -> SQLDatabaseToolkit
#   db : SQLDatabase    — 已连接的数据库对象
#   llm: BaseChatModel  — 用于 sql_db_query_checker 工具内部校验 SQL 语法的模型
#   返回值: SQLDatabaseToolkit — 包含 4 个协作工具的工具集对象（详见下方说明）
sql_llm = ChatOpenAI(model="gpt-4o-mini", temperature=0)  # ChatOpenAI: 工具集内部使用的模型
toolkit = SQLDatabaseToolkit(db=db, llm=sql_llm)          # SQLDatabaseToolkit: SQL 工具集实例

# toolkit.get_tools() -> List[BaseTool]
#   返回值: List[BaseTool] — Toolkit 内包含的 4 个工具，说明如下：
#
#   ┌─────────────────────────┬────────────────────────────────────────────────────────┐
#   │ 工具名称                  │ 功能说明                                               │
#   ├─────────────────────────┼────────────────────────────────────────────────────────┤
#   │ sql_db_list_tables      │ 列出数据库中所有可访问的表名（Agent 探索入口）              │
#   │ sql_db_schema           │ 查询指定表的 CREATE TABLE 语句 + 示例数据（理解表结构）    │
#   │ sql_db_query_checker    │ 将 Agent 生成的 SQL 发给 LLM 做语法校验，返回修正后的 SQL  │
#   │ sql_db_query            │ 执行合法 SQL 并返回查询结果（字符串格式）                  │
#   └─────────────────────────┴────────────────────────────────────────────────────────┘
#
#   Agent 典型调用顺序（ReAct 循环）：
#     1. sql_db_list_tables  → 了解有哪些表
#     2. sql_db_schema       → 查看相关表的字段和示例行
#     3. sql_db_query_checker→ 生成 SQL 后先校验语法
#     4. sql_db_query        → 执行 SQL 获取数据
sql_tools = toolkit.get_tools()  # List[BaseTool]: Toolkit 提供的 4 个工具列表

# 打印工具信息，验证工具集内容
for t in sql_tools:
    print(f"  • {t.name:<25} {t.description[:60]}...")  # str: 工具名 + 功能描述预览

# ── Step 4：构建 SQL Agent ─────────────────────────────────────
# 使用 LangChain Hub 上专为 SQL ReAct Agent 设计的提示词模板
# hub.pull(repo_id) -> ChatPromptTemplate
#   repo_id : str — Hub 上的提示词仓库 ID
#   返回值   : ChatPromptTemplate — 已包含 {tools}/{tool_names}/{input}/{agent_scratchpad} 占位符
sql_prompt = hub.pull("hwchase17/react")  # ChatPromptTemplate: ReAct 通用提示词模板

# create_react_agent(llm, tools, prompt) -> Runnable
#   llm    : BaseChatModel      — 推理模型（须支持 Tool Calling 或可输出 ReAct 格式）
#   tools  : List[BaseTool]     — 工具列表（此处传入 Toolkit 的 4 个 SQL 工具）
#   prompt : ChatPromptTemplate — ReAct 提示词模板
#   返回值  : Runnable           — Agent 单步推理核心（不含执行循环）
sql_agent = create_react_agent(
    llm=sql_llm,          # ChatOpenAI: 推理和工具调用模型
    tools=sql_tools,      # List[BaseTool]: SQLDatabaseToolkit 提供的 4 个工具
    prompt=sql_prompt,    # ChatPromptTemplate: ReAct 格式提示词
)

# AgentExecutor(agent, tools, verbose, handle_parsing_errors) -> AgentExecutor
sql_agent_executor = AgentExecutor(
    agent=sql_agent,               # Runnable: SQL Agent 核心推理逻辑
    tools=sql_tools,               # List[BaseTool]: 与 create_react_agent 保持一致
    verbose=True,                  # bool: 打印完整的 ReAct 推理过程（Thought/Action/Observation）
    handle_parsing_errors=True,    # bool: LLM 输出格式不规范时自动重试，避免直接报错
    max_iterations=10,             # int: 最多执行 10 步，防止无限循环
)

# ── Step 5：执行查询演示 ────────────────────────────────────────
# 示例 1：跨表聚合查询（Agent 需自行探索表结构并 JOIN）
result1 = sql_agent_executor.invoke({
    "input": "研发部有几名员工？他们的平均薪资是多少？"
    # Agent 推理路径（预期）：
    #   1. sql_db_list_tables     → 发现 employees / departments 两张表
    #   2. sql_db_schema          → 查看 employees 表结构，确认 department 和 salary 字段
    #   3. sql_db_query_checker   → 校验 "SELECT COUNT(*), AVG(salary) FROM employees WHERE department='研发部'"
    #   4. sql_db_query           → 执行 SQL，返回行数和平均值
    #   5. 汇总结果，用自然语言回答
})
print(f"\n【查询 1 结果】\n{result1['output']}")  # str: Agent 最终的自然语言回答

# 示例 2：排序 + 限制行数
result2 = sql_agent_executor.invoke({
    "input": "薪资最高的前 3 名员工是谁？列出姓名、部门和薪资。"
})
print(f"\n【查询 2 结果】\n{result2['output']}")  # str: Agent 整合查询结果后的回答

# 示例 3：多表 JOIN（验证 Agent 能否自主 JOIN）
result3 = sql_agent_executor.invoke({
    "input": "各部门的平均薪资是多少？按平均薪资从高到低排列。"
})
print(f"\n【查询 3 结果】\n{result3['output']}")  # str: Agent 最终回答
```

#### 9.5.5 TavilySearchResults — 生产级实时网络搜索工具

```python
# ============================================================
# TavilySearchResults — 专为 AI Agent 设计的实时网络搜索工具
# 安装依赖：pip install langchain-community tavily-python
# 前置条件：在环境变量中配置 TAVILY_API_KEY
#   import os; os.environ["TAVILY_API_KEY"] = "tvly-xxxxxxxx"
#   或在 .env 文件中写入 TAVILY_API_KEY=tvly-xxxxxxxx
# 适合：生产环境 Agent 实时联网搜索，结果经过 AI 优化整理，
#       比 DuckDuckGo 更稳定、结构化，不易触发反爬限制
# API Key 申请：https://tavily.com（每月 1000 次免费额度）
# ============================================================
import os                                                          # os: 标准库，用于读取环境变量
from langchain_community.tools.tavily_search import TavilySearchResults  # Tavily 搜索工具封装

# ── Step 1：配置 API Key ────────────────────────────────────────
# TavilySearchResults 初始化时会自动读取环境变量 TAVILY_API_KEY
# 也可以在此处显式传入（不推荐硬编码到代码中）：
# os.environ["TAVILY_API_KEY"] = "tvly-xxxxxxxx"  # str: Tavily API 密钥

# ── Step 2：实例化工具 ──────────────────────────────────────────
# TavilySearchResults(max_results, search_depth, include_answer, ...) -> TavilySearchResults
#   max_results   : int  — 返回的最大搜索结果条数（默认 5，建议 Agent 场景设为 1~3）
#   search_depth  : str  — 搜索深度，"basic"（快速）或 "advanced"（更深入，消耗更多配额）
#   include_answer: bool — 是否在结果中包含 Tavily 生成的直接答案摘要（默认 False）
#   include_raw_content: bool — 是否返回原始网页正文（默认 False，True 时 Token 消耗大幅增加）
#   返回值: TavilySearchResults — 继承自 BaseTool 的搜索工具实例
#     .name        : str — 固定为 "tavily_search_results_json"，LLM 按此名调用工具
#     .description : str — 工具功能说明，LLM 在决策阶段读取此文本判断是否调用
#     .invoke(query): str/List[dict] — 执行搜索并返回结构化结果（详见下方）
search = TavilySearchResults(
    max_results=3,          # int: 返回最相关的 3 条搜索结果，平衡信息量与 Token 消耗
    search_depth="basic",   # str: "basic" 速度快、配额消耗少；"advanced" 结果更全面
    include_answer=True,    # bool: True 时结果中包含 Tavily 生成的直接摘要答案
)

# ── Step 3：直接调用验证 ────────────────────────────────────────
# search.invoke(query) -> List[dict]
#   query  : str — 自然语言搜索查询词
#   返回值  : List[dict] — 结构化搜索结果列表，每条结果为含以下键的字典：
#     "url"    : str — 来源网页 URL
#     "content": str — 经过 Tavily AI 整理的网页内容摘要（适合直接喂给 LLM）
#     "score"  : float — 相关性得分（0~1，越高越相关）
results = search.invoke("LangChain 最新版本特性 2026")  # List[dict]: 结构化搜索结果列表
for i, r in enumerate(results, 1):
    print(f"\n── 结果 {i} ──")
    print(f"URL    : {r['url']}")           # str: 来源网址
    print(f"内容   : {r['content'][:200]}") # str: 内容摘要（前 200 字符预览）
    print(f"相关度 : {r.get('score', 'N/A')}") # float: 相关性得分

print(f"\n工具名称: {search.name}")              # str: "tavily_search_results_json"
print(f"工具描述: {search.description[:80]}")   # str: 工具功能说明（前 80 字符）

# ── Step 4：在 Agent 中使用 Tavily ─────────────────────────────
# TavilySearchResults 实例已是 BaseTool 子类，与 DuckDuckGoSearchRun 用法完全一致，
# 直接放入 tools 列表传给 Agent 即可，无需额外包装
from langchain.agents import create_agent  # create_agent: v1.x 推荐 Agent 工厂

tools = [search]  # List[TavilySearchResults]: 工具列表，仅含 Tavily 搜索工具

# create_agent 底层基于 LangGraph 构建 CompiledGraph，接口与 9.5.3 完全一致
tavily_agent = create_agent(
    model="openai:gpt-4o-mini",  # str: 模型标识，须支持 Tool Calling
    tools=tools,                 # List[BaseTool]: 工具列表
    system_prompt=(
        "你是一位实时信息助手，能够通过网络搜索获取最新信息。"
        "当用户询问需要实时数据或近期事件时，必须先调用搜索工具，再综合结果回答。"
    ),  # str: 系统提示词，指导 Agent 何时使用搜索工具
)

# tavily_agent.invoke(input_dict) -> Dict[str, Any]
#   input_dict: {"messages": List[dict]} — LangGraph 状态输入（详见 9.5.3 注释）
#   返回值     : Dict — 含 "messages" 字段，result["messages"][-1] 为 Agent 最终回答
result = tavily_agent.invoke({
    "messages": [
        {"role": "user", "content": "2026 年 Python 最流行的 AI 框架有哪些？"}
        # dict: role="user" 表示用户消息，content 为消息正文
    ]
})
final_msg = result["messages"][-1]  # AIMessage: 最后一条消息即 Agent 最终回答
print(final_msg.content)            # str: Agent 整合 Tavily 搜索结果后的综合回答

# ── 附：DuckDuckGo vs Tavily 选型对比 ──────────────────────────
# ┌──────────────────┬──────────────────────────┬──────────────────────────────┐
# │ 对比维度          │ DuckDuckGoSearchRun       │ TavilySearchResults          │
# ├──────────────────┼──────────────────────────┼──────────────────────────────┤
# │ API Key          │ 不需要                    │ 需要（免费注册可得）           │
# │ 费用             │ 完全免费                  │ 1000 次/月免费，超出收费       │
# │ 稳定性           │ 易触发反爬，生产不推荐     │ 商业 API，稳定可靠             │
# │ 结果格式         │ 纯文本拼接                │ 结构化 List[dict]，含评分      │
# │ 结果质量         │ 一般                      │ 高，专为 AI 优化               │
# │ 推荐场景         │ 本地学习 / 原型开发        │ 生产环境 Agent                 │
# └──────────────────┴──────────────────────────┴──────────────────────────────┘
```

#### 9.5.6 create_retriever_tool — 将检索器包装为 Agent 工具

```python
# ============================================================
# create_retriever_tool — 将任意 LangChain Retriever 包装成 BaseTool
# 安装依赖：pip install langchain langchain-community faiss-cpu
# 作用：让 Agent 能够像调用普通工具一样查询向量数据库或任意检索器，
#       实现「工具调用触发 RAG 检索」的 Agent + RAG 融合模式
# 核心价值：Retriever 本身不是 BaseTool，无法直接放入 tools 列表；
#           create_retriever_tool 充当适配器，完成这一转换
# ============================================================
from langchain.tools.retriever import create_retriever_tool  # create_retriever_tool: 检索器工具工厂
from langchain_community.vectorstores import FAISS            # FAISS: 本地向量数据库
from langchain_openai import OpenAIEmbeddings                 # OpenAIEmbeddings: 文本向量化模型

# ── Step 1：构建演示用向量数据库 ────────────────────────────────
# 准备示例文档（实际场景可从文件、数据库、网页等加载）
docs = [
    "LangChain 是一个用于构建 LLM 应用的开源框架，支持链式调用、Agent 和 RAG。",
    "LangGraph 是 LangChain 生态中基于图结构的 Agent 编排框架，支持循环和条件分支。",
    "RAG（检索增强生成）通过在生成前检索相关文档，有效缓解大模型的幻觉问题。",
    "FAISS 是 Facebook 开源的高效向量相似度搜索库，常用于本地向量数据库场景。",
    "Tool Calling 允许 LLM 在推理时决策调用外部工具，是现代 Agent 的核心机制。",
]  # List[str]: 待索引的文本文档列表

# OpenAIEmbeddings() -> OpenAIEmbeddings
#   自动读取环境变量 OPENAI_API_KEY，将文本转为高维向量（1536 维）
#   返回值: OpenAIEmbeddings — 嵌入模型实例，实现 .embed_documents() 和 .embed_query()
embeddings = OpenAIEmbeddings()  # OpenAIEmbeddings: 文本向量化模型实例

# FAISS.from_texts(texts, embedding) -> FAISS
#   texts    : List[str]       — 待向量化并索引的文本列表
#   embedding: Embeddings      — 嵌入模型，用于将文本转为向量
#   返回值   : FAISS            — 已建立索引的向量数据库实例
vectorstore = FAISS.from_texts(docs, embeddings)  # FAISS: 本地向量数据库

# vectorstore.as_retriever(search_kwargs) -> VectorStoreRetriever
#   search_kwargs: dict — 检索参数，"k" 指定返回最相似的文档数量
#   返回值        : VectorStoreRetriever — 实现 .invoke(query) 接口的检索器对象
#                   注意：VectorStoreRetriever 继承自 BaseRetriever，不是 BaseTool，
#                   因此不能直接放入 Agent 的 tools 列表，需要 create_retriever_tool 包装
retriever = vectorstore.as_retriever(
    search_kwargs={"k": 2}  # dict: k=2 表示每次检索返回最相似的 2 篇文档
)  # VectorStoreRetriever: 向量检索器实例

# ── Step 2：将 Retriever 包装为 Tool ───────────────────────────
# create_retriever_tool(retriever, name, description) -> Tool
#   retriever  : BaseRetriever — 任意 LangChain 检索器实例（FAISS、Chroma、Pinecone 等均可）
#   name       : str           — 工具唯一标识符，LLM 按此名决策何时调用该工具
#                                命名建议：清晰描述数据源，如 "search_langchain_docs"
#   description: str           — 工具功能说明，LLM 读取此文本决定是否调用；
#                                描述越精准（说明数据源内容、适用查询类型），调用时机越准确
#   返回值     : Tool           — 继承自 BaseTool 的工具实例，可直接放入 Agent tools 列表
#                                内部的 .invoke(query) 会调用 retriever.invoke(query)，
#                                并将检索到的文档拼接为字符串返回给 LLM
retriever_tool = create_retriever_tool(
    retriever=retriever,  # VectorStoreRetriever: 向量检索器
    name="search_langchain_knowledge",  # str: 工具名，LLM 调用时按此名指定
    description=(
        "搜索 LangChain 相关知识库。"
        "当用户询问 LangChain、LangGraph、RAG、Tool Calling、FAISS 等 AI 框架和技术时，"
        "必须调用此工具检索相关文档后再回答，不要凭记忆直接作答。"
    ),  # str: 详细描述数据源内容和适用场景，引导 LLM 在正确时机调用
)  # Tool: 包装后的检索器工具，已是 BaseTool 子类

# 验证工具属性
print(f"工具名称: {retriever_tool.name}")              # str: "search_langchain_knowledge"
print(f"工具描述: {retriever_tool.description[:80]}")  # str: 工具描述（前 80 字符）
print(f"工具类型: {type(retriever_tool)}")             # type: <class 'langchain_core.tools.retriever.RetrieverInput'>

# ── Step 3：直接调用验证检索效果 ────────────────────────────────
# retriever_tool.invoke(query) -> str
#   query  : str — 自然语言查询词
#   返回值  : str — 检索到的文档内容拼接成的字符串（直接传给 LLM 作为上下文）
#
# 【注意】返回 str 而非 list[Document] 的原因：
#   retriever_tool 是经 create_retriever_tool 包装后的工具对象，与原始检索器行为不同：
#     - retriever.invoke(query)       -> list[Document]  # 原始检索器，返回文档列表，可访问 metadata 等
#     - retriever_tool.invoke(query)  -> str             # 工具对象，内部先调用检索器得到 list[Document]，
#                                                        # 再将每个 Document.page_content 拼接成纯文本字符串
#   这样设计的目的是：Tool 的返回值会作为工具调用结果传给 LLM，
#   LLM 只能理解纯文本，无法直接处理 Document 对象，因此工具层做了这层自动转换。
retrieved_text = retriever_tool.invoke("什么是 RAG？")  # str: 检索结果拼接文本（各文档 page_content 拼接）
print(f"\n检索结果:\n{retrieved_text}")  # str: 打印检索到的相关文档内容

# ── Step 4：集成到 Agent ────────────────────────────────────────
# retriever_tool 与其他 BaseTool 用法完全一致，直接放入 tools 列表
from langchain.agents import create_agent  # create_agent: v1.x 推荐 Agent 工厂

rag_agent = create_agent(
    model="openai:gpt-4o-mini",  # str: 模型标识，须支持 Tool Calling
    tools=[retriever_tool],      # List[Tool]: 仅含检索器工具的列表
    system_prompt=(
        "你是一位 LangChain 技术助手。回答问题前，必须先调用知识库搜索工具检索相关文档，"
        "再基于检索结果给出准确回答，不要凭空捏造。"
    ),  # str: 强制 Agent 先检索再回答，防止幻觉
)  # CompiledGraph: 基于 LangGraph 构建的 Agent 状态图

# rag_agent.invoke(input_dict) -> Dict[str, Any]
#   input_dict: {"messages": List[dict]} — LangGraph 状态输入
#   返回值     : Dict — result["messages"][-1] 为 Agent 最终回答（AIMessage）
#
# 【Agent 内部完整消息流转过程】
# messages 列表在整个推理过程中持续追加（不覆盖），完整链路如下：
#
#   [0] HumanMessage:  "LangGraph 和 LangChain 有什么区别？"
#         ↓  LLM 判断需要检索，生成工具调用指令
#   [1] AIMessage:     content=""
#                      tool_calls=[{name:"search_langchain_knowledge", args:{"query":"..."}}]
#         ↓  框架执行 retriever_tool.invoke(query) -> str
#   [2] ToolMessage:   content="<检索到的各文档 page_content 拼接字符串>"  ← 工具返回的 str 存于此
#         ↓  LLM 拿到完整 messages（含工具结果）综合生成回答
#   [3] AIMessage:     content="LangGraph 是... LangChain 是..."  ← 最终回答
#
#   result["messages"][-1]  即 [3] 这条 AIMessage
#
# 【与旧版 AgentExecutor scratchpad 的区别】
#   旧版 AgentExecutor：中间结果以字符串形式拼接到 scratchpad，对外隐藏，多轮工具调用顺序拼入 prompt
#   LangGraph Agent  ：中间结果以 ToolMessage 对象追加到 messages 列表，全程可查，每轮工具调用有独立 ToolMessage
result = rag_agent.invoke({
    "messages": [
        {"role": "user", "content": "LangGraph 和 LangChain 有什么区别？"}
        # dict: 用户提问，Agent 将调用 retriever_tool 检索知识库后作答
    ]
})
final_msg = result["messages"][-1]  # AIMessage: Agent 最终回答
print(final_msg.content)            # str: 基于知识库检索结果的综合回答

# ── 附：create_retriever_tool 与 @tool 自定义检索工具对比 ────────
# ┌──────────────────────┬──────────────────────────────┬────────────────────────────────┐
# │ 对比维度              │ create_retriever_tool         │ @tool 自定义检索函数             │
# ├──────────────────────┼──────────────────────────────┼────────────────────────────────┤
# │ 代码量               │ 1 行，开箱即用                 │ 需手动写函数体 + 调用 retriever  │
# │ 适用检索器            │ 任意 BaseRetriever 子类        │ 任意（灵活性更高）               │
# │ 返回值格式            │ 自动拼接为字符串               │ 自定义（可返回结构化数据）         │
# │ 推荐场景              │ 标准 RAG 检索，快速集成        │ 需要对检索结果做后处理时           │
# └──────────────────────┴──────────────────────────────┴────────────────────────────────┘
```

---

### 9.6 MCP（Model Context Protocol）工具接入

#### 9.6.1 MCP 概念与架构

MCP（**Model Context Protocol**）是 Anthropic 于 2024 年提出、目前已成为行业标准的开放协议，  
用于统一 **LLM Agent ↔ 外部工具/数据源** 之间的通信格式。  
主要角色：


| 角色             | 说明                                                |
| -------------- | ------------------------------------------------- |
| **MCP Server** | 提供工具、资源或提示词的服务进程（可本地 stdio 或远程 HTTP/SSE）          |
| **MCP Client** | 连接 MCP Server、获取工具列表并代理 Agent 发起调用的客户端            |
| **Host**       | 宿主应用（如 Cursor / Claude Desktop），管理多个 Client 的生命周期 |


LangChain 通过 `langchain-mcp-adapters` 包将 MCP 工具无缝转换为 `BaseTool`，  
可直接传入任何 LangChain Agent，无需改动已有 Agent 代码。

#### 9.6.2 安装依赖

```python
# ============================================================
# 安装 MCP 集成所需的额外依赖
# ------------------------------------------------------------
# langchain-mcp-adapters : LangChain 官方 MCP 适配器，
#                          将 MCP 工具转换为 LangChain BaseTool
# mcp                    : MCP Python SDK（提供 stdio/sse 传输层）
# ============================================================
# pip install langchain-mcp-adapters mcp
```

#### 9.6.3 连接本地 stdio MCP Server

```python
# ============================================================
# 场景：通过 stdio 传输连接本地 MCP Server（最常见方式）
# MCP Server 以子进程形式启动，通过标准输入/输出与 Client 通信
#
# 以官方示例 math_server.py（提供 add / multiply 两个工具）为例：
#   from mcp.server.fastmcp import FastMCP
#   mcp = FastMCP("Math")
#   @mcp.tool()
#   def add(a: int, b: int) -> int:
#       """两数相加"""
#       return a + b
#   @mcp.tool()
#   def multiply(a: int, b: int) -> int:
#       """两数相乘"""
#       return a * b
#   if __name__ == "__main__":
#       mcp.run(transport="stdio")
# ============================================================
import asyncio                                         # asyncio: Python 异步 I/O 标准库
from langchain_mcp_adapters.client import MultiServerMCPClient  # MultiServerMCPClient: 多 MCP Server 管理客户端
from langchain.agents import create_agent              # create_agent: LangChain v1.x Agent 工厂

async def run_math_agent():
    """
    使用 stdio MCP Server 提供的数学工具运行 Agent。

    Returns:
        str — Agent 最终回答的文本内容
    """
    # MultiServerMCPClient(servers_config) -> AsyncContextManager
    #   servers_config: dict — Server 名称 → 连接配置的映射字典
    #   连接配置字段：
    #     transport  : str  — 传输方式，"stdio" 表示子进程标准流传输
    #     command    : str  — 启动 Server 的可执行文件（如 "python"）
    #     args       : list — 命令行参数列表（如脚本路径）
    #     env        : dict — 传递给子进程的额外环境变量（可选）
    async with MultiServerMCPClient(
        {
            "math": {
                "transport": "stdio",          # str: 使用 stdio 传输（子进程方式）
                "command":   "python",         # str: 启动子进程的命令
                "args":      ["math_server.py"],  # List[str]: 传递给命令的参数
            }
        }
    ) as client:
        # client.get_tools() -> List[BaseTool]
        #   返回值: List[BaseTool] — 所有已连接 MCP Server 暴露的工具列表，
        #           每个工具已自动封装为 LangChain BaseTool，可直接传给 Agent
        tools = client.get_tools()
        print(f"发现工具数量: {len(tools)}")            # int: 工具总数
        print(f"工具名称列表: {[t.name for t in tools]}")  # List[str]: 工具名称

        # create_agent 用法与普通工具完全一致
        agent = create_agent(
            model="openai:gpt-4o-mini",  # str: 模型标识（须支持 Tool Calling）
            tools=tools,                 # List[BaseTool]: MCP 工具列表
            system_prompt="你是一位数学助手，使用提供的工具完成计算任务。",  # str: 系统提示词
        )

        # agent.ainvoke(input_dict) -> Dict[str, Any]
        #   input_dict: {"messages": List[dict]} — 包含用户消息的输入字典
        #   返回值     : Dict — 含 "messages" 字段，最后一条消息为最终回答
        result = await agent.ainvoke(
            {"messages": [{"role": "user", "content": "3 加 5 等于多少？再把结果乘以 2。"}]}
        )
        final_answer = result["messages"][-1].content  # str: Agent 最终回答
        print(f"Agent 回答: {final_answer}")            # str: 打印最终回答
        return final_answer                             # str: 返回最终回答字符串

# asyncio.run() 在脚本中运行异步入口函数
asyncio.run(run_math_agent())
```

#### 9.6.4 连接远程 SSE MCP Server

```python
# ============================================================
# 场景：通过 HTTP + SSE（Server-Sent Events）连接远程 MCP Server
# 适合跨机器部署、云端服务、无需本地安装 Server 依赖的场景
# ============================================================
import asyncio                                         # asyncio: 异步标准库
from langchain_mcp_adapters.client import MultiServerMCPClient  # MultiServerMCPClient: 多 Server 管理客户端
from langchain.agents import create_agent              # create_agent: Agent 工厂

async def run_remote_agent():
    """
    连接远程 SSE MCP Server 并运行 Agent。

    Returns:
        str — Agent 最终回答文本
    """
    # SSE 连接配置
    #   transport : "sse"     — 使用 HTTP Server-Sent Events 传输
    #   url       : str       — MCP Server 的 SSE 端点 URL
    #   headers   : dict      — 附加 HTTP 请求头（如 Authorization，可选）
    #   timeout   : float     — 连接超时秒数（默认 5.0，可选）
    async with MultiServerMCPClient(
        {
            "remote_tools": {
                "transport": "sse",                           # str: SSE 传输方式
                "url":       "http://localhost:8000/sse",     # str: Server SSE 端点
                "headers":   {"Authorization": "Bearer TOKEN"},  # dict: 鉴权头（可选）
                "timeout":   10.0,                            # float: 连接超时 10 秒
            }
        }
    ) as client:
        tools = client.get_tools()                        # List[BaseTool]: 远程工具列表
        print(f"远程工具: {[t.name for t in tools]}")    # List[str]: 工具名称列表

        agent = create_agent(
            model="openai:gpt-4o-mini",  # str: 模型标识
            tools=tools,                 # List[BaseTool]: 远程 MCP 工具
            system_prompt="你是一位智能助手，可调用远程服务完成任务。",  # str: 系统提示词
        )
        result = await agent.ainvoke(
            {"messages": [{"role": "user", "content": "请帮我完成一项任务。"}]}
        )
        return result["messages"][-1].content  # str: 最终回答

asyncio.run(run_remote_agent())
```

#### 9.6.5 同时连接多个 MCP Server

```python
# ============================================================
# 场景：将多个 MCP Server 的工具汇聚到同一个 Agent
# MultiServerMCPClient 自动合并所有 Server 暴露的工具列表
# ============================================================
import asyncio                                         # asyncio: 异步标准库
from langchain_mcp_adapters.client import MultiServerMCPClient  # MultiServerMCPClient: 多 Server 管理
from langchain.agents import create_agent              # create_agent: Agent 工厂

async def run_multi_server_agent():
    """
    同时连接数学工具（stdio）和天气工具（SSE）两个 MCP Server，
    构建可跨服务调用的综合 Agent。

    Returns:
        str — Agent 最终回答文本
    """
    # servers_config 中可同时配置任意数量的 Server，传输方式可混用
    async with MultiServerMCPClient(
        {
            "math": {
                "transport": "stdio",              # str: 本地子进程传输
                "command":   "python",             # str: 启动命令
                "args":      ["math_server.py"],   # List[str]: 命令行参数
            },
            "weather": {
                "transport": "sse",                          # str: 远程 SSE 传输
                "url":       "http://weather-api.example.com/mcp/sse",  # str: 远程 URL
            },
        }
    ) as client:
        # get_tools() 自动合并所有 Server 的工具，以 "server_name__tool_name" 格式去重
        all_tools = client.get_tools()                         # List[BaseTool]: 全部工具
        print(f"合并后工具总数: {len(all_tools)}")             # int: 工具总数
        for t in all_tools:
            print(f"  - {t.name}: {t.description[:50]}...")   # str: 工具名和描述摘要

        agent = create_agent(
            model="openai:gpt-4o-mini",  # str: 模型标识
            tools=all_tools,             # List[BaseTool]: 跨 Server 合并的工具列表
            system_prompt=(              # str: 强调 Agent 可调用多来源工具
                "你是一位全能助手，可以调用数学计算和天气查询工具。"
                "根据用户问题自动选择合适的工具完成任务。"
            ),
        )
        result = await agent.ainvoke({
            "messages": [
                {
                    "role":    "user",
                    "content": "今天北京天气怎么样？顺便算一下 12 乘以 8 等于多少？"
                }
            ]
        })
        final_answer = result["messages"][-1].content  # str: Agent 综合两个工具的最终回答
        print(f"Agent 回答: {final_answer}")            # str: 打印结果
        return final_answer                             # str: 返回最终回答

asyncio.run(run_multi_server_agent())
```

#### 9.6.6 在 Jupyter Notebook 中使用 MCP（nest_asyncio）

```python
# ============================================================
# Jupyter Notebook 中已存在运行中的事件循环，
# 直接调用 asyncio.run() 会抛出 RuntimeError。
# 解决方案：安装 nest_asyncio 并在 Notebook 顶部打补丁，
# 之后可直接使用 asyncio.run() 或 await 语法。
# ============================================================
# pip install nest_asyncio

import nest_asyncio                                    # nest_asyncio: 允许嵌套事件循环
nest_asyncio.apply()                                   # 对当前事件循环打补丁，允许嵌套调用

import asyncio                                         # asyncio: 异步标准库
from langchain_mcp_adapters.client import MultiServerMCPClient  # MultiServerMCPClient: MCP 客户端
from langchain.agents import create_agent              # create_agent: Agent 工厂

async def notebook_mcp_demo():
    """
    Notebook 环境下的 MCP 工具调用演示。

    Returns:
        str — Agent 最终回答文本
    """
    async with MultiServerMCPClient(
        {
            "math": {
                "transport": "stdio",              # str: stdio 传输
                "command":   "python",             # str: 启动命令
                "args":      ["math_server.py"],   # List[str]: Server 脚本路径
            }
        }
    ) as client:
        tools = client.get_tools()               # List[BaseTool]: MCP 工具列表
        agent = create_agent(
            model="openai:gpt-4o-mini",          # str: 模型标识
            tools=tools,                         # List[BaseTool]: MCP 工具
            system_prompt="你是一位数学计算助手。",  # str: 系统提示词
        )
        result = await agent.ainvoke(
            {"messages": [{"role": "user", "content": "计算 100 加 200 再乘以 3 的结果。"}]}
        )
        return result["messages"][-1].content  # str: 最终回答

# Notebook 中可直接 await 顶层协程（nest_asyncio 已允许嵌套）
answer = asyncio.run(notebook_mcp_demo())  # str: Agent 最终回答
print(answer)                              # str: 打印计算结果
```

#### 9.6.7 MCP 工具与自定义工具混用

```python
# ============================================================
# MCP 工具（BaseTool 实例）与 @tool 自定义工具类型完全兼容，
# 可直接合并进同一个 tools 列表传给 Agent
# ============================================================
import asyncio                                         # asyncio: 异步标准库
from langchain_mcp_adapters.client import MultiServerMCPClient  # MultiServerMCPClient: MCP 客户端
from langchain_core.tools import tool                  # @tool: 自定义工具装饰器
from langchain.agents import create_agent              # create_agent: Agent 工厂
from datetime import datetime, timezone, timedelta     # datetime: 时间处理标准库

@tool
def get_current_time() -> str:
    """
    获取当前 UTC+8 北京时间。

    Returns:
        str — 格式化时间字符串，如 "2026-07-27 14:00:00"
    """
    tz  = timezone(timedelta(hours=8))   # timezone: UTC+8 时区对象
    now = datetime.now(tz)               # datetime: 当前北京时间
    return now.strftime("%Y-%m-%d %H:%M:%S")  # str: 格式化时间字符串

async def run_hybrid_agent():
    """
    混合使用 MCP 工具和自定义 @tool 工具的 Agent 演示。

    Returns:
        str — Agent 最终回答文本
    """
    async with MultiServerMCPClient(
        {
            "math": {
                "transport": "stdio",            # str: stdio 传输
                "command":   "python",           # str: 启动命令
                "args":      ["math_server.py"], # List[str]: Server 脚本
            }
        }
    ) as client:
        mcp_tools    = client.get_tools()              # List[BaseTool]: MCP 工具列表
        custom_tools = [get_current_time]              # List[BaseTool]: @tool 自定义工具
        all_tools    = mcp_tools + custom_tools        # List[BaseTool]: 合并后的完整工具列表

        agent = create_agent(
            model="openai:gpt-4o-mini",  # str: 模型标识
            tools=all_tools,             # List[BaseTool]: MCP + 自定义混合工具
            system_prompt=(              # str: 系统提示词
                "你是一位综合助手，可以进行数学计算，也能告知当前时间。"
            ),
        )
        result = await agent.ainvoke({
            "messages": [
                {
                    "role":    "user",
                    "content": "现在几点？另外，7 乘以 6 等于多少？"
                }
            ]
        })
        final_answer = result["messages"][-1].content  # str: 最终回答
        print(f"Agent 回答: {final_answer}")            # str: 打印结果
        return final_answer                             # str: 返回最终回答

asyncio.run(run_hybrid_agent())
```

### 9.7 工具错误与异常处理

```python
# ============================================================
# 工具错误处理三要素：
#
#   1. ToolException  — 工具内部主动抛出，告知 Agent 失败原因；
#                       与普通 Exception 不同，LangChain 会将其文本封装为
#                       ToolMessage 追加到对话上下文，LLM 可读取并重试/告知用户
#
#   2. handle_tool_error — StructuredTool.from_function() / @tool.copy() 的配置参数，
#                          控制 ToolException 如何传回 LLM：
#                            False（默认）: 异常向上抛出，Agent 循环中断
#                            True         : 将异常消息字符串作为 ToolMessage 传回
#                            str          : 用固定文本传回（屏蔽内部细节）
#                            Callable     : 自定义函数处理异常后返回字符串传回
#
#   3. try/except 防御 — 工具内部捕获网络/系统级不可控异常，
#                        转为友好文本返回，避免 Agent 循环意外中断；
#                        参数非法等可预见错误则用 raise ToolException
# ============================================================
from langchain_core.tools import StructuredTool, ToolException  # StructuredTool / ToolException
from langchain.agents import create_agent                        # create_agent: v1.x Agent 工厂
from pydantic import BaseModel, Field                            # BaseModel / Field: 参数 schema

# ── 步骤 1：定义可能失败的工具函数 ───────────────────────────
class DivideInput(BaseModel):
    """除法工具的输入参数 schema。"""
    a: float = Field(description="被除数")              # float（必填）
    b: float = Field(description="除数，不能为零")       # float（必填）

def safe_divide(a: float, b: float) -> str:
    """
    安全除法工具，演示 ToolException + try/except 双层防御。

    Args:
        a: float — 被除数
        b: float — 除数

    Returns:
        str — 计算结果；参数非法时抛出 ToolException
    """
    try:
        if b == 0:
            # 参数非法 → raise ToolException，由 handle_tool_error 机制传回 LLM
            raise ToolException("除数不能为零，请提供非零的除数。")  # ToolException: 业务失败信号
        result = a / b            # float: 除法结果
        return f"{a} ÷ {b} = {result}"  # str: 格式化结果字符串
    except ToolException:
        raise  # ToolException 直接向上传播，交由 handle_tool_error 处理
    except Exception as e:
        # 不可控的系统异常 → 转为友好文本返回，避免 Agent 循环崩溃
        return f"工具执行出错（系统异常）：{e}，请稍后重试。"  # str: 友好错误提示

# ── 步骤 2：创建工具并配置 handle_tool_error ─────────────────
# handle_tool_error=True：捕获 ToolException，将异常消息作为 ToolMessage 传回 LLM（最常用）
# handle_tool_error="固定提示"：屏蔽内部细节，统一返回固定文本
# handle_tool_error=callable：自定义函数处理异常（如记录日志再返回提示）
divide_tool = StructuredTool.from_function(
    func=safe_divide,                        # Callable: 工具执行函数
    name="safe_divide",                      # str: 工具名称
    description="计算两数相除，除数不得为零。",  # str: 工具描述（LLM 据此决策调用时机）
    args_schema=DivideInput,                 # Type[BaseModel]: 参数 schema
    handle_tool_error=True,                  # bool: 捕获 ToolException，异常文本传回 LLM
)

# 验证：ToolException 被捕获后返回错误字符串而非抛出
print(divide_tool.invoke({"a": 10.0, "b": 0.0}))   # str: "除数不能为零，请提供非零的除数。"
print(divide_tool.invoke({"a": 10.0, "b": 2.0}))   # str: "10.0 ÷ 2.0 = 5.0"

# ── 步骤 3：注册到 Agent，Agent 可感知错误并自主决策 ──────────
agent = create_agent(
    model="openai:gpt-4o-mini",  # str: 模型标识（须支持 Tool Calling）
    tools=[divide_tool],         # List[StructuredTool]: 带错误处理的工具
    system_prompt=(              # str: 告知 Agent 遇到工具失败时的策略
        "你是一位计算助手。若工具返回错误，请根据错误提示修正参数后重试，"
        "仍然失败则如实告知用户。"
    ),
)

# 正常场景：LLM 提取正确参数，工具正常返回
result_ok = agent.invoke({
    "messages": [{"role": "user", "content": "计算 15 除以 3 的结果"}]
    # dict: role="user" — 用户消息，content 为消息内容
})
print("正常结果:", result_ok["messages"][-1].content)  # str: Agent 最终回答

# 异常场景：LLM 填入除数 0，工具返回 ToolException 文本，Agent 据此告知用户
result_err = agent.invoke({
    "messages": [{"role": "user", "content": "计算 10 除以 0 的结果"}]
})
print("异常结果:", result_err["messages"][-1].content)  # str: Agent 感知失败后的回答
```

---

## 十、结构化输出（Structured Output）

### 10.1 使用 Pydantic 定义输出结构

```python
# ============================================================
# with_structured_output() — 强制模型输出符合 Pydantic schema 的 JSON
# 相比 JsonOutputParser，类型更安全，自动验证字段
# ============================================================
from langchain_openai import ChatOpenAI  # ChatOpenAI: OpenAI 模型
from pydantic import BaseModel, Field    # BaseModel: Pydantic 数据模型基类；Field: 字段描述工具
from typing import List, Optional        # List: 列表类型；Optional: 可选类型

# 定义期望的输出结构 — 继承 Pydantic BaseModel
class ArticleAnalysis(BaseModel):
    """
    文章分析结果的结构化输出模型。

    Attributes:
        title      : str — 文章标题
        main_topic : str — 文章主题
        key_points : List[str] — 3~5 个关键要点
        sentiment  : str — 情感倾向（正面/负面/中性）
        word_count : Optional[int] — 估计字数（可选）
    """
    title:      str        = Field(description="文章的标题")
    main_topic: str        = Field(description="文章讨论的核心主题，一句话概括")
    key_points: List[str]  = Field(description="文章的 3~5 个关键要点，每条简洁明了")
    sentiment:  str        = Field(description="文章的情感倾向：正面、负面或中性")
    word_count: Optional[int] = Field(default=None, description="估计的文章字数（可选）")

llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温保证输出格式稳定

# model.with_structured_output(schema) -> Runnable
#   schema   : Type[BaseModel] / dict — Pydantic 模型类或 JSON Schema
#   返回值    : Runnable — 输出自动解析为 schema 指定类型的对象
structured_llm = llm.with_structured_output(ArticleAnalysis)  # Runnable: 结构化输出链

from langchain_core.prompts import ChatPromptTemplate  # ChatPromptTemplate: 对话模板

analyze_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位文章分析专家，请对给定文章进行深度分析。"),
    ("human",  "请分析以下文章：\n\n{article}"),  # {article}: str — 待分析文章文本
])

# 完整结构化分析链
analyze_chain = analyze_prompt | structured_llm  # Runnable: 输入 dict，输出 ArticleAnalysis

sample_article = """
LangChain 重新定义了 AI 应用开发的方式。通过其创新的 LCEL 语法，
开发者可以像搭积木一样将模型、提示词、检索器和工具组合成强大的应用。
这一框架不仅降低了 RAG 和 Agent 的开发门槛，
还通过统一的 Runnable 接口保证了组件的可互换性和可扩展性。
社区已贡献了超过 500 种集成，使 LangChain 成为 LLM 应用开发的首选工具。
"""  # str: 待分析的示例文章文本

result = analyze_chain.invoke({"article": sample_article})  # ArticleAnalysis: 结构化输出对象
print(f"标题: {result.title}")         # str: 文章标题
print(f"主题: {result.main_topic}")    # str: 核心主题
print(f"要点: {result.key_points}")   # List[str]: 关键要点列表
print(f"情感: {result.sentiment}")    # str: 情感倾向
print(f"字数: {result.word_count}")   # Optional[int]: 估计字数
print(f"类型验证: {type(result)}")    # <class 'ArticleAnalysis'>: Pydantic 对象类型
```

---

## 十一、完整端到端流程（RAG + Agent 一键跑通）

```python
# ============================================================
# 端到端完整示例：
# 构建一个「支持工具调用的 RAG 智能助手」
# 具备以下能力：
#   1. 从知识库检索 LangChain 相关文档并回答
#   2. 使用计算器处理数学问题
#   3. 查询天气信息
#   4. 多轮对话记忆
# ============================================================
import os                                                      # 操作系统接口
from langchain_openai import ChatOpenAI, OpenAIEmbeddings      # OpenAI 模型 + 嵌入模型
from langchain_chroma import Chroma                            # Chroma 向量数据库
from langchain_text_splitters import RecursiveCharacterTextSplitter  # 文本分块器
from langchain_core.documents import Document                  # 文档数据类
from langchain_core.tools import tool                          # Tool 装饰器
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder  # 提示词组件
from langchain_core.output_parsers import StrOutputParser      # 字符串解析器
from langchain_classic.agents import create_tool_calling_agent, AgentExecutor  # Agent 组件（langchain-classic）
from langchain_community.chat_message_histories import ChatMessageHistory  # 对话历史
import json                                                    # JSON 处理
import math                                                    # 数学运算

# ============================================================
# 步骤 1：准备知识库文档
# ============================================================
knowledge_docs = [  # List[Document]: 知识库原始文档列表
    Document(
        page_content=(
            "LangChain 由 Harrison Chase 于 2022 年 10 月创建，"
            "是目前 GitHub 上最受欢迎的 LLM 应用开发框架之一，获得超过 14 万星。"
            "框架核心是 LCEL（LangChain Expression Language），"
            "使用 | 运算符将组件串联成可流式、可异步的管道。"
        ),
        metadata={"source": "langchain_overview", "topic": "历史与概述"},
        # metadata: dict — 文档来源和主题元数据，用于过滤检索
    ),
    Document(
        page_content=(
            "RAG（Retrieval-Augmented Generation，检索增强生成）是 LangChain 最常见的应用场景。"
            "典型流程：文档加载 → 文本分块 → 向量嵌入 → 存入向量库 → 相似度检索 → 注入 Prompt → LLM 生成。"
            "常用向量数据库：Chroma（本地开发）、FAISS（高性能）、Pinecone（云服务）。"
        ),
        metadata={"source": "langchain_rag", "topic": "RAG 技术"},
    ),
    Document(
        page_content=(
            "LangChain Agent 让 LLM 具备主动调用工具的能力。"
            "通过 @tool 装饰器定义工具，create_tool_calling_agent 构建 Agent 推理逻辑，"
            "AgentExecutor 负责「推理 → 工具调用 → 观察」的循环执行。"
            "Agent 能自动决策何时调用哪个工具，适合处理复杂多步任务。"
        ),
        metadata={"source": "langchain_agent", "topic": "Agent"},
    ),
    Document(
        page_content=(
            "LangChain 生态系统包含多个包："
            "langchain-core（核心抽象）、langchain（高级封装）、"
            "langchain-openai（OpenAI 集成）、langchain-community（社区集成，500+ 种）、"
            "langchain-chroma（Chroma 集成）。"
            "langchain-text-splitters 提供文本分块工具，独立维护以控制依赖体积。"
        ),
        metadata={"source": "langchain_ecosystem", "topic": "生态系统"},
    ),
]

# ============================================================
# 步骤 2：分块 + 向量化 + 存入 Chroma
# ============================================================
splitter = RecursiveCharacterTextSplitter(
    chunk_size=300,   # int: 每块最多 300 字符
    chunk_overlap=50, # int: 相邻块重叠 50 字符
)
chunks = splitter.split_documents(knowledge_docs)  # List[Document]: 分块后的文档列表

embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small",  # str: 嵌入模型名称
    base_url=OPENAI_BASE_URL,        # str: API 端点地址
)

# 使用内存模式（不持久化），适合快速演示
vectorstore_e2e = Chroma.from_documents(
    documents=chunks,   # List[Document]: 文档块列表
    embedding=embeddings,  # OpenAIEmbeddings: 嵌入模型
)
retriever_e2e = vectorstore_e2e.as_retriever(
    search_kwargs={"k": 2}  # dict: 每次检索返回最相关的 2 个文档块
)
print(f"✓ 知识库构建完成，共 {len(chunks)} 条向量")

# ============================================================
# 步骤 3：定义工具集
# ============================================================
@tool
def search_langchain_docs(query: str) -> str:
    """
    在 LangChain 知识库中检索相关文档，用于回答 LangChain 相关问题。

    Args:
        query: str — 检索查询词，如"LangChain 的 RAG 流程"

    Returns:
        str — 检索到的相关文档内容片段
    """
    docs = retriever_e2e.invoke(query)     # List[Document]: 检索结果文档列表
    if not docs:                            # 未检索到相关内容时
        return "知识库中未找到相关信息。"  # str: 未找到信息的提示
    return "\n\n".join(doc.page_content for doc in docs)  # str: 拼接文档内容

@tool
def math_calculator(expression: str) -> str:
    """
    计算数学表达式，支持基本算术运算和 math 库函数（sqrt、pow、log 等）。

    Args:
        expression: str — 数学表达式字符串，如 "sqrt(256) + log(100)"

    Returns:
        str — 计算结果字符串
    """
    try:
        allowed = {k: v for k, v in math.__dict__.items() if not k.startswith("_")}  # Dict[str, Any]: 允许访问的 math 函数命名空间（排除私有成员）
        result = eval(expression, {"__builtins__": {}}, allowed)  # Any: eval 在受限环境中的计算结果
        return f"计算结果：{result}"  # str: 格式化后的计算结果
    except Exception as e:             # Exception: 捕获所有计算异常
        return f"计算失败：{str(e)}"   # str: 错误信息

@tool
def get_city_weather(city: str) -> str:
    """
    查询城市天气信息（演示版，仅支持北京、上海、广州）。

    Args:
        city: str — 城市名称

    Returns:
        str — JSON 格式的天气信息字符串
    """
    mock_weather = {  # Dict[str, Dict]: 模拟天气数据
        "北京": {"温度": "22°C", "天气": "晴", "空气质量": "良"},
        "上海": {"温度": "27°C", "天气": "阴", "空气质量": "优"},
        "广州": {"温度": "31°C", "天气": "小雨", "空气质量": "良"},
    }
    data = mock_weather.get(city, {"提示": f"暂无 {city} 的天气数据"})  # Dict: 获取城市天气，不存在时返回提示信息
    return json.dumps(data, ensure_ascii=False, indent=2)  # str: 格式化 JSON 天气信息

e2e_tools = [search_langchain_docs, math_calculator, get_city_weather]  # List[Tool]: 端到端演示用工具列表

# ============================================================
# 步骤 4：构建带记忆的 Agent
# ============================================================
agent_llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: Agent 推理模型

e2e_prompt = ChatPromptTemplate.from_messages([
    ("system",
     "你是一位智能 AI 助手，具备以下能力：\n"
     "1. 通过知识库检索 LangChain 相关文档来回答技术问题\n"
     "2. 执行数学计算\n"
     "3. 查询城市天气\n\n"
     "请优先使用工具获取信息，再综合信息给出完整、清晰的回答。"),
    MessagesPlaceholder(variable_name="chat_history", optional=True),
    # optional=True: 历史为空时不报错
    ("human", "{input}"),                                   # str: 用户当前输入
    MessagesPlaceholder(variable_name="agent_scratchpad"),  # Agent 工具调用记录（必须）
])

agent_core = create_tool_calling_agent(
    llm=agent_llm,       # ChatOpenAI: 模型实例
    tools=e2e_tools,     # List[Tool]: 工具列表
    prompt=e2e_prompt,   # ChatPromptTemplate: Agent 提示词
)
agent_exec = AgentExecutor(
    agent=agent_core,              # Runnable: Agent 推理核心
    tools=e2e_tools,               # List[Tool]: 可用工具
    verbose=False,                 # bool: 关闭详细日志，仅展示最终结果
    max_iterations=8,              # int: 最多循环 8 步
    handle_parsing_errors=True,    # bool: 自动处理解析错误
)

# ChatMessageHistory: 多轮对话历史管理器
conversation_history = ChatMessageHistory()  # ChatMessageHistory: 空历史容器

def smart_assistant(user_input: str) -> str:
    """
    智能助手主函数，整合 Agent + Memory，支持多轮对话。

    Args:
        user_input: str — 用户输入的问题或指令

    Returns:
        str — Assistant 的最终回答文本
    """
    # agent_exec.invoke() -> Dict[str, Any]
    #   input        : str — 用户当前输入
    #   chat_history : List[BaseMessage] — 历史消息列表
    result = agent_exec.invoke({
        "input":        user_input,                    # str: 当前用户输入
        "chat_history": conversation_history.messages, # List[BaseMessage]: 历史消息
    })
    answer = result["output"]  # str: Agent 的最终回答文本

    # 更新对话历史
    conversation_history.add_user_message(user_input)  # 追加 HumanMessage，None 无返回值
    conversation_history.add_ai_message(answer)         # 追加 AIMessage，None 无返回值

    return answer  # str: 当前轮次的最终回答

# ============================================================
# 步骤 5：端到端测试
# ============================================================
print("=" * 60)
print("LangChain 智能助手（RAG + Agent + Memory）")
print("=" * 60)

# 测试用例列表 — List[str]: 覆盖不同工具调用场景
test_cases = [
    "LangChain 的 RAG 技术是什么，有哪些常用的向量数据库？",   # → 触发 search_langchain_docs
    "请计算 log(1000) + sqrt(625) 的结果",                      # → 触发 math_calculator
    "上海今天天气如何？",                                         # → 触发 get_city_weather
    "根据刚才的 RAG 介绍，LangChain Agent 和 RAG 可以结合使用吗？",  # → 多工具 + 记忆
]

for i, question in enumerate(test_cases, 1):  # i: int 问题序号（从 1 开始），question: str 问题
    print(f"\n[第 {i} 轮] 用户：{question}")
    print("-" * 40)
    answer = smart_assistant(question)     # str: 智能助手的回答
    print(f"助手：{answer}")
    print(f"（历史消息总数：{len(conversation_history.messages)}）")  # int: 当前历史消息条数

print("\n" + "=" * 60)
print("✓ 端到端流程全部完成！")
print("=" * 60)
```

---

## 十二、补充知识点

### 12.1 非对话模型 LLMs vs ChatModel 对比

```python
# ============================================================
# LangChain 支持三类模型：
#   1. LLMs（Text Model / 非对话模型）— 输入文本字符串，输出文本字符串
#   2. Chat Models（对话模型）        — 输入 List[BaseMessage]，输出 AIMessage
#   3. Embedding Model（嵌入模型）    — 输入文本，输出 List[float] 向量
# ============================================================
import os
import dotenv  # dotenv: 环境变量加载工具

dotenv.load_dotenv()  # bool: 加载 .env 文件中的环境变量
os.environ["OPENAI_API_KEY"]  = os.getenv("OPENAI_API_KEY",  "your-key")   # str: API 密钥
os.environ["OPENAI_BASE_URL"] = os.getenv("OPENAI_BASE_URL", "https://api.openai.com/v1")  # str: API 地址

# ---- 类型 1：非对话模型（LLMs）----
# 特点：直接输入字符串，输出字符串；不支持多轮对话上下文；已逐步被 ChatModel 取代
from langchain_openai import OpenAI  # OpenAI: 非对话模型封装类（对应旧版 text-completion API）

llm = OpenAI(
    model="gpt-3.5-turbo-instruct",  # str: 支持 text-completion 的非对话模型
    temperature=0.7,                  # float: 采样温度，0~2，越高越随机
    max_tokens=256,                   # int: 最大生成 Token 数
)
result_str = llm.invoke("写一首关于春天的七言绝句")  # str: 直接返回字符串（非 AIMessage）
print(type(result_str))   # <class 'str'>: 非对话模型返回纯字符串
print(result_str)         # str: 生成的诗句文本

# ---- 类型 2：对话模型（Chat Models）—— 主流用法 ----
# 特点：支持多角色消息列表；原生支持多轮上下文；返回 AIMessage 对象
from langchain_openai import ChatOpenAI                           # ChatOpenAI: 对话模型封装
from langchain_core.messages import SystemMessage, HumanMessage  # 消息类型

chat_model = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 对话模型实例

response = chat_model.invoke([              # List[BaseMessage] → AIMessage
    SystemMessage(content="你是一位唐诗专家"),
    HumanMessage(content="写一首关于春天的七言绝句"),
])
print(type(response))          # <class 'langchain_core.messages.ai.AIMessage'>: 对话模型返回 AIMessage
print(response.content)        # str: AIMessage.content 字段提取文本
print(response.response_metadata)  # dict: 包含 token_usage、model_name 等元数据
```

### 12.2 Token 机制详解

```python
# ============================================================
# Token 是大模型处理文本的最小单位，也是收费计量依据
# ------------------------------------------------------------
# 中文：1 Token ≈ 1~1.8 个汉字
# 英文：1 Token ≈ 3~4 个字母
# 计算工具：https://platform.openai.com/tokenizer
#
# 常见模型上下文窗口（影响可输入的最大 Token 数）：
#   gpt-4o          : 128k Token
#   gpt-4o-mini     : 128k Token
#   deepseek-v3     : 128k Token（DeepSeek 官方文档，Max Output 8k）
#   qwen-max        : 32k Token（通义千问，具体版本请以官网为准）
# ============================================================
from langchain_openai import ChatOpenAI  # ChatOpenAI: 对话模型封装

token_demo_model = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温稳定

# 调用后可通过两种方式获取 Token 使用量
response = token_demo_model.invoke("LangChain 是什么？用一句话回答。")  # AIMessage 对象

# ---- 方式 1（推荐）：usage_metadata — LangChain v1.x 统一标准字段 ----
# AIMessage.usage_metadata: Optional[UsageMetadata] — 跨 provider 的标准 Token 统计接口
# UsageMetadata 字段：
#   input_tokens  : int — 输入（提示词）消耗的 Token 数
#   output_tokens : int — 输出（生成内容）消耗的 Token 数
#   total_tokens  : int — 总消耗 Token 数（计费依据）
usage = response.usage_metadata  # Optional[UsageMetadata]: 标准 Token 用量对象
if usage:                         # 不为 None 时打印（部分 Provider 可能不返回）
    print(f"输入 Token: {usage['input_tokens']}")   # int: 输入 Token 数
    print(f"输出 Token: {usage['output_tokens']}")  # int: 输出 Token 数
    print(f"总计 Token: {usage['total_tokens']}")   # int: 总 Token 数

# ---- 方式 2（兼容）：response_metadata — Provider 原始元数据（结构因 provider 而异）----
# 仅用于调试或需要 provider 特有字段时使用，字段名因供应商不同而不同
meta = response.response_metadata  # dict: OpenAI 原始响应元数据
print(f"原始元数据: {meta}")        # dict: 含 model_name、finish_reason 等字段

print(f"回答内容: {response.content}")  # str: AI 生成的回答文本
```

### 12.3 消息类型（Message Types）系统

```python
# ============================================================
# LangChain 消息类型体系 — langchain_core.messages 模块
# 所有消息类型均继承自 BaseMessage，包含 .content 和 .additional_kwargs
# ------------------------------------------------------------
# SystemMessage  : 系统级指令，设定 AI 角色/行为规则，通常排在消息列表首位
# HumanMessage   : 用户消息（Human 角色）
# AIMessage      : AI 回复消息，由模型生成或用于构造 Few-Shot 示例
# ChatMessage    : 自定义角色的通用消息（role 字段可自定义）
# ToolMessage    : 工具调用结果消息，由工具执行结果填充（Agent 场景使用）
# ============================================================
from langchain_core.messages import (
    SystemMessage,   # SystemMessage: 系统指令消息，content: str 系统提示词
    HumanMessage,    # HumanMessage : 用户输入消息，content: str 用户问题
    AIMessage,       # AIMessage    : AI 回复消息，content: str 模型回答
    ChatMessage,     # ChatMessage  : 自定义角色消息，role: str 自定义角色名
    ToolMessage,     # ToolMessage  : 工具结果消息，content: str 结果，tool_call_id: str 调用 ID
)

# SystemMessage — 设定 AI 身份与行为规范
system_msg = SystemMessage(
    content="你是一位专业的 Python 编程老师，每次解释都要举例说明。",
    # content: str — 系统级指令文本
    # 注意：additional_kwargs 会作为额外字段发送给 API，
    # 只应填写 API 支持的标准字段（如 OpenAI 支持的 name: str）。
    # 填写 API 不认识的自定义字段可能导致请求报错，不应用于存本地元数据。
)

# HumanMessage — 用户输入
human_msg = HumanMessage(content="什么是列表推导式？")  # content: str — 用户的问题

# AIMessage — 模拟 AI 回答（常用于 Few-Shot 构建对话示例）
ai_msg = AIMessage(content="列表推导式是 Python 中用于创建列表的简洁语法，例如 [x*2 for x in range(5)]")
# content: str — AI 历史回复文本，用于多轮对话或 Few-Shot 示例

# ChatMessage — 自定义角色（如"助教"角色）
custom_msg = ChatMessage(
    role="助教",        # role: str — 自定义角色名称（区别于 system/human/ai）
    content="补充说明：列表推导式还支持条件过滤，如 [x for x in range(10) if x % 2 == 0]"
)

# ToolMessage — 工具调用返回的结果消息
tool_msg = ToolMessage(
    content="计算结果：42",     # content: str — 工具执行结果文本
    tool_call_id="call_001",    # tool_call_id: str — 对应 AIMessage 中工具调用的 ID
)

# 构建完整对话消息列表，传给 ChatModel 进行多轮推理
messages = [system_msg, human_msg, ai_msg]  # List[BaseMessage]: 完整对话历史

from langchain_openai import ChatOpenAI  # ChatOpenAI: 对话模型封装
msg_model = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 模型实例
follow_up = msg_model.invoke(messages + [HumanMessage(content="再举一个带条件过滤的例子")])
# AIMessage: 模型基于完整历史（含 AIMessage 示例）生成的后续回答
print(follow_up.content)  # str: 基于多轮上下文的回答
```

### 12.4 异步调用（ainvoke / astream / abatch）

```python
# ============================================================
# async / await 关键字基础
# ------------------------------------------------------------
# async def func():   — 将普通函数声明为"协程函数"（coroutine function）
#   调用时不会立即执行函数体，而是返回一个协程对象（Coroutine）。
#   协程对象本身不占用线程，只有被事件循环调度时才真正执行。
#
# await expr:         — 准确含义：触发协程执行，等待其完成，并取出 return 的值。
#   具体步骤：
#     ① 触发：将 expr（协程对象）交给事件循环，开始真正执行其函数体；
#     ② 挂起：当前协程暂停，把 CPU 执行权交还给事件循环（可调度其他协程）；
#     ③ 等待：直到 expr 执行完毕（或 I/O 就绪）；
#     ④ 取值：将 expr 函数体中 return 的值作为 await 表达式的结果返回给调用方。
#   await 只能出现在 async def 函数体内部。
#   注意：await 不是"解包"——协程对象本身不包裹返回值，
#         返回值只有在协程执行完毕后才产生，由 await 负责等待并接收。
#
# ── 操作系统视角：协程与线程的分类关系 ──────────────────────────────────
# 操作系统中"线程"按管理者不同分为两类：
#
#   ┌─────────────────────────────────────────────────────────────┐
#   │                        线程 (Thread)                        │
#   ├──────────────────────────┬──────────────────────────────────┤
#   │   内核级线程               │   用户级线程                     │
#   │  (Kernel-Level Thread)   │  (User-Level Thread, ULT)        │
#   ├──────────────────────────┼──────────────────────────────────┤
#   │ 由操作系统内核创建与调度    │ 由用户态库/运行时创建与调度         │
#   │ 内核可见，可跨 CPU 并行     │ 内核不可见，对内核透明             │
#   │ 切换需系统调用（开销大）    │ 切换在用户态完成（开销极小）         │
#   │ 例：pthread、OS 线程       │ 例：协程、绿色线程、纤程            │
#   └──────────────────────────┴──────────────────────────────────┘
#
# 用户级线程的再细分（按调度方式）：
#
#   用户级线程
#   ├── 抢占式用户级线程：运行时可在任意时刻中断并切换
#   │     例：Go 的 goroutine（Go 运行时实现抢占调度）
#   └── 协作式用户级线程：必须由代码主动让出，不可被强制中断
#         例：Python 协程（async/await）← 本节所讲
#
# Python 协程 在操作系统分类中的精确定位：
#   协程 = 无栈的、协作式调度的用户级线程
#   ① "用户级"：事件循环（asyncio）在用户态管理所有协程，内核完全不感知；
#               一个 OS 线程可承载数万个协程，不消耗内核线程资源。
#   ② "协作式"：只能在 await 处主动让出 CPU，运行时不会强制中断；
#               因此协程之间天然无数据竞争，不需要锁。
#   ③ "无栈"  ：协程不分配独立调用栈，执行状态保存在帧对象（frame object）中；
#               内存占用远小于用户级线程（字节级 vs. KB 级）。
#
# ── 用户级线程与内核级线程的映射模型 ────────────────────────────────────
# 操作系统定义了三种经典的映射模型（ULT = 用户级线程，KLT = 内核级线程）：
#
#   多对一（M:1）          一对一（1:1）          多对多（M:N）
#   ─────────────          ─────────────          ─────────────
#   ULT ULT ULT            ULT ULT ULT            ULT ULT ULT ULT
#       │                   │   │   │               │  ╲  ╱  │
#      KLT                 KLT KLT KLT             KLT  KLT KLT
#
#   Python 协程              Java/C pthread          Go goroutine
#   所有协程共用              每个用户线程             M 个协程映射到
#   同一个 OS 线程            对应一个内核线程          N 个 OS 线程
#
# Python async/await 属于 多对一（M:1）模型：
#   · 所有协程运行在同一个内核级线程（即事件循环所在的 OS 线程）上；
#   · 任意时刻只有一个协程占用 CPU，其余协程均处于挂起等待状态；
#   · 内核始终只看到 1 个线程，无法将协程分配到多个 CPU 核心并行执行。
#
# 直接推论——协程的能力边界：
#   ✓ 适合 I/O 密集型任务（网络请求、数据库查询、文件读写）：
#       等待 I/O 时主动让出 CPU，事件循环调度其他协程，实现高并发；
#       总耗时 ≈ 最慢单次 I/O，而非所有 I/O 之和。
#   ✗ 不适合 CPU 密集型任务（矩阵运算、图像处理、模型推理）：
#       协程无法分配到多核，CPU 计算仍是串行的，async 带来的只是调度开销；
#       此类场景应使用 multiprocessing 或 concurrent.futures.ProcessPoolExecutor。
#
# 与多线程的核心区别：
#   多线程：多个线程"同时"运行，操作系统负责切换，存在数据竞争风险。
#   async  ：单线程内"交替"运行，由事件循环在 await 处主动让出，无锁无竞争。
#   LLM 调用属于 I/O 密集型（等待网络响应），最适合用 async 并发。
#
# "主动让出"让出的是什么？
#   让出的是：CPU 执行权（计算资源）
#   不让出的：I/O 连接本身（网络请求在后台由网卡持续传输，与 CPU 无关）
#
#   同步阻塞时：协程发出 HTTP 请求 → 线程挂起 → CPU 空转浪费，什么也做不了。
#   await 时  ：协程发出 HTTP 请求 → 主动声明"我不需要 CPU 了"
#               → 事件循环把 CPU 立刻转交给其他协程继续运行
#               → 网卡在后台独立传输数据（I/O 资源不受影响）
#               → I/O 完成后事件循环通知协程，从 await 处恢复执行。
#
#   结果：3 个 LLM 请求并发时，总耗时 ≈ 最慢那个请求的时延（而非 3 倍）。
#   原因：三个请求几乎同时发出，等待时间在后台"并行重叠"，不是串行累加。
#
#   甘特图对比（每格 = 1s，█ = 等待网络中，░ = 空转/阻塞）：
#
#   【同步串行】                 0    1    2    3    4    5    6
#                               |    |    |    |    |    |    |
#   请求A                       █░░░░|    |    |    |    |    |
#   请求B                       ░░░░░|████████ |    |    |    |
#   请求C                       ░░░░░|░░░░░░░░ |████████████ |
#                               总耗时 = 1+2+3 = 6s
#
#   【async 并发】               0    1    2    3
#                               |    |    |    |
#   请求A                       █░░░░|    |    |
#   请求B                       ████████  |    |
#   请求C                       ████████████   |
#                               总耗时 = max(1,2,3) = 3s
#
#   三个请求在 t=0 同时发出（await 让出 CPU 后事件循环立即发下一个），
#   网络等待时间在后台并行重叠，互不阻塞，总耗时由最慢的请求决定。
#
# LangChain 所有 Runnable 均提供异步版本：
#   .ainvoke(input)     → 异步单次调用，返回 Coroutine[Any, Any, T]
#   .astream(input)     → 异步流式迭代，返回 AsyncIterator[chunk]
#   .abatch(inputs)     → 异步批量并行，返回 Coroutine[Any, Any, List[T]]
#
# 适用场景：
#   - FastAPI / Starlette 等异步 Web 框架
#   - 同时发起多个 LLM 请求（并发提速）
#   - Jupyter Notebook 中（直接 await，无需 asyncio.run）
#     原因：async def 协程必须在事件循环中运行。
#       普通 .py 脚本默认没有事件循环，需用 asyncio.run(coro()) 手动启动；
#       Jupyter 的 IPython 内核本身已持续运行着一个事件循环，
#       因此 Cell 中可以直接写 `await coro()`，无需额外的 asyncio.run()。
# ============================================================
import asyncio  # asyncio: Python 异步 I/O 标准库
from langchain_openai import ChatOpenAI          # ChatOpenAI: 对话模型封装
from langchain_core.prompts import ChatPromptTemplate      # ChatPromptTemplate: 提示词模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器

async_model  = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)   # ChatOpenAI: 模型实例
async_prompt = ChatPromptTemplate.from_messages([("human", "{input}")])  # 提示词模板
async_parser = StrOutputParser()                                    # StrOutputParser: 解析器
async_chain  = async_prompt | async_model | async_parser           # Runnable: 异步可调用链

async def demo_async():
    """
    展示三种异步调用方式，对比同步 invoke 效率。

    Returns:
        None — 直接打印各类异步调用结果
    """
    # ---- ainvoke：异步单次调用 ----
    # chain.ainvoke(input) -> Coroutine → str
    #   input  : dict — 输入变量字典
    #   返回值  : str（await 等待执行完毕后取出）— 与 invoke 结果相同，但不阻塞事件循环
    result = await async_chain.ainvoke({"input": "用一句话解释递归"})  # str: 异步调用结果
    print(f"ainvoke 结果: {result}")

    # ---- astream：异步流式输出 ----
    # chain.astream(input) -> AsyncIterator[str]
    #   每次 yield 一个 str 片段（一个或多个 Token）
    print("astream 流式: ", end="")
    async for chunk in async_chain.astream({"input": "用一句话解释闭包"}):
        # chunk: str — 流式输出的单个文本片段
        print(chunk, end="", flush=True)  # 实时打印，不换行
    print()  # 流式结束换行

    # ---- abatch：异步批量并行（效率最高）----
    # chain.abatch(inputs) -> Coroutine → List[str]
    #   inputs  : List[dict] — 多个输入字典
    #   返回值  : List[str]（await 等待执行完毕后取出）— 与输入一一对应，并发执行
    questions = [     # List[dict]: 批量问题输入
        {"input": "什么是装饰器？"},
        {"input": "什么是生成器？"},
        {"input": "什么是上下文管理器？"},
    ]
    results = await async_chain.abatch(questions)  # List[str]: 并发执行的批量结果
    for q, r in zip(questions, results):   # q: dict 输入，r: str 输出
        print(f"Q: {q['input']}\nA: {r}\n")  # 打印问答对

# Jupyter Notebook 中直接运行：await demo_async()
# 普通 Python 脚本中运行：
# asyncio.run(demo_async())
```

### 12.5 多国内平台模型接入

```python
# ============================================================
# LangChain 兼容所有遵循 OpenAI API 规范的模型提供商
# 只需替换 base_url、api_key、model_name 即可无缝切换
# ============================================================
import os
from langchain_openai import ChatOpenAI  # ChatOpenAI: 通用接口，支持所有 OpenAI 兼容 API

# ---- 方式1：阿里云百炼平台（通义千问 / DeepSeek 等）----
# 注册并获取 API Key：https://bailian.console.aliyun.com/#/home
# 支持的模型：qwen-max、qwen-plus、qwen-turbo、deepseek-r1 等
qwen_model = ChatOpenAI(
    model="qwen-max",                                              # str: 通义千问旗舰模型
    api_key=os.getenv("DASHSCOPE_API_KEY", "your-dashscope-key"), # str: 百炼平台 API Key
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1", # str: 百炼 OpenAI 兼容端点
    temperature=0.7,                                               # float: 采样温度
)

# ---- 方式2：百度千帆平台（ERNIE 文心一言）----
# 注册并获取 Bearer Token：https://cloud.baidu.com/doc/qianfan-docs/
ernie_model = ChatOpenAI(
    model="ernie-4.0-turbo-8k",                                  # str: 文心一言旗舰模型
    api_key=os.getenv("QIANFAN_API_KEY", "your-qianfan-key"),    # str: 千帆 Bearer Token
    base_url="https://qianfan.baidubce.com/v2",                  # str: 千帆 OpenAI 兼容端点
    temperature=0.7,
)

# ---- 方式3：智谱 AI（ChatGLM）----
# 注册并获取 API Key：https://www.bigmodel.cn/usercenter/proj-mgmt/apikeys
glm_model = ChatOpenAI(
    model="glm-4",                                               # str: ChatGLM-4 模型
    api_key=os.getenv("ZHIPUAI_API_KEY", "your-zhipuai-key"),   # str: 智谱 API Key
    base_url="https://open.bigmodel.cn/api/paas/v4/",           # str: 智谱 OpenAI 兼容端点
    temperature=0.7,
)

# ---- 所有模型均可无缝接入 LCEL 管道 ----
from langchain_core.prompts import ChatPromptTemplate      # ChatPromptTemplate: 提示词模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器

test_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位简洁的 AI 助手"),
    ("human", "{question}"),  # str: 用户问题占位符
])
test_parser = StrOutputParser()  # StrOutputParser: 字符串解析器

# 切换模型只需替换 llm 变量，链的其余部分不变
# qwen_chain = test_prompt | qwen_model | test_parser   # 通义千问链
# ernie_chain = test_prompt | ernie_model | test_parser # 文心一言链
# glm_chain   = test_prompt | glm_model | test_parser   # ChatGLM 链
# 调用方式完全相同：chain.invoke({"question": "..."})
```

### 12.6 PromptTemplate（非对话式模板）

```python
# ============================================================
# PromptTemplate — 纯文本提示词模板（适合非对话模型或简单场景）
# 区别于 ChatPromptTemplate：
#   PromptTemplate     → 生成单个字符串（StringPromptValue）
#     .to_string() 返回一整段拼接好的纯文本，直接传给文本补全模型（无角色区分）。
#     示例："你是一位专业的程序员。\n请用简洁的语言回答以下问题：\n什么是 GIL？"
#
#   ChatPromptTemplate → 生成消息列表（ChatPromptValue）
#     .to_messages() 返回 List[BaseMessage]，每条消息携带独立的角色标签，
#     传给对话模型时按角色分别送入不同的 token 位置（system / user / assistant）。
#     示例：[
#       SystemMessage(content="你是一位专业的 AI 领域专家..."),  # system 角色
#       HumanMessage(content="什么是 Transformer？"),           # user 角色
#     ]
#
#   两者都实现同一个 PromptValue 接口（均有 .to_string() 和 .to_messages()），
#   LCEL 管道会根据下游模型类型自动选择调用哪个方法转换格式，无需手动处理。
# ============================================================
from langchain_core.prompts import PromptTemplate  # PromptTemplate: 纯文本提示词模板类

# 方式 1：from_template() 工厂方法（推荐）
# PromptTemplate.from_template(template) -> PromptTemplate
#   template: str — 包含 {变量名} 占位符的模板字符串
#   返回值   : PromptTemplate — 模板对象，自动解析变量列表
prompt1 = PromptTemplate.from_template(
    "你是一位专业的{role}。\n请用简洁的语言回答以下问题：\n{question}"
    # {role}    : str — 动态注入的角色名称
    # {question}: str — 动态注入的用户问题
)

# prompt.format(**kwargs) -> str
#   返回值: str — 将占位符替换为具体值后的完整字符串
formatted_str = prompt1.format(role="Python 程序员", question="什么是 GIL？")  # str: 完整格式化后的提示词字符串
print(formatted_str)   # 打印格式化结果
print(type(formatted_str))  # <class 'str'>

# 方式 2：构造函数（显式指定变量列表）
prompt2 = PromptTemplate(
    input_variables=["domain", "topic"],  # List[str]: 模板中使用的变量名列表
    template="作为{domain}领域专家，请详细解释{topic}的核心原理。",
    # template: str — 提示词模板字符串，{} 中为变量名
)

# prompt.invoke(input_dict) -> StringPromptValue
#   返回值: StringPromptValue — 与 ChatPromptTemplate 共享 Runnable 接口
prompt_value = prompt2.invoke({"domain": "机器学习", "topic": "反向传播算法"})
print(prompt_value.to_string())  # str: 调用 to_string() 转为字符串

# 方式 3：+ 运算符拼接（组合提示词）
# PromptTemplate 支持用 + 运算符将多个模板或字符串拼接
combined_prompt = (
    PromptTemplate.from_template("请分析以下主题：{topic}")
    + "，要求回答简洁明了"
    + "\n并用{language}语言回复"
    # 拼接后自动合并变量：{topic} 和 {language} 都成为变量
)
print(combined_prompt.format(topic="LCEL管道", language="中文"))  # str: 拼接后格式化结果

# ---- PromptTemplate 与 ChatPromptTemplate 结合 LCEL ----
from langchain_openai import OpenAI          # OpenAI: 非对话模型（配合 PromptTemplate）
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器

text_llm   = OpenAI(model="gpt-3.5-turbo-instruct", temperature=0.7)  # OpenAI: 非对话模型
text_chain = prompt2 | text_llm | StrOutputParser()  # Runnable: PromptTemplate → LLM → str

result = text_chain.invoke({"domain": "量子物理", "topic": "量子纠缠"})  # str: 生成结果
print(result)  # str: 基于 PromptTemplate 的非对话模型生成结果
```

### 12.7 RunnablePassthrough.assign（多字段流水线）

```python
# ============================================================
# RunnablePassthrough.assign(**kwargs) — 在保留输入字典的基础上追加新字段
# 等价于旧版 SequentialChain 的多变量场景
# 适合多步流水线中「既保留原始输入，又追加每步中间结果」的场景
# ============================================================
from langchain_core.runnables import RunnablePassthrough  # RunnablePassthrough: 透传 Runnable
from langchain_core.prompts import ChatPromptTemplate     # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                   # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 解析器

# 第一步：产品名 → 定价（只输出整数）
price_prompt = ChatPromptTemplate.from_template(
    "只输出一个合理的整数价格（元），不要任何其他文字。产品：{product}"
    # {product}: str — 产品名称占位符
)

# 第二步：产品名 + 价格 → 促销文案
promo_prompt = ChatPromptTemplate.from_template(
    "为「{product}」（售价 {price} 元）写一句 40 字以内的促销文案。"
    # {product}: str — 原始产品名（来自初始输入，被 assign 保留）
    # {price}  : str — 上一步 LLM 生成的价格（被 assign 追加）
)

# RunnablePassthrough.assign(key=runnable) -> RunnableAssign
#   key     : str      — 追加到输入字典的新键名
#   runnable: Runnable — 用来计算该键值的子链
#   输入/输出: Dict → Dict（在原字典基础上新增 key 字段）
chain = (
    RunnablePassthrough.assign(
        price=price_prompt | llm | parser  # Runnable: 计算 price 字段，输出 str 价格
        # 此时输入 dict 从 {"product": "..."} → {"product": "...", "price": "299"}
    )
    | RunnablePassthrough.assign(
        promo_text=promo_prompt | llm | parser  # Runnable: 计算 promo_text 字段
        # 此时 dict → {"product": "...", "price": "299", "promo_text": "..."}
    )
)

result = chain.invoke({"product": "无线降噪耳机"})  # Dict: 最终包含所有字段的输出字典
# result: Dict[str, str] — 包含 product/price/promo_text 三个键
print(f"产品: {result['product']}")        # str: 原始产品名
print(f"定价: {result['price']} 元")       # str: LLM 生成的价格
print(f"文案: {result['promo_text']}")     # str: LLM 生成的促销文案
```

### 12.8 RunnableParallel（并行执行）

```python
# ============================================================
# RunnableParallel — 同一输入并行跑多条子链，结果合并为 dict
# 适合「翻译+摘要」、「多视角分析」、「多语言输出」等并行任务
# 内部自动并发执行各分支，性能优于顺序执行
# ============================================================
from langchain_core.runnables import RunnableParallel  # RunnableParallel: 显式并行 Runnable
from langchain_core.prompts import ChatPromptTemplate   # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                 # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
parser = StrOutputParser()                                   # StrOutputParser: 解析器

# 定义三条并行子链
translate_chain = (  # Runnable: 翻译链，输入 dict，输出 str（英文翻译）
    ChatPromptTemplate.from_template("把以下文本翻译成英文：{text}")
    | llm | parser
)

summarize_chain = (  # Runnable: 摘要链，输入 dict，输出 str（中文摘要）
    ChatPromptTemplate.from_template("用一句话总结以下文本的核心意思：{text}")
    | llm | parser
)

sentiment_chain = (  # Runnable: 情感分析链，输入 dict，输出 str（情感标签）
    ChatPromptTemplate.from_template("分析以下文本的情感倾向（正面/负面/中性），只输出一个词：{text}")
    | llm | parser
)

# RunnableParallel(key=runnable, ...) -> RunnableParallel
#   key     : str      — 结果字典中该子链输出对应的键名
#   runnable: Runnable — 并行执行的子链
#   输入     : dict — 所有子链共享同一份输入
#   返回值   : Dict[str, Any] — 各子链结果组成的字典
parallel = RunnableParallel(
    translation=translate_chain,  # Runnable: 翻译子链，结果存入 translation 键
    summary=summarize_chain,      # Runnable: 摘要子链，结果存入 summary 键
    sentiment=sentiment_chain,    # Runnable: 情感分析子链，结果存入 sentiment 键
)

text_input = {"text": "LangChain 是一个强大的 LLM 应用开发框架，让开发者能够快速构建 RAG 和 Agent 应用。"}

result = parallel.invoke(text_input)  # Dict[str, str]: 三条链并行执行的结果字典
print(f"英文翻译: {result['translation']}")  # str: 翻译结果
print(f"内容摘要: {result['summary']}")       # str: 摘要结果
print(f"情感倾向: {result['sentiment']}")     # str: 情感标签（正面/负面/中性）
```

### 12.9 RunnableBranch（条件路由）

```python
# ============================================================
# RunnableBranch — 根据条件将输入路由到不同子链
# 等价于旧版 RouterChain / MultiPromptChain 的现代写法
# 适合「按问题类型分流」、「多语言识别路由」等条件分支场景
# 更复杂的状态机/循环场景使用 LangGraph
# ============================================================
from langchain_core.runnables import RunnableBranch, RunnableLambda  # 分支 Runnable 组件
from langchain_core.prompts import ChatPromptTemplate     # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                   # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温保证分类稳定
parser = StrOutputParser()                                   # StrOutputParser: 解析器

# 条件判断函数 — 判断输入属于哪种类型
def is_math_question(x: dict) -> bool:
    """
    判断输入是否为数学计算类问题。

    Args:
        x: dict — 包含 question 键的输入字典

    Returns:
        bool — True 表示数学问题，False 表示非数学问题
    """
    q = x.get("question", "")  # str: 获取问题文本
    # 检查是否含有数学相关关键词
    math_keywords = ["多少", "计算", "+", "-", "*", "/", "等于", "平方", "开根号"]
    return any(kw in q for kw in math_keywords)  # bool: 任意关键词命中则为数学问题

def is_code_question(x: dict) -> bool:
    """
    判断输入是否为编程相关问题。

    Args:
        x: dict — 包含 question 键的输入字典

    Returns:
        bool — True 表示编程问题，False 表示非编程问题
    """
    q = x.get("question", "")  # str: 获取问题文本
    code_keywords = ["代码", "编程", "函数", "bug", "报错", "Python", "Java", "API"]
    return any(kw in q for kw in code_keywords)  # bool: 任意关键词命中则为编程问题

# 各分支对应的子链
math_chain = (  # Runnable: 数学助手链
    ChatPromptTemplate.from_template("你是一位数学老师，请详细解题：{question}")
    | llm | parser
)
code_chain = (  # Runnable: 编程助手链
    ChatPromptTemplate.from_template("你是一位资深程序员，请提供代码解答：{question}")
    | llm | parser
)
general_chain = (  # Runnable: 通用助手链（默认分支）
    ChatPromptTemplate.from_template("你是一位博学的知识助手，请回答：{question}")
    | llm | parser
)

# RunnableBranch((condition, chain), ..., default_chain) -> RunnableBranch
#
#   condition    : Callable[[dict], bool]
#                  —— Python typing 语法：Callable[[参数类型, ...], 返回值类型]
#                     内层 [] 是固定写法，表示「参数类型列表」；
#                     Callable[[dict], bool] 意为：接收 1 个 dict 参数，返回 bool
#                  —— RunnableBranch 内部依次调用 condition(input)；
#                     此处 input 为 branch 本次 invoke 收到的入参，不做任何变换：
#                       · branch 在链首端：input = 外部直接传入的值（本例为 dict）
#                       · branch 在链中间：input = 上一步 Runnable 的输出（类型由上一步决定）
#                       · branch 在链末尾：input = 上一步 Runnable 的输出
#                     因此 condition 的参数类型必须与 branch 实际收到的 input 类型一致
#
#   chain        : Runnable — 条件满足时执行的子链；接收与 condition 相同的 input
#   default_chain: Runnable — 所有条件均不满足时执行的默认链（最后一项，无需条件）；
#                  同样接收相同的 input
branch = RunnableBranch(
    (is_math_question, math_chain),   # (condition, chain): 数学问题 → math_chain
    (is_code_question, code_chain),   # (condition, chain): 编程问题 → code_chain
    general_chain,                    # Runnable: 默认链（无条件匹配时执行）
)

# 测试不同类型的问题路由
test_questions = [           # List[dict]: 覆盖三种分支的测试用例
    {"question": "12 * 34 + 5 等于多少？"},      # → 数学分支
    {"question": "Python 中如何处理 bug？"},      # → 编程分支
    {"question": "LangChain 是什么框架？"},       # → 通用分支（默认）
]

for q_dict in test_questions:        # q_dict: dict — 包含 question 键的输入字典
    print(f"\n问题：{q_dict['question']}")
    answer = branch.invoke(q_dict)   # str: 根据条件路由后执行对应子链的结果
    print(f"回答：{answer[:100]}...")  # str: 截取前 100 字展示
```

### 12.10 create_stuff_documents_chain（文档填充工厂链）

LangChain 提供了一组「工厂链（Factory Chain）」函数，用于快速组装常见的文档处理 / RAG 流程，无需手动拼接 LCEL 管道。下表列出最常用的几个：


| 工厂函数                                                     | 所在包                                          | 核心功能                                         | 典型场景                    |
| -------------------------------------------------------- | -------------------------------------------- | -------------------------------------------- | ----------------------- |
| `create_stuff_documents_chain(llm, prompt)`              | `langchain_classic.chains.combine_documents` | 将 `List[Document]` 一次性拼入 `{context}` 后调用 LLM | 文档量少、总长度在上下文窗口内的摘要 / 问答 |
| `create_retrieval_chain(retriever, combine_chain)`       | `langchain_classic.chains`                   | 将 Retriever 与问答链串联：先检索再回答                    | 标准 RAG 管道的一键封装入口        |
| `create_history_aware_retriever(llm, retriever, prompt)` | `langchain_classic.chains`                   | 先用对话历史重写用户问题，再交给 Retriever 检索                | 多轮对话 RAG，保证检索与历史上下文一致   |


> **选型建议**：文档量少 → `create_stuff_documents_chain`；标准 RAG → `create_retrieval_chain`；
> 多轮对话 RAG → 先 `create_history_aware_retriever` 再套 `create_retrieval_chain`。

```python
# ============================================================
# create_stuff_documents_chain — 将多个 Document 塞进一次 LLM 调用
# 适合文档数量少、总长度在上下文窗口内的「摘要/问答」场景
# 比手动 format_docs 更规范，是 LangChain 官方推荐的工厂链
# ============================================================
from langchain_classic.chains.combine_documents import create_stuff_documents_chain
# LangChain v1.x 中 langchain.chains 已移至 langchain-classic；
# 若使用 langchain<1.0 则可改为：from langchain.chains.combine_documents import create_stuff_documents_chain
from langchain_core.documents import Document                                 # Document: 文档数据类
from langchain_core.prompts import ChatPromptTemplate, PromptTemplate         # ChatPromptTemplate: 对话模板；PromptTemplate: 单字符串模板，用于格式化单篇文档
from langchain_openai import ChatOpenAI                                       # OpenAI 模型

llm = ChatOpenAI(model="gpt-4o-mini", temperature=0.0)  # ChatOpenAI: 低温保证事实性

# create_stuff_documents_chain 要求 Prompt 中包含 {context} 变量
# {context} 字段由工厂函数自动将 List[Document] 拼接后注入
stuff_prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一位数据分析助手，请基于给定资料精确回答问题，不要编造数据。"),
    ("human",
     "参考资料：\n{context}\n\n"  # {context}: str — 由工厂链自动填充为文档内容
     "问题：{question}"),         # {question}: str — 用户问题占位符
])

# create_stuff_documents_chain 完整签名：
#   llm                  : LanguageModelLike        — 语言模型实例（必填）
#   prompt               : BasePromptTemplate       — 包含 {context} 占位符的提示词模板（必填）
#   output_parser        : BaseOutputParser | None  — 输出解析器，默认 StrOutputParser
#   document_prompt      : BasePromptTemplate | None — 控制每篇 Document 如何格式化成字符串；
#                          输入变量可为 "page_content" 或 Document.metadata 中的任意键；
#                          默认只使用 Document.page_content
#   document_separator   : str                      — 多篇文档格式化后拼接时使用的分隔符，默认 "\n\n"
#   document_variable_name: str                     — 格式化后的文档字符串注入到 prompt 的变量名，默认 "context"
#   返回值: Runnable — 输入 {"context": List[Document], ...}，输出类型取决于 output_parser

# document_prompt: 定义每篇文档的格式，这里同时展示来源元数据
# 输入变量 "page_content" 自动取 Document.page_content
# 输入变量 "source" 自动从 Document.metadata["source"] 中获取
doc_format_prompt = PromptTemplate(
    input_variables=["page_content", "source"],   # list[str]: 模板中使用的变量名
    template="【来源：{source}】\n{page_content}",  # str: 每篇文档的格式化模板
)

stuff_chain = create_stuff_documents_chain(
    llm,                                    # LanguageModelLike: 语言模型实例
    stuff_prompt,                           # BasePromptTemplate: 包含 {context} 的主提示词
    document_prompt=doc_format_prompt,      # BasePromptTemplate: 每篇文档的格式化模板，默认只有 page_content
    document_separator="\n\n---\n\n",       # str: 多篇文档之间的分隔符，默认 "\n\n"
    document_variable_name="context",       # str: 拼接结果注入 prompt 的变量名，默认 "context"
)

# 准备文档列表（实际场景替换为 retriever.invoke(question) 的检索结果）
docs = [  # List[Document]: 待分析的文档列表
    Document(
        page_content="公司 2024 年总营收 1.8 亿元，同比增长 32%。",
        metadata={"source": "财报Q4", "page": 1},  # dict: 文档元数据
    ),
    Document(
        page_content="产品 A 销售额占比 55%，产品 B 占比 35%，其他产品占比 10%。",
        metadata={"source": "产品分析", "page": 3},
    ),
    Document(
        page_content="华东地区贡献 45% 营收，华南次之为 30%，华北为 25%。",
        metadata={"source": "区域分析", "page": 5},
    ),
]

# stuff_chain.invoke(input_dict) -> str
#   input_dict: {"context": List[Document], "question": str}
#   返回值    : str — 基于文档内容生成的回答
answer = stuff_chain.invoke({
    "context":  docs,                           # List[Document]: 文档列表，自动拼接为 context
    "question": "产品 A 大约贡献了多少营收？",  # str: 用户问题
})
print(answer)  # str: 基于三份文档的综合计算回答（约 1.8亿 × 55% = 0.99亿）
```

### 12.11 Callbacks（回调机制）

```python
# ============================================================
# Callbacks — LangChain 运行时事件钩子
# 允许在 LLM 调用的各个阶段插入自定义逻辑：
#   on_llm_start      : LLM 开始调用时触发
#   on_llm_end        : LLM 调用完成时触发（含最终输出）
#   on_llm_error      : LLM 调用报错时触发
#   on_chain_start    : Chain 开始时触发
#   on_chain_end      : Chain 完成时触发
#   on_tool_start     : Tool 开始调用时触发
#   on_tool_end       : Tool 调用完成时触发
# 常用于：日志记录、性能监控、Token 统计、调试分析
# ============================================================
from langchain_core.callbacks import BaseCallbackHandler  # BaseCallbackHandler: 回调基类
from langchain_openai import ChatOpenAI                   # ChatOpenAI: 对话模型
from langchain_core.prompts import ChatPromptTemplate      # ChatPromptTemplate: 提示词模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from typing import Any, Dict, List, Union                  # 类型提示工具
import time  # time: 时间模块，用于性能计时

class TimingCallbackHandler(BaseCallbackHandler):
    """
    计时回调处理器：记录每次 LLM 调用的耗时和 Token 使用量。

    Attributes:
        start_time: float — LLM 调用开始的时间戳（Unix 时间）
    """

    def __init__(self):
        """初始化回调处理器，设置计时器起始值。"""
        super().__init__()
        self.start_time: float = 0.0  # float: LLM 调用开始时间戳

    def on_llm_start(
        self,
        serialized: Dict[str, Any],  # Dict: LLM 配置的序列化信息（模型名、参数等）
        prompts: List[str],           # List[str]: 发送给非对话 LLM 的提示词列表
        **kwargs: Any                 # Any: 其他关键字参数
    ) -> None:
        """非对话 LLM（OpenAI text-completion 等）开始调用时触发，记录开始时间。"""
        self.start_time = time.time()  # float: 记录调用开始时间戳
        print(f"[Callback] LLM 开始调用，发送 {len(prompts)} 条提示词")  # 打印回调触发信息

    def on_chat_model_start(
        self,
        serialized: Dict[str, Any],       # Dict: Chat Model 配置的序列化信息
        messages: List[List[Any]],         # List[List[BaseMessage]]: 发送给 Chat Model 的消息批次
        **kwargs: Any                      # Any: 其他关键字参数
    ) -> None:
        """Chat Model（ChatOpenAI 等）开始调用时触发，记录开始时间。
        
        注意：ChatOpenAI 是 Chat Model，触发 on_chat_model_start 而非 on_llm_start。
        两者均在 on_llm_end 结束，因此计时逻辑在此方法中启动。
        """
        self.start_time = time.time()  # float: 记录 Chat Model 调用开始时间戳
        total_msgs = sum(len(batch) for batch in messages)  # int: 消息总条数
        print(f"[Callback] Chat Model 开始调用，发送 {total_msgs} 条消息")  # 打印回调触发信息

    def on_llm_end(
        self,
        response: Any,  # LLMResult: LLM 调用的完整响应对象（含 generations 和 llm_output）
        **kwargs: Any   # Any: 其他关键字参数
    ) -> None:
        """LLM 调用完成时触发，打印耗时统计。"""
        elapsed = time.time() - self.start_time          # float: 调用耗时（秒）
        token_usage = getattr(response, "llm_output", {}) or {}
        usage = token_usage.get("token_usage", {})        # dict: Token 使用量字典
        print(f"[Callback] LLM 调用完成，耗时 {elapsed:.2f}s")     # 打印耗时
        if usage:                                          # Token 使用量不为空时打印
            print(f"[Callback] Token 消耗: 输入={usage.get('prompt_tokens', '?')}, "
                  f"输出={usage.get('completion_tokens', '?')}")

    def on_llm_error(
        self,
        error: Union[Exception, KeyboardInterrupt],  # Exception: 捕获到的异常对象
        **kwargs: Any                                 # Any: 其他关键字参数
    ) -> None:
        """LLM 调用报错时触发，打印错误信息。"""
        print(f"[Callback] LLM 调用失败: {error}")  # 打印异常信息

# 将回调处理器注入模型
timing_handler = TimingCallbackHandler()  # TimingCallbackHandler: 自定义回调处理器实例

cb_model = ChatOpenAI(
    model="gpt-4o-mini",
    temperature=0.0,
    callbacks=[timing_handler],  # List[BaseCallbackHandler]: 绑定回调处理器列表
)

cb_prompt = ChatPromptTemplate.from_messages([("human", "{input}")])  # 提示词模板
cb_chain   = cb_prompt | cb_model | StrOutputParser()  # Runnable: 带回调的完整链

result = cb_chain.invoke({"input": "用一句话解释 Callback 模式"})  # str: 调用结果
print(f"回答：{result}")  # str: 模型生成的回答
```

### 12.12 Legacy Chain 对照表（旧版写法迁移指南）

```python
# ============================================================
# LangChain v1.x 版本迁移说明
# ------------------------------------------------------------
# v0.3.x 中已废弃但可用的旧式 Chain，在 v1.x 中已完全从 langchain 包移除，
# 统一迁移至独立包 langchain-classic（需单独安装：pip install langchain-classic）。
# ============================================================

# ┌────────────────────────────────────────────────────────────────────────────────┐
# │  旧写法（v0.3.x 废弃 / v1.x 移至 langchain-classic）                          │
# │                              ↓ 现代推荐写法（LCEL / v1.x 原生）               │
# ├────────────────────────────────────────────────────────────────────────────────┤
# │ from langchain.chains import LLMChain           → prompt | llm               │
# │ LLMChain(llm, prompt, parser)                   → prompt | llm | parser      │
# │ .run() / .predict()                             → .invoke() / .batch()       │
# │ SimpleSequentialChain                           → chain1 | lambda | chain2   │
# │ SequentialChain（多变量）                       → RunnablePassthrough.assign  │
# │ RouterChain / MultiPromptChain                  → RunnableBranch              │
# │ ConversationChain + 旧 Memory                   → MessagesPlaceholder + 历史  │
# │ ConversationalRetrievalChain                    → LCEL RAG 链                 │
# │ LLMMathChain                                    → @tool 计算器 + Agent        │
# │ StuffDocumentsChain（旧类）                     → langchain_classic.chains…  │
# │ AgentExecutor / create_tool_calling_agent       → create_agent()              │
# │   （v1.x 中导入：from langchain_classic.agents import AgentExecutor）         │
# └────────────────────────────────────────────────────────────────────────────────┘

# ---- 旧写法示例（仅作对比，新项目禁止使用，v1.x 须改用 langchain-classic）----
# from langchain_classic.chains import LLMChain  # v1.x 旧式写法（需安装 langchain-classic）
# chain = LLMChain(llm=llm, prompt=prompt)
# result = chain.run("输入内容")  # ❌ .run() 已废弃，请用 .invoke()

# ---- 新写法示例（LCEL 推荐）----
from langchain_core.prompts import ChatPromptTemplate      # ChatPromptTemplate: 对话模板
from langchain_core.output_parsers import StrOutputParser  # StrOutputParser: 字符串解析器
from langchain_openai import ChatOpenAI                    # ChatOpenAI: 模型实例

llm    = ChatOpenAI(model="gpt-4o-mini", temperature=0.7)  # ChatOpenAI: 模型实例
prompt = ChatPromptTemplate.from_messages([("human", "{input}")])  # 提示词模板
parser = StrOutputParser()  # StrOutputParser: 解析器

# ✅ LCEL 写法：prompt | llm | parser
modern_chain = prompt | llm | parser  # Runnable: 现代 LCEL 链

result = modern_chain.invoke({"input": "LCEL 相比旧版 LLMChain 有什么优势？"})  # str: LCEL 链执行结果
print(result)  # str: 模型对 LCEL 优势的解释

# 核心原则：「一切皆 Runnable，管道即架构」
# 所有组件（Prompt / Model / Parser / Retriever / Tool）均实现 Runnable 接口
# 用 | 串联即可构建任意复杂的 LLM 工作流，无需再学习各类专用 Chain 类
```

---

## 十三、总结

### 13.1 LangChain 核心架构回顾

```
langchain-core（核心抽象层）
├── Runnable 接口       → .invoke() / .stream() / .batch() / .ainvoke()
├── BaseMessage         → HumanMessage / AIMessage / SystemMessage
├── BasePromptTemplate  → ChatPromptTemplate / PromptTemplate
├── BaseOutputParser    → StrOutputParser / JsonOutputParser
└── BaseRetriever       → VectorStoreRetriever

langchain（高级封装层，v1.x）
├── LCEL 管道    → prompt | llm | parser（| 运算符串联 Runnable）
├── Agent        → create_agent（v1.x 推荐）
└── Memory       → RunnableWithMessageHistory（deprecated 1.3.3）/ ChatMessageHistory

langchain-classic（遗留兼容层）
├── Agent        → create_tool_calling_agent / AgentExecutor
└── Chain        → LLMChain / SequentialChain / ConversationalRetrievalChain 等

langchain-openai（OpenAI 集成）
├── ChatOpenAI          → 聊天模型接口（支持 Tool Calling / Structured Output）
└── OpenAIEmbeddings    → 文本向量化（text-embedding-3-small 推荐）

langchain-community（社区集成，500+ 种）
├── Document Loaders    → TextLoader / WebBaseLoader / PyPDFLoader 等
├── Vector Stores       → FAISS / Chroma / Milvus 等
└── Chat Histories      → ChatMessageHistory / RedisChatMessageHistory 等
```

### 13.2 学习路径建议


| 阶段        | 目标              | 核心知识点                                                                                                         |
| --------- | --------------- | ------------------------------------------------------------------------------------------------------------- |
| 入门        | 跑通第一个 LCEL 链    | `ChatOpenAI` + `ChatPromptTemplate` + `StrOutputParser`                                                       |
| 进阶        | 构建 RAG 应用       | 文档加载 + 分块 + 向量库 + `as_retriever()` + `RunnablePassthrough`                                                    |
| 高阶        | 实现单 Agent 工具调用  | `@tool` + `create_agent`（v1.x 推荐）；或 `create_tool_calling_agent` + `AgentExecutor`（需 `langchain-classic`，向后兼容） |
| LangGraph | 构建多 Agent 状态机流程 | `StateGraph` + 节点/边定义 + `MemorySaver` 持久化 + 条件路由 + Human-in-the-loop                                          |
| 生产        | 可观测 + 持久化       | LangSmith 追踪 + Redis/Postgres `Checkpointer` + 异步 `.ainvoke()` + LangGraph `interrupt`                        |


### 13.3 关键注意事项

```python
# ✅ 推荐做法
from langchain_openai import ChatOpenAI               # 使用集成包，不用 langchain.llms
chain = prompt | llm | parser                          # 使用 LCEL | 运算符
llm.with_structured_output(MyModel)                   # 结构化输出使用 Pydantic
retriever = vectorstore.as_retriever(search_kwargs={"k": 3})  # 明确设置检索数量 k

# ❌ 已移除 / 废弃做法（v1.x 中 langchain.chains 已移至 langchain-classic，直接 import 会报错）
# from langchain.chains import LLMChain              # ❌ v1.x 中 ModuleNotFoundError，用 LCEL 替代
# from langchain.chains import SequentialChain       # ❌ v1.x 中 ModuleNotFoundError，用 LCEL 替代
# from langchain.chains import ConversationalRetrievalChain  # ❌ v1.x 中 ModuleNotFoundError，用 RAG 链替代
# from langchain.agents import AgentExecutor         # ❌ v1.x 中 ImportError，用 create_agent() 替代
# retriever.get_relevant_documents(query)            # ❌ 已废弃，用 .invoke() 替代
```

### 13.4 相关资源


| 资源                  | 链接                                                                                                         |
| ------------------- | ---------------------------------------------------------------------------------------------------------- |
| 官方 GitHub           | [https://github.com/langchain-ai/langchain](https://github.com/langchain-ai/langchain)                     |
| Python 文档           | [https://docs.langchain.com/oss/python/](https://docs.langchain.com/oss/python/)                           |
| API 参考              | [https://reference.langchain.com/python/langchain/](https://reference.langchain.com/python/langchain/)     |
| LangSmith（可观测性）     | [https://smith.langchain.com](https://smith.langchain.com)                                                 |
| LangGraph（复杂 Agent） | [https://github.com/langchain-ai/langgraph](https://github.com/langchain-ai/langgraph)                     |
| 社区集成列表              | [https://docs.langchain.com/oss/python/integrations/](https://docs.langchain.com/oss/python/integrations/) |


